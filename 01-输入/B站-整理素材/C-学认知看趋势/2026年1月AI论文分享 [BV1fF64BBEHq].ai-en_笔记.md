---
category: C
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 2026年1月AI论文分享 [BV1fF64BBEHq].ai-en.srt
status: reference
---

# 2026年1月AI论文分享

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

Welcome back to continue sharing papers with everyone This year's first paper share Let's try something different What's different this time? This time, we will focus on a specific topic For example, there were roughly over 5,000 papers in January. From these 5,000-plus papers carefully selected papers focused on a specific topic to delve into let's talk about So what topic have we chosen for today? agent planning and orchestration When it comes to planning, perhaps everyone thinks of ReAct first. then you probably can't think of anything else Today, we're going to cover everything beyond just ReAct let's go through them together we will establish a systematic understanding Also, this year's papers Because some friends previously mentioned that The share link for this paper always fails to open so I've also moved it to a Feishu Knowledge base And then every month, a new knowledge base will be added like this. and see if this method This makes it easier for more friends to access Then, I will put the link to the knowledge base in the video's About section. Alright, Let's get back to the topic Let's begin Today's topic is the planning and orchestration of agents Let's start by discussing the classification. Generally speaking, we refer to this, uh, agent its entire execution process Let's categorize this How can we categorize them? First, the simplest way is to categorize by quantity, right a single-agent execution mode and a collaborative execution mode for multi-agent systems Also, we have another classification method is based on its execution mode We can divide them into dynamic and static This static mode is quite straightforward to understand simply put, I have this predefined an execution flowchart of an agent For example, like the Coze Define we discussed earlier Then through this static diagram approach the execution path of the agent has been pre-planned This approach is called static. Now that we have static there must be dynamic Now, as for dynamic execution As we just mentioned The most typical example is the ReAct mode What is the ReAct model? That is, I will first give it a series of execution principles What is your ultimate goal I will tell you and then your usage path Actually, I won't predefine this definition for you but by having you first think about what needs to be done in the current step and then execute an action for example, calling an MCP interface to collect some data after the collection is complete rethink again After finishing the thinking process, then proceed to make the next plan It's like drawing a circle, constantly thinking and acting. Think Act Hey This is ReAct Apart from the ReAct approach there are many other dynamic modes for example, reflection is this reflection mode that is, after I perform each action, I'll reflect once again How am I doing with this current step? conduct an evaluation then proceed to the next step of planning that is reflect reflection whether it's react or reflection You can imagine its execution chain as being linear that is to say, one point after another This is a line. What about non-linear approaches? and columnar like TOT The execution process is no longer linear. but at the beginning, uh, when I'm designing or making a plan I will create multiple branches Then for each step, uh, I might try it out or I first evaluate which steps to try Then after trying it out Then under this specific branch I may further establish branches try again this unfolded type That is no longer linear then Compared to ReAct It can be seen that this execution path is significantly broader or much broader That is to say, it will adopt a more divergent way of thinking to find the potentially correct execution path so its success probability is higher than that of ReAct At the same time, the time consumed or the consumption of tokens will be much higher than ReAct each has its pros and cons So there are also many options in dynamic execution paths. but there are also many other approaches we will see today uh Clearly, when considering the quantity, ReAct it is this dynamic single-agent such an execution mode As for the tree mode mentioned earlier It is a dynamic multi-agent execution mode. as mentioned earlier, define or or CoS It is a static multi-agent execution mode. So now, whether we're talking about single-agent multi-agent systems Multi-agent systems are being discussed more and more frequently. We'll talk about single-agent in a moment It does have some limitations. multi-agent systems It can now be considered mainstream Okay, we just talked about some background knowledge. Now let's dive into today's papers Now let's go through them one by one The first paper is It's about the architecture of multi-agent systems. This paper, 13671, discusses the concept from a conceptual perspective. The title of this paper discusses intelligent agents multi-agent orchestration system from the architecture This communication protocol and enterprise applications are discussed from multiple perspectives. The paper adopts an evolutionary approach to unfold its discussion. It's about starting from a single-agent system and step by step evolved into the current multi-agent this process we won't delve into this in detail Let's just look at this final evolved architecture diagram. Help us systematically organize Now in multi-agent systems should include or what its overall architecture should look like what components should it include Let's first systematically categorize and break this down In a multi-agent system First of all, there must be multiple agents, right? Here, the agents are divided into several categories including these so-called worker agents abstract-type service-oriented agent and supportive agents No matter how you categorize it Anyway, there are many agents there among the agents The communication protocol can be A to A In addition to the agents There are also some tools or external interface data The agent calls external tools interfaces or The MCP protocol is used. These two protocols are basically the mainstream options currently. Apart from these two parts The most important thing we're going to discuss today is the agent orchestration layer He abstracts this into a separate layer for discussion. This part is about the orchestration and planning of agents Within agent orchestration and planning, it is further divided into He believes it consists of many components or modules. That is also during the process of orchestration or planning by this agent what you need to consider The first part is definitely to consider the plan or your policy part what principles do you follow to proceed the planning of your agent With these principles in place It is also necessary to control the entire execution process. control the execution process of the agent For example, which is the first agent it has completed execution I need to transfer this control to the second agent, right? The transfer of control rights is considered in this part. Then each agent needs to determine which resources to use This is also an issue that orchestration planning needs to consider. The third aspect is the overall execution process of your the transition of a state What has the system's state or context changed into? The fourth aspect is the overall quality control For example, to which step has the execution progressed? The current response is already very poor. Should I terminate early Do not proceed any further This is all about controlling the quality of the response. Finally, there are also some observation tools monitor the entire execution process This is the entire orchestration and planning layer. some issues to consider Next, let's look at some practical cases let's see how they are applied in this orchestration and planning layer how to apply it in actual production In practical applications within multi-agent systems a concept called routing is frequently used This is most profoundly reflected in this paper 742. This paper is titled Adaptive Orchestration this self-evolving multi-agent system Then let's take a look at the front part, which we'll skip for now Let's look directly at its core architectural diagram As we just mentioned, in a multi-agent system each agent or each agent expert each has its own expertise and the tools they can utilize Then when a request comes in the first problem I need to solve which expert should handle this current request This is the first thing I need to consider So in practice, we often we will introduce a router role this routing decides I need to assign the current request to which expert This routing is typically also implemented by a large language model. In this paper, He refers to this routing as a front desk. You can also understand it this way very vivid a request comes in first assign it to a front desk for processing But if we use a large language model to do this what problem will there be is a very obvious For example, when I have 100 experts behind me I need to inform the front-end or the large language model what are the capabilities of each expert What is this expert good at? what that expert is good at But when I have 100 experts This set of prompts describing the experts' capabilities would become very lengthy occupies a very large chunk of my context For example, describing each expert I use 500 tokens Just describing these 100 experts alone takes up 50,000 tokens plus there might be historical conversations The context has become very lengthy At this point, the foreground is very prone to losing focus he wouldn't know What should I answer you or what to talk about then the process of routing to a certain expert will also be inaccurate To address this issue this paper then proposes a series of various solutions This paper proposes a self-evolving the architecture of mixture-of-experts routing It sounds quite mysterious Actually, it's quite easy to understand once explained. How exactly is it done? First, he has an expert registry that is, all experts will register in this registry What do I do? I have registered my expertise in there Then, not all experts are active. maybe only about ten experts or so The most commonly used ones are placed inside first. These ten experts are highly active. The other 90 experts are inactive. Those inactive ones will not be injected into the foreground context. For example, the frontend temporarily cannot see them. Then a request comes in If it hits one of these ten experts That would of course be the best directly assign it to a certain expert for processing Then after this is processed return the result This is the best case scenario What if the capability I hit not one of these ten experts What should I do then It adopts an asynchronous compensation mechanism Well, I can't answer you this time. OK no problem However, it establishes an asynchronous queue below and then take this failed This part is matched with the list of all experts and then match it my current capability in which aspect is it lacking Then, regarding this deficient part which expert happens to provide Then, I will register this expert back. become active That means the number of active experts increases combined with an elimination mechanism for example, some experts are not frequently used I will change it from an active state to an inactive state Through this mode continuous activation and elimination What remains in the end are the ones most frequently used by the user those experts This can prevent the context from becoming too lengthy and avoid selecting the wrong tools, among other core issues It also avoids enormous resource consumption that's the advantage of this system To summarize Such a self-evolving system can achieve dynamic loading unload this specific agent rather than static deployment The entire agent system is no longer a static execution process but rather a dynamic runtime process and then based on the actual situation and the user's practical usage needs to dynamically combine these expert modules in real-time thereby enabling on-demand and continuous evolution control resource consumption This is the first paper we share today this self-evolving multi-agent system As just mentioned, a large language model is used as the front-end as a router As mentioned earlier, this approach It has its shortcomings Now, more often, what is actually being used is using vector-based methods for routing Let's take a look at this paper This paper, titled 'Beyond gm3pro' Revisiting Large Language Models a paper on routing and integration Well, it's actually about an improvement to the current mainstream vector routing approach But before we dive into it we need to talk about What is the current mainstream vector routing approach? and then talk about its improvements So what is the current mainstream approach to vector routing? Let's take a look at this diagram Look at the figure in the top right corner, it's roughly like this After the user inputs a query statement Ah, for example, if you want to do something like help me with something then it is sent to the rou routing large language model or the frontend It will first perform a vector matching that is, I also have many expert models or agents behind me Then during the training process I will assign an, uh, something like a capability vector what it can do or what questions he answers has a relatively high success rate such a vector as an identifier vector for its model Then we will simultaneously vectorize the user's input also perform embedding vectorization Then the two vectors undergo cosine similarity matching this way we can find What is the most suitable expert model to answer this question? you've identified it Then through this vector matching I'll have my front-end model the authority to answer this question pass it to the expert model let him answer At this point, sometimes one is selected sometimes multiple will be selected When multiple are selected I also involve a final aggregation process For example, I have selected three experts, I will merge the results from your three experts or make a selection or choice can potentially address specific scenarios and then return the final result to the frontend which is currently the mainstream vector-based routing method This vector-based routing method has many issues. This paper begins to address these issues The first improvement is that after a user inputs a statement, I match the capabilities of this model the model capability no longer includes only the user's query vector but also includes the vector of the model's historical responses Then perform cosine similarity matching on these two parts That is to say, when actually matching with the user My model contains a bit more information. This not only includes what he has processed in the past those user questions also includes the part where it previously answered users may also include the length of his responses to users the consumption of tokens, etc. I take all these factors into consideration This way, the matching results will be more accurate. The second part of the improvement lies in the selection process. In the past, I just select the most suitable or the most capable one this particular model to handle the task Now, in this paper, he did some filtering Or I can perform filtering by domain I'll first categorize this user's question which field it belongs to Then I'm not using all these models filter within the expert-expert models Instead, I need to first filter out the experts in this field and then perform similarity judgment with the experts in this field Finally, I'll apply weighting that is, the factors I consider carry more weight, uh more comprehensive The third improvement is the combination method For example, in the past, there would be a problem when, for example, two out of three expert responses are good when one is really bad the previous integration result will also include this terrible expert And in his paper, he would filter out those poorly answered ones This is what we call self-adaptation I discard some poor results first I don't want it I only want the answers from the best experts and then integrate them thereby improving the effectiveness of this response This paper achieves through these three methods Ultimately achieving the use of multiple models This paper is also called Beyond JIMMIN3pro, which uses multiple models this, uh, through this routing approach including a solution for the response effectiveness of a single model Regarding the application of routing, The most complex or most impressive paper this month is Ah, this is paper 308 called Agree Agent and contract-driven planning and capability-related tool orchestration a framework in the real world this paper It's about the set of agents they designed a specific case applied in real-world agriculture So what does the specific architecture look like? Let's first look at the first diagram Three diagrams are used to illustrate. The first diagram provides an overall perspective its frontend is also connected to a router The router is only responsible for routing two branches The first one is called the fast path or the ta path It actually mimics human fast thinking and slow thinking This approach uses the fast path to handle simple problems. Simple tasks are assigned to this path. then for more complex tasks it will be routed to the slow-thinking path So the design of this routing is actually quite simple just needs to determine the complexity of this current task is sufficient and then decide which path to throw it to Then let's take a look at this The fast path corresponds to the fast-thinking route This path is quite straightforward a typical RG system there are just one or two agents in it and there's no complex orchestration either when a problem comes up After I finish processing multimodal tasks are handed over to the multimodal module After processing, the result is simply handed over to you and that's it. Simply do a bit of enhancement and that's it. The key point is the diagram below that is, the so-called slow thinking path Let's take a closer look During this slow-thinking execution process It does not have a specific execution graph. So the first stage is called the planning phase Creating a plan also follows a process which involves multiple individuals proposing various plans Then these individuals debate and revise with each other continuously refine the plan Finally, we arrive at a relatively mature a reliable execution plan such an execution plan can be understood as a directed acyclic graph or an execution flowchart This gives us Then for each node or step in the plan I need to generate a requirement contract In this requirement contract, it is described in a relatively abstract manner depicts this stage What I need to do Then what capabilities and tools do I need What are the inputs and outputs of this step? are relatively speaking described Now that we have such a contract, Then, I will proceed with this contract contact the tool center I'm going to apply for the relevant tools For example, I need to use a search tool for one of them then I need to apply for it get the search tool check if this tool is available in the library If it is found That would be great to add it in then How do I use this tool I found? What is the input What is the output Now I understand everything There is also a negotiation process here, which is like a tug-of-war Through the process of negotiation I have identified the most suitable tools and components Alright The execution graph is ready which tools to invoke in each execution graph have been determined The next step is to execute according to the plan. Finally, there is also a verification step after the execution. That's it. From here we can see that In fact, this slow processing procedure is quite complex. If we try to understand it somewhat like that if we understand the way of defining workflows it is during runtime dynamically recreate this workflow and the creation process involves a lot of back-and-forth and also based on the required capabilities query the tools I need from the tool library and then perform a series of orchestrations Finally, the process is determined and executed. To summarize First, it is divided into fast thinking and slow thinking to solve simple tasks Complex tasks enter slow thinking During the process of slow thinking The plan is dynamically formulated is determined at runtime based on the complexity of the task This avoids using a one-size-fits-all approach for all problems but tailor the approach based on the specific problem's complexity When designing specific plans, also through this multi-step debate approach to reduce errors in this plan thereby enhancing the executability of the plan Then at each step, it's through this contractual form, uh, determines which tools to execute in each step enhanced its controllability and interpretability brilliant A highly referential paper Alright, let's go back to the mind map The last paper to share is a review paper This paper and it's the best paper I've read this month. This paper is titled 'AI Agent System Architecture, Applications, and Evaluation' So this is a review paper It provides a comprehensive discussion on all aspects of AI agents. Let's take a look at its table of contents The first part introduces Part Two: The Paradigm of Intelligent Agents This is also one of its features this paper contains numerous diagrams There are roughly over 20 figures Each diagram is meticulously drawn this agent in the current stage For example, it talks about how to build an agent He depicted each step with great clarity Then, Chapter 3 is about agent learning. The learning section mentions this part about agent strategies or the execution strategy and mechanism part This is actually the part we'll be focusing on later Chapter 4 focuses on the classification of agents. Chapter 5 is about the specific applications of the agents. Chapter 6 evaluates this entire process We can represent it with such a diagram is the overall framework of the paper Today, since we're discussing agent planning and routing We are not covering all parts So we will focus on This section is about, um, planning and routing its learning and evolution which is part of its Chapter 3 Okay Let's take a look at this diagram that is, during the execution process of the agent In what aspects can we optimize it? First and foremost, at the first level It must be at the model level pre-training model level I train this to make the model perform well then its planning capability or routing capability will definitely improve The second part is the engineering level of the agent system. That includes the fundamental architecture of the engineering. the division of modules then it calls the interface to return, uh The process of calling the interface and returning results is more stable more comprehensive data This will definitely enhance the overall execution performance as well. So the final part is the planning and scheduling we discussed today This part is the control phase including your controller Can you optimize it Can your prompts be written better Can your strategy be continuously optimized? This part can also enhance the execution capability of the agent. Now let's dive into the details. He proposed several specific approaches to enhance your agent's execution capability The first approach is a reinforcement learning solution Through reinforcement learning that is, let the agent continuously interact with the environment let it experience setbacks and then enhance its execution strategy This part actually enhances the model's capabilities enable the model to adapt to the environment The plans made will be more reliable and trustworthy. Then the next part The second example he talked about is called imitation learning This part is also mainly used to train the model. I will provide you with more of these an execution path of the agent expert Use these execution paths to train the model the plans it makes or make the plan more reliable like the agricultural case we mentioned earlier our entire execution plan it is designed by a large model design this execution plan However, the approach he adopted at that time through multiple rounds of tugging and debate to enhance the reliability of the plan Actually, the more reliable approach in academia is is to provide it with more training samples continuously train the model to make the planning more reasonable and reliable This approach is called imitation learning. The third method is what we call the RGB mode after training the model. often mentioned as what he calls the RGB mode This part is not about training model parameters. but rather designing some traffic regulations the classification diagram we initially mentioned those mentioned in it, those solutions For example, this rule-based approach like 'if-else' if this then that or based on this kind of flowchart For example, the previously mentioned 'define' or Kozi draw the flowchart in advance define your execution process in advance or this kind of tree-based is executed through a tree-like structure for divergence This includes both dynamic also includes static He categorized all of these under the traditional RGB-mode Agent system which is the orchestration capability we're discussing today Finally, he mentioned one approach here, ah, which is contextual learning This approach is actually more advanced the model has already been trained the planning and routing have also been designed So during execution, I can also adjust the specific execution process through prompts further adjust the specific execution process Through these four diagrams In fact, we can understand the entire agent execution process This strategy is actually hierarchical. It includes the training of the underlying model model fine-tuning including using reinforcement learning to adjust its planning the output of such strategies Then comes the mid-level orchestration capability of your system how the system plans the execution process of agents including using routing or or using other mechanisms to the top level fine-tune the execution effectiveness of the entire agent using prompts These aspects These layers collectively determine the ultimate behavior of your agent system At the same time, it is also hybrid Hybrid means that I can have both dynamic it can also be static I can use static not defining the main process or supplementary rules and then in this way use workflows to ensure overall reliability At the same time, introduce AI models at uncertain stages or nodes It is also feasible to use the large language model for dynamic reasoning. Finally, he actually proposed the so-called agent selection, that is, the so-called orchestration process It's actually a trade-off issue If you want to improve the performance of this agent essentially, it's about these variables make trade-offs according to your needs For example, if you want reliability then you can define the process in advance then you have reliability If you want flexibility then you can use this search-based this search-based planning approach such as this tree-based you gain flexibility but at the same time, while you have flexibility, your costs and time increase So when actually designing a powerful agent it is necessary to consider from multiple perspectives tailor it to your specific scenario plan a reasonable execution path At the same time, it's also necessary to consider each of these specific elements balance these elements against the cost Figure out exactly what you want Only by designing a system in this way can it be powerful and reliable. Alright That's all for this month's share on agent planning and orchestration the related papers mentioned Hope you find it helpful Remember to like and follow! See you next time

## 标签
[]
