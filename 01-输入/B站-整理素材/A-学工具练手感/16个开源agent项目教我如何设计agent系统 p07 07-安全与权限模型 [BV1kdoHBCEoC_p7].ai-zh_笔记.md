---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p07 07-安全与权限模型 [BV1kdoHBCEoC_p7].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p07 07-安全与权限模型

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

欢迎来到第七讲安全与权限模型 这一章我们要聊的是一个被很多开发者忽视 但其实极其重要的问题 当AI agent拥有了真实的系统访问权限 它到底能造成多大的损害 大家想象一下这个画面 你的AI agent可以执行shell命令 读写文件 调用外部API 这相当于什么 相当于你把一台电脑的root权限 给了一个语言模型 这个模型可以被欺骗 可以判断失误 可以陷入循环 一个被攻击或出错的AI agent能造成的损害 和一个拿到用户权限的恶意程序没有本质区别 它可以删除文件 窃取密钥 建立反向shell向外泄露数据这一章 我们通过分析六个真实开源项目的安全设计 来 看看不同团队对这个问题 给出了怎样不同的答案 这些不同的答案 其实揭示了各自对AI可信度的根本假设 这不是简单的工程细节 而是设计哲学的核心分歧和好 这六个项目 对安全问题给出了两种截然不同的哲学回答 我把它叫做主动防御派和被动隔离派 ghost的假设是什么呢 AI本身不是敌人 但可能被攻击或判断失误 所以他的安全措施针对的是外来威胁 操控agent和agent的偶发错误 防御方式是在每次工具调用之前过一遍 五层检查列nano cloud的假设则完全不同 AI可能是敌人或者已经被攻破 他针对的是agent本身成为攻击者 这个最坏情况 防御方式是不给agent任何他不该有的访问权限 注意这不是通过代码检查来实现的 而是通过内核级的文件系统隔离 简单来说 goose防的是恶意内容注入agent 让他做坏事 nana crow防的是agent做了坏事能波及多远 这是安全工程里两个永恒的经典思路 主动防御和被动隔离 他们针对的是不同的攻击向量 好 现在我们来深入看看goose的五层检查链 是怎么工作的 这是他在create goose aspect to inspection to inspection us里实现的安全架构 每次工具调用请求进来就会串行 经过这五个检查器 第一层是security inspector 用静态模式匹配来检测八大类威胁文件系统 破坏远程代码 执行反向shell等等 注意这里有个关键设计细节 security inspector不直接deny 他只输出require approval 让用户最终决定这样做是为了避免误判 永久拒绝合法操作 只有deny操作来自用户明确拒绝 或者goose mode chat模式 第二层是egress inspector 专门做出网目标检测 它不看你命令的形式 而是看你要连接到哪里 合法的课到GITHUBAPI和恶意的刻到攻击者服务器 语法上可能完全相同 但目标不同 这里通过GUCALODMZ环境变量配置信任域名 白名单 第三层是adversary inspector 这是最有争议的设计 用一个LLM来审查 另一个LLM生成的shell命令 这是可选功能 需要额外的配置文件激活 第四层是permission inspector 基于gooose mode决定是否需要用户确认 第五层是repetition inspector 做循环检测 如果相同工具用相同参数被调用 超过阈值次数就提示用户agent可能陷入循环 每层检查完输出三种结果 allow放行 deny拒绝require Approval 需要用户审批 我们来仔细看看第一层security inspector的实现细节 他在crates goose security patterns security patterns里定义了八大威胁类别 文件系统破坏 比如RM rf或d d if f等于d if f除以zero 这种直接往磁盘上写零的命令 远程代码执行curl管道 bh get it管道 这是最经典的工期向量 数据外发 把文件内容往外传系统修改 改pass改crown tap 还有网络访问NC建立反向shell进程操作q-9 杀掉关键进程 权限提升 Sudo kim777 开放权限命令注入通过分号拼接的命令列 每个类别都有对应的风险等级 从load0.45到critical的0.95 critical级别的典型例子包括 R m r f buying at qu RSDD直接往deaths的写数据 NCE建立反向shell 但是这里有个重要的局限性 静态正则模式对混淆攻击是无效的 比如arm off或者base64编码的命令 静态匹配根本识别不了 而且他只能看到单条命令 无法检测跨工具的组合攻击 比如工具A先写文件 工具B再执行这个文件 这是adversary inspector存在的原因 本这是goose最独特也最有争议的设计 值得单独深入分析 Adversary inspector 让一个LLM审查另一个LLM生成的shell命令 问他根据这些规则 这条命令是否应该被阻止 这里有两个潜在的致命问题 第一个问题叫共享弱点 审查LLM和被审查 LLM可能来自相同的训练分布 共享相同的感知盲点 如果攻击者知道 这两个LLM对危险命令的判断边界 他完全可以构造出通过审查但仍然危险的命令 这本质上是用同一种感知能力 检验另一种同类感知能力 两个盲人互相检查视力 谁能发现共同的盲区呢 第二个问题更加致命 fail open默认规则明确写着 Err on the side of allow 如果审查LLM的API调用失败 网络不稳定 API过载配置错误 默认结果就是allow 这意味着在网络不稳定的时候 adversary inspector等效于不存在 这是可用性优先于安全性的明确取舍 但更危险的是 用户可能根本不知道 自己当前处于无LOLM审查的状态 这个设计的真正价值在于处理静态模式匹配 无法覆盖的语义及威胁 比如一个看似正常的备份脚本 实际上是在把数据库导出到外部服务器 但它绝对不应该被当做最后一道防线 好现在我们来看第四层 permission inspector背后的goose mode系统 这本质上是一个用户和agent之间的信任协议 有四个档次 对应四种不同的使用场景 auto模式 所有工具自动批准 面向开发者自动化流程 信任agent能做出正确判断的场景 approve模式 所有工具都需要用户确认面向审计场景 每个操作都要看到 并确认适合临时检查agent在做什么 smart approve模式 基于MCP标准的工具注解来智能分级 red only hint标记为true的工具 比如读文件查状态 自动批准destructive hint或open world hint 标记为true的破坏性或开放世界操作 需要用户确认这是最适合日常使用的模式 读操作不打扰写操作才询问chat模式 所有工具拒绝执行纯聊天模式 探索agent能做什么 但不想真正执行 这里有个关键工程细节 绘画及权限记忆 当用户选择本会话 始终允许某个操作后 permission manager会记住这个决定 不再重复询问 但注意它不跨绘画持久化 每次新绘画都会重置 还有一个很贴心的设计 goose mode支持运行时动态切换 通过p t sessions it mode这个API 你可以随时从auto切换到approve 去审查agent在做什么 检查完再切回来 这是用户体验和安全性的最优平衡 好现在我们来换一个完全不同的思路 NANOCLW的被动隔离方案 这里有一个非常反直觉但极其重要的安全原则 我必须让大家理解清楚 agent不是因为权限被拒绝才访问不了SESH 而是这个路径在容器文件系统里根本不存在 听起来差不多 实际上有本质区别 权限被拒绝意味着这个路径确实存在 只是被代码逻辑拦截了 但代码逻辑可以有bug 可以被绕过 可以因为配置错误而失效 路径不存在 是内核级别的现实 容器内的文件系统命名空间是独立的 宿主机的路径 在其中物理上不存在 这不依赖任何代码的正确性 Neoclaw 通过docker或apple的沙箱容器实现这个隔离容器 以UID1000非root用户运行 而且加了room m标志 容器退出就销毁 不留持久攻击据点 S c h w et cetera passpbus 这些敏感路径在容器里完全不存在 这就是被动隔离的精髓 不尝试检测和拦截危险操作 而是从根本上消除操作的可能性 不NANOCLUDE Mount a lowest jason 配置文件在项目目录之外 agent根本访问不到 这个配置 实现了三重关键检查 第一重检查是容器内路径 验证路径不能是绝对路径 也不能包含 这是防止路径穿越的基础 第二重检查是real path解析 这个太关键了 大家想象一下 攻击者创建一个符号链接 safe link指向shade side 挂载时写的是safe link 看起来完全合法 但如果先执行real pass解析 就会得到真实的宿主路径 Home user saush deer sa 然后接下来的模式匹配就能检测到它 符号链接检查在模式匹配之前执行 这意味着路径穿越攻击在第一步就被斩断了 第三重检查是硬编码阻断模式 任何尝试挂载 包含sash gnab oss Env credentials Dot Envy Credentials Dot pm key 这些模式的路径都会被直接拒绝 而且这个阻断列表无法被用户配置覆盖 还有一个小细节 非主权的额外挂载强制只读主权 可以有读写权限 但其他权的挂载只能是只读的 这是防止跨群攻击的机制 这是nano cloud最独特的安全设计 我管它叫零信任凭证注入 问题在哪里呢 agent需要调用cloud API 需要API key 传统方案是环境变量 风险是什么 容器内的agent可以执行echo anthropic pig 读出密钥 然后通过任何网络请求发送到外部 这是完全合法的bash命令 任何代码层的安全检查都不会拦截它 nt cloud解决方案非常优雅 one CLI代理运行在宿主机上 容器内的cloud SDK配置的API n point 指向one CLI 而不是直接指向ANTHROPICWCAI 在宿主机层面注入真实API key 容器内的agent永远看不到密钥长什么样 更绝的是 这个设计容器内的DAVIN键被devon now覆盖挂载 本来应该存放环境变量的文件 在容器里实际上指向davon now 任何读取操作cat也好 echo也好 都只能得到空值 one CLI还提供额外的安全层 每个agent的独立速率限制 高权限操作审批流 完整的API调用审计日志 这就是零信任原则在agent安全领域的具体实现 默认不信任容器内的任何代码能安全处理密钥 在架构层面消除密钥泄露的可能性 现在我们来比较一下不同项目的安全设计 复杂度 HERMES只有requires approval不这么简单的机制 而goose需要五层检查链 这是工程能力差异吗 不是的 这是场景差异 HERMES的目标用户是技术型开发者 在自己的机器上运行 完全信任自己的agent 执行自己的命令 他的requires approval只对少数高危工具shell执行 开启其余操作自动批准 ghost的目标用户更广泛 包括不熟悉命令行的日常用户 需要智能区分安全的读操作和危险的写操作 需要防御提示词注入攻击 需要用户随时能切换审批模式 super powers更极端 他压根没有技术安全机制 纯粹依赖系统提示词塑造AI行为 这合理吗 因为它的定位是个人效率工具 面向的是自己使用 自己是唯一威胁来源的场景 行为塑造能减少误操作 但对抗不了对抗性攻击 这里有个关键原则 安全机制的复杂度必须匹配威胁模型的复杂度 过度安全和安全不足一样 使问题只是代价不同 一个损失用户体验一个损失 安全 好我们来做一次横向对比 看各个项目在安全维度上的覆盖情况 从这张表格可以清楚看到 goose专注于主动检测模式 匹配出网检测 LLM审查循环检测一应俱全 但它没有容器隔离 没有跨用户隔离 NANOCHLO专注于结构性隔离 容器凭证代理IPC授权 但它的安全完全依赖架构 没有代码层的命令检查 HERMES和super powers走轻量化路线 适合低威胁场景 关键洞察 没有任何项目覆盖了所有维度 也没有必要 每个项目的安全设计 都是在他的目标威胁模型下达到足够的防护 而不是追求最高分 这两个维度主动检测和被动隔离其实是互补的 理想的生产及agent的系统应该同时具备两者 但这需要相应的工程投入 所以现实中往往需要做取舍做 现在我们来提炼这一章的核心洞察 代码层安全 也就是goose的路线是通过检查逻辑来防御攻击 他的优点是可以细粒度控制 允许某些操作拒绝 另一些缺点是依赖代码逻辑的正确性 如果检查逻辑有bug或者攻击者找到了绕过方案 整个防线就失效了 架构层安全 也就是nano cloud路线是通过系统结构来消除攻击面 它的优点是不依赖代码正确性 依赖的是操作系统内核的保证 极难被绕过 缺点是力度比较粗 一旦进了容器 在容器权限范围内做什么都是自由的 两种路线的盲点也很清楚 ghost的agent1旦绕过检查链 就能访问整个宿主机文件系统 neo cloud容器内网络访问不受限 已经挂载进来的数据可以被外泄 最佳实践是什么 架构层安全是基础 先确保爆炸半径由界代码层安全是补充 在此基础上细化控制 nano cloud容器隔离 加上goose的repetition inspector和permission inspector 比单独用任何一种都更安全 但天下没有免费的午餐 这需要功臣代价 所以关键问题是 你的威胁模型是什么 你愿意为安全付出多少用户体验的代价 好现在我们进入实战环节 如何根据具体场景选择合适的安全方案 个人本机唯一用户直接选goose mode auto 没有多用户威胁 每次执行git status都弹确认框 只会让人恼火 不提供任何安全价值 有价值代码库防误操作为主选故事的前两层 security inspector加aggress inspector模式匹配 防误删 出网检测 防数据外泄 两层覆盖主要风险就够了 执行不信任代码 比如用户提交的代码 第三方脚本选nano cloud的容器隔离 这是唯一能防止系统调用及攻击的方案 代码层检查防不住这种攻击 CICD自动化脚本选goose 五层完整链脚本内容不可预知 可能包含混淆攻击 恶意依赖 需要全部防线 企业合规审计 选goose mode approve 加JASONL日志 每个操作都要有确认记录 可追溯 可审计多用户sas agent暴露给终端用户 选nano cloud i pc授权加WCLI结构性隔离 防止跨用户攻击 密钥永不在用户可访问的容器内 我把刚才的决策逻辑整理成一个流程图 方便大家在实际工作中使用 首先问你的代码或系统 是否会暴露给不受信任的第三方 如果是直接上nano cloud的容器隔离模式 这是物理及防护 如果不是 继续问 是单用户本机使用还是多用户服务端部署 单用户本机用goose mode auto 加上事后的JASONL审计日志就够了 继续问是否有企业合规要求 Sock 2i s o27001 这些标准要求关键操作有人工审批记录 那就用goose mode approve加JSONL日志 继续问你的威胁来源是误操作还是主动攻击 误操作为主 比如内部用户不小心删错东西 前两层检查链就够了 主动攻击为主 比如外部输入可能包含恶意代码 需要五层完整链 最后问 是否在CICD或自动化场景下执行任意脚本 如果是脚本内容不可预知 必须上五层完整链 如果不是前两层加smart proof已经够用 按照这个流程走 你的安全选型不会出大问题 最后为了方便大家深入研究 我整理了各个项目 涉及安全相关的关键文件索引 goose项目的核心文件都在creates goose s r c目录下 tool inspection rs是五层检查链的总入口 security patterns RSS定义了八类威胁模式 security adversary inspector rs s实现了LLM审查机制 Permission inspector rs处理goose mode权限路由 nano cloud安全关键文件 在SRC目录下 mount security t s实现了mount allawless的五重验证逻辑 container run adult t s负责容器启动和挂载策略 HERMES的安全设计极其简单 主要在tools environments base pie里定义requires approval标志 如果大家想深入研究某个具体机制的实现 可以从这些文件入手调好 我们来总结一下这一章的核心观点 第一AI agent的安全不是最后再加的功能 当agent可以执行真实系统命令时 它本质上就是一个拥有用户权限的程序 安全问题 必须在设计阶段就考虑 而不是等项目快完成了再打补丁 第二主动防御和被动隔离 针对的是不同的攻击向量 goose防的是恶意内容注入agent 难道close防的是A证 做了坏事能波及多远 两者不是非此即彼的关系 而是互补的 第三安全复杂度必须匹配威胁模型复杂度 个人开发工具和企业SARS面对的威胁完全不同 照搬一个方案 过去要么过度工程 要么防护不足 第四架构层安全是基础 代码层安全是补充 nano cloud的容器隔离 确保了及时检查链被绕过 爆炸半径也是有限的 在这个基础上 再用goose的检查列来细化控制 这张图 展示了各个方案 在用户体验和安全性之间的平衡位置 没有绝对正确的选择 每个项目根据自己的目标 用户和威胁模型选择不同的平衡点 关键是理解自己在哪里 为什么选那里 以及代价是什么 这一章就到这里 下一章我们将讨论什么 敬请期待

## 标签
[]
