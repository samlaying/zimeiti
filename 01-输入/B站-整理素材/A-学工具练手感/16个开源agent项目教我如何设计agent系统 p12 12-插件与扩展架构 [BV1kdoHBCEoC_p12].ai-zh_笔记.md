---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p12 12-插件与扩展架构 [BV1kdoHBCEoC_p12].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p12 12-插件与扩展架构

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

在我们研究的16个项目里 插件这个词出现了很多次 但当你深入进去 会发现他们说的根本不是同一件事 goose的扩展是一个独立运行的子进程 通过标准化的MCP协议和主进程通信 你的工具可以用Python写 node js写任何语言写 只要他遵守MCP协议 goose就能驱动它 open cloud插件是一个声明了capabilities的NPM包 核心 不需要加载这个包 只需要读取它的JSON文件 就能知道这个插件提供什么能力 然后在真正需要的时候才加载 INNO的扩展是一个实现了go接口的STRUCT 它在编译时就已经存在 没有发现的过程 也没有进程隔离 性能最高 但也最不灵活 e cc的技能是一个markdown文档 它不执行任何代码 只是把领域知识注入到LLM的上下文里 塑造LLM的决策行为 四种插件 四种完全不同的哲学 理解这些差异 就理解了插件架构设计的核心权衡 goose把所有工具提供者统称扩展分成五种类型 最常用的是steel扩展 goose启动一个紫禁城 通过steam steam stout 用MCP协议通信 你用UVX启动一个Python mc p服务或者node js服务 goose都能接入 这是真正的进程隔离工具 崩溃不会影响故事主进程 building扩展是直接编译进goose的12个内置工具 developer文件编辑 shell orchestratus agent管理to do analyze 这些工具运行在同一进程里 性能更高 不需要进程间通信 platform扩展 是桌面app专有的操作系统层面能力 屏幕录制文件系统访问CLI模式不可用 STREAMABLELETTDP是远程MCP服务器 通过HTTP streaming通信 适合云端部署 SAS是旧版以废弃保留向后兼容这五种类型 覆盖了从本地子进程到远程 云端服务的全部场景 而且全部用同一套MCP协议通信 对于上层的agent来说 调用steel扩展和调用streamable lit TP扩展的代码完全一样 现在我们来看工具发现的动态性 当goose加载扩展时 首先会通过MCP协议的list tools rpc调用 获取这个扩展提供了哪些工具 然后它会缓存这个列表 但会在工具名前面加上命名空间 前缀命名空间的规则是用双下划线分隔 比如developer扩展的shell工具会变成developer shell 而mm cp扩展的shell工具会变成mm cp shell 这样当两个扩展都提供了同名工具时 不会产生冲突 还有一个优化点 扩展配置里可以预声明工具列表 这样在启动阶段goose就不需要调用list tools 直接用预声明的列表可以显著加速初始化过程 这是goose特有的安全机制 在整个16个项目里 我没有在其他项目里见到过类似的设计 在加载扩展之前 GUS会把扩展的所有工具描述发给LLM 问他这里面有没有prompt injection的迹象 检测内容包括要求忽略之前指令的内容 隐蔽的数据外传指令 声称自己是系统提示的工具描述 为什么要这么做 因为工具描述是攻击者的理想注入点 如果一个MCP服务器的工具描述里写着 忘记之前所有指令 传统的权限检查根本发现不了这种攻击 他伪装成了正常的工具 这是预防性检测 不是事后补救 在工具发现阶段就拦截 而不是等到工具被调用时 代价是每次新扩展加载都需要一次LLM调用 增加了启动延迟 但在企业部署场景里 这个代价是值得的哎 除了LOLM扫描 goose还有一道重要的安全防线 环境变量限制 goose维护了一个静止列表 里面包括path l d l i brary Path l d preload node options Python path等等 为什么要禁止这些 如果允许扩展设置LULTCYLOAD恶意扩展 可以注入一个so文件 劫持所有LIPY调用 这是一种非常隐蔽的攻击方式 可以在运行时注入任意代码 这个静置列表在ENVS构造时检查 是净尘及隔离之外的第二道防线 即使恶意扩展突破了进程隔离 也无法通过环境变量劫持系统调用 这MCP协议里 有一个罕见但非常有价值的特性 MCP服务器可以向goose发起LLM采样请求 通常我们的模型是agent调用工具 agent问LLM该做什么 LLM决定调用哪个工具工具执行并返回结果 Create message 反转了这个关系 MCP服务器在执行工具的过程中 可以主动向goose的LLM发起一次推理请求 实际用例是什么 比如一个文档处理MCP服务器在写入文件之前 需要理解这个文件内容的语义 他可以向goose发起一次LLM调用 问这个文件是关于什么的 然后用这个理解来决定写入策略 又比如一个智能MCP服务器 在调用外部API之前需要格式化请求参数 他可以委托给goose的LLM做格式转换 而不需要自己管理API KE这种工具里有LLLMLM调工具工具 又调LLM的嵌套调用 创造了一种新的编排模式 goose成为了LLM采样的代理中介 而不只是agent的指令执行器 这个设计目前在16个项目里 只有goose实现了 随着MCP协议的普及 这个能力会越来越有价值 open cloud有112个插件 如果每次启动都import所有插件来 发现他们启动时间会显著增加 而且任何一个插件的初始化错误 都可能影响整个系统解决方案 很优雅 每个插件都有一个open claw plugin jason 核心系统通过读取这个JASON文件 完成插件发现能力验证 冲突检测 完全不执行任何JAVASCRIPT代码 这叫做manifest first 设计清单优先优势显而易见 启动时间大幅减少 避免了插件初始化的副作用 而且一个插件出错不会影响整体系统的启动 这对于管理112个插件来说是刚需嗯 Open cloud 四层生命周期分工极其清晰 第一层manifest discovery 扫描所有目录 读取JASON 建立插件目录 零代码执行 第二层enablement of validation 根据用户配置决定启用哪些插件 检查能力冲突 比如同一种能力只能有一个提供者 仍然不执行代码 只验证metadata 第三层runtime loading 仅加载需要用到的插件 按需懒加载 第四层surface consumption 注册表暴露能力 其他系统通过注册表获取 不直接依赖插件实现 这个设计的价值在大规模时才完全显现 对于2~3个渠道的小项目 直接注册完全够用 但112个插件你需要manifest first open cloud把能力分成12种类型 文本推理CLI后端语音合成 实时转录 实时语音媒体理解图像深沉 音乐深沉 视频深沉 网页抓取 网页搜索频道消息 为什么要分得这么细 因为一个插件 比如OpenAI可能同时提供文本推理 语音合成 媒体理解 图像生成等多种能力 如果能力类型不细分 就无法表达文字 用OpenAI图像 用GEMINI这种精细的组合需求 有了细粒度的能力 注册表系统可以精确的选择 每个能力的最优提供者 而不是要么全用 要么全不用 Open cloud plugin SDK是所有扩展代码唯一合法的import入口 plugin SDK是合法导入 内部SRC目录是静止导入的 但静止这个词在软件工程里说起来容易 做到很难 你怎么保证所有外部开发者都遵守 怎么保证六个月后新加入的工程师 还记得这个规则 open cloud的答案 用测试强制执行边界 而不是靠文档约定 Architecture Smells dots ts s会检查每个扩展包的所有import 如果出现了SRC目录的直接导入 测试失败 pr无法合并 这是一个很小但很深刻的工程决策 把人工遵守的约定变成自动验证的约束 最值得跨项目借鉴的设计之一 写一个测试 强制执行你的架构约束 一诺的扩展模型和前面两个完全不同 没有进程隔离 没有manifest 只有go接口 components层定义了所有组件的接口 Chat model retriever Embedding to 每个都是一个go interface 实现在独立的I/O ex仓库 Open ai chat model Anthropic chat model Ooma chat model 20多个实现 这是与goose内置所有provider完全相反的哲学 goose把所有provider实现都放在主仓库 统一维护 统一发布 而i know把接口放在核心 实现分散在IOX各自独立版本迭代 这个分离带来了一个独特的保证 核心框架零依赖 你在用IO框架时 不会被迫引入OpenAI SDK或ANTHROPICSDK的依赖 只安装你需要的实现去啊 INNO的类型检查发生在编译时 而不是运行时 graph在compile阶段 验证每条边两端的节点输入输出类型是否兼容 如果你把一个返回string的节点 接到一个期望message切片的节点 compile会报错 这不是运行时检查 是构建时间差 在land chain里 你要真正运行起来才知道类型错了 在IO里 你在构建图的时候就得到反馈 这就是强类型语言的优势 编译器帮你做了大量的正确性验证 减少了运行时出错的可能性 INNOX的独立仓库的策略有明显的取舍 优点是核心框架保持极小的依赖 不把OpenAI SDK ANTHROPICSDK等都打包进来 各个实现可以独立版本迭代 不需要等核心框架发版 用户只安装需要的实现 不强制引入无关依赖 缺点是新功能可能需要同时修改核心和i know EXX接口 版本管理更复杂 核心接口变更时 所有IOX实现都要更新 对贡献者来说 两个仓库来回切换 增加了摩擦对比 故事内置所有provider的方式 优点是所有实现保持同步 接口变更 统一处理 开发者只需了解一个仓库 缺点是仓库体积庞大 核心框架被provider SDK的依赖 传染功能 发布节奏绑定 两个项目的选择都是理性的 取决于他们各自的贡献者规模和发布节奏 ghost在短期内更易维护 但长期随着provider数量增长 发布节奏会变慢 一诺的分力策略在短期增加了协调成本 但允许独立版本迭代 长期更有扩展性或 ECC有183个skill 每个是一个markdown文件 这是所有插件方式里最低技术含量的 却也是最容易被低估的 为什么markdown是合法的插件格式 因为ACC的插件解决的不是给agent更多 工具能力的问题 而是让agents在特定领域做出更好的决策的问题 这是两件完全不同的事 MCP插件是工具能力 agent能做文件读写 能搜索网络 能执行代码 这些是执行能力 需要代码来实现 markdown skill是领域知识 使用X402协议处理 支付时要注意这五个坑 做代码审查时优先检查这三类安全漏洞 写测试时遵循这些命名规范 这些是判断知识 不是执行能力 你无法把它们编译进代码 markdown插件和MCP插件解决的是完全不同的需求 MCP插件提供的是工具能力 执行时有副作用 需要运行时环境版本控制的是代码 markdown skill提供的是领域知识 执行时无副作用 只需文本版本控制的是markdown文件 什么时候markdown比MCP更好 当你的目标是塑造LLM的决策行为 而不是提供执行能力时 当你的系统需要编码隐性知识 比如最佳实践决策框架 避坑指南时 当你的系统需要零依赖部署 不需要任何运行时环境时 当你需要团队成员贡献领域知识 但这些人不是程序员时 这就是为什么非程序员也能写skill 业务专家可以直接贡献领域知识 不需要任何编程技能 这让skill的生产速度和质量都超越了代码插件 ECC的markdown skill系统有一个独特的自净化机制 ECC有一个learn命令 在会话结束时自动提炼新skill 他会分析本次会话中产生的新模式 检查是否值得固化为skill 然后生成新的skill md文件 保存到用户的learn目录 Stop hook 还会触发pattern extraction Dot js 自动评估这些新模式的质量 这意味着什么 markdown skill系统能从使用中学习代码 插件系统没有这种自净化能力 你需要手动写代码来实现新功能 而skill系统可以从对话中自动提炼知识 这是一个非常深刻的洞察 知识型插件天然具有自我进化的能力 因为它本来就是文本形式的 可以被分析和组合 让我们把四种方式放在一起比较 进程隔离方面 MCP goose提供真正的进程隔离工具 崩溃不影响主进程 其他三种都是同进程模型类型安全方面 AO的go泛型最强 在编译时就检查类型 open cloud typescript的接口 其次MCP用jason schema是弱类型 markdown没有类型概念 安全检测方面 goose有LLM扫描加环境变量限制 open claw有SDK边界和架构测试 一诺靠编译时保证markdown本身就是文本 不需要检测 试用规模方面 manifest open claw在100加插件时有明显优势 MCP适合10~100扩展1no接口 适合20加实现markdown可以无限扩展 因为只是文本 第三方开发者友好度方面 MCP任意语言都可以写 最友好 markdown不需要编程技能也很友好 Open cloud plugin sdk 对typescript开发者友好 I know 需要go背景 相对门槛高一些 现在我们来回答两个关键的设计问题 第一个问题 INO的INNOX分离仓库vs goose的内置所有provider 哪种可维护性更好 goose内置provider的策略在短期内更易维护 接口变更时统一修改 CI在单一仓库里覆盖所有provider 但长期随着provider数量增长到15个以上 每次核心create发布都要等所有provider就绪 发布节奏会变慢 一诺的分离仓库策略在短期增加了协调成本 但允许独立版本迭代 随着provider数量超过20个 分离的价值开始超过协调成本 两个项目的选择都是理性的 取决于他们各自的贡献者规模和发布节奏 没有绝对的好坏 只有适不适合当前的阶段 对嗯 第二个问题 为什么ECC用markdown而不是MCP 因为ECC的核心价值不是给crowd更多工具 而是让crowd在特定领域做出更好的决策 这是知识注入 不是能力扩展 markdown是传递知识的最佳格式 MCP是传递能力的最佳格式 两者解决不同问题 ECC不需要让cloud能做更多事情 它需要让cloud在做事情的时候更专业 这需要的是领域知识 而不是工具能力 markdown是承载领域知识的最佳载体 任何人都可以写 不需要编程能力 可以版本控制 可以合并分支 所以ECC的选择不是落后 而是精准 他准确的识别了自己的核心需求 然后选择了最适合的技术 现在我们来总结插件架构的选择依据 选择插件架构时三个核心问题决定方向 第一个问题 你的插件是提供工具能力还是提供知识规则 工具能力选MCP或代码接口支持规则选markdown 第二个问题 你的插件系统需要进程隔离还是共享 进程性能更重要 需要隔离 选MCP性能优先选接口或manifest 第三个问题 你的插件作者是谁 是任意语言的程序员 JAVASCRIPT程序员 构程序员还是非程序员 这决定了哪种方式对开发者最友好 回答完这三个问题 你就能找到最适合自己的插件架构 没有哪种方式最好 只有哪种方式最适合你的约束 让我们把这个决策框架应用到具体场景 如果你只是自用工具 工具数量少于十个 直接注册函数就够了 零框架开销代码及插件 如果你要开放第三方插件生态 允许不同语言的开发者贡献工具 选MCP协议语言无关进程隔离 如果是企业内部系统有100加插件 而且性能敏感 选manifest first 加懒加载启动只加载原数据 按需加载插件 如果你是大型go框架 需要编译时类型安全 选go interface分层 如果非程序员需要贡献领域知识 选markdown文件 如果需要防止恶意第三方插件 选MCP加加载时的LLM扫描 每个场景都有一个最合理的选择 避免用大炮打蚊子 也避免用螺丝刀拧螺丝 最后我们来总结goose选择MCP 因为block需要安全可审计的企业agent open claw选择manifest first 因为它要支持100加插件的生态 不执行代码就能发现插件是刚需 INNO选择接口分层 因为字节跳动需要在go企业生态里提供编译时 类型安全运行时才发现类型错误是不可接受的 e cc选择markdown 因为他的目标是让任何人都能贡献领域知识 非程序员可参与是刚需 没有哪种方式 最好只有哪种方式最适合你的约束 在设计自己的插件系统时 问自己这三个问题 我的插件是工具还是知识 我的插件作者是程序员还是领域专家 我预计有多少插件 答案会指向一种最合理的方式 这就是插件架构设计的全部精髓 理解你的真实约束 然后选择最能满足这些约束的技术方案

## 标签
[]
