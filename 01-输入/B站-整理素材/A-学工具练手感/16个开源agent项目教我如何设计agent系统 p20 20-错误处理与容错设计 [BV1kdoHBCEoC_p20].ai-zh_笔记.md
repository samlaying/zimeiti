---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p20 20-错误处理与容错设计 [BV1kdoHBCEoC_p20].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p20 20-错误处理与容错设计

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

今天我们聊一个听起来很平凡 但实际上极其重要的话题 AI agent出错了怎么办 这不是边缘情况 任何在生产环境里运行过A证的人都知道 API限流是几乎每天都会遇到的事 上下文艺出在涨任务里 必然出现工具调用 有时就是会失败 agent有时会反复做同一件事而没有进展 这些都是日常运营的现实 我们今天要看的五个项目 给出了截然不同的应对方式 有的精细到把API错误分成12种类型 每种类型用不同的恢复策略 有的靠一个硬上限 1000轮来兜底 有的在框架层面提供了暂停 然后从原点继续的机制 有的把目标设定为不犯错 而不是犯错后补救 这些差异不是随机的 它们反映了各自设计团队 对错误处理的不同哲学 理解这些差异比理解任何单一技术实现 更有价值嗯 hermes agent的error classified Do pi 是这五个项目里 对AAPI错误处理最系统化的实现 它定义了12种 Fail of over reon 每种类型对应不同的恢复策略 最关键的几个分类是rate limit 状态码指数退避重试 这个错误是临时的 等一会就好 同时轮换API凭证 如果有备用key 换一个可以跳过等待context overflow 上下文溢出 压缩上下文后重试 不切换模型 这是最容易做错的地方 很多系统看到错误就切换for back模型 但上下文溢出 换个模型没用 因为新模型的上下文限制通常更小或一样大 正确做法是压缩缩小上下文后再试off 401和403不重视 用相同的key重试会继续失败 浪费时间 应该立刻轮换凭证或切换fall back模型 BING402不重视轮换provider 但有个边界案例 有些provider把本小时quota用完5分钟后重置 也编码为402 Error classifier 会检查消息里有没有try again之类的信号 有就视为临时限流 没有才视为真正的计费问题 每个分类都附带三个标志位 Retriable should compress Should fall back 调用方不需要重新分析错误 读这三个标志 就知道下一步怎么做做 为什么这个分类管道要分七层 而且这个顺序很重要 优先级从高到低 第一层是provider特定模式 因为同一个429状态码 ANTHROPIC的long context TARGATE和普通限流应该区别处理 前者需要压缩上下文 后者只需等待 所以provider特定模式 必须先于通用HTTP状态码判断 第二层才是HTTP状态码加消息感知精化 第三层是结构化错误码解析 error点code或errodot type字段 第四层是消息模式匹配 从错误消息文本里区分billing rate limit context和off 第五层很有意思 服务器断开加上大会话 判断为上下文溢出 这不是简单看状态码 而是结合绘画大小做出的推断 第六层是传输层 错误类型名 第七层是兜底未知错误 默认可重视 这个顺序背后的逻辑是更精确的判断 优先于更模糊的判断 这样能确保每个错误都被最合适的策略处理 嗯这是个值得深想的设计问题 为什么不简单分成能重视和不能重试 两类答案是相同的 能重视正确的动作是完全不同的 rate limit可以重试 但需要退避等待 context overflow也可以重试 但需要先压缩上下文 如果不压缩直接重试 会无限循环 time out可以重试 但可能需要重建连接 overloaded503状态码可以重试 但不需要切换凭证 只需等待 如果把这些都合并成可重视上层逻辑 必须自己区分这些情况 把分类逻辑散落在各处集中的error classifier 把这个知识封装在一个地方 上层只需要读标志位 代价是什么 大约500行代码维护了大量字符串模式列表 当新的provider出现新的错误格式时 需要更新这些列表 这是显著的复杂度成本 这个设计在单一provider的场景下 可能是过度设计 但hermes agent的目标是支持200加轴模型 和多provider 不同provider的错误消息格式完全不同 统一的分类层 让上层逻辑不需要为每个provider写特殊处理 规模 决定了这个复杂度是值得的 goose没有一个统一的error classifier 而是在agent的主循环里 叠加了多个独立的容错机制 每层解决不同维度的问题 第一层是repetition inspector 他追踪agent对工具的连续调用 当相同工具以相同参数被连续调用 超过阈值次数时拒绝执行 要求用户介入 这针对的是一种典型死循环 agent反复调用同一个工具 每次都成功执行 但结果没有推进任务 agent却不改变策略 这种循环不会触发API错误 普通的错误处理机制发现不了它 第二层是fix conversation 在每次LLM调用之前 把对话历史修复成合法状态 用户取消了工具执行网络中断 导致two response没写入 Provider 返回了空消息 这些都会留下脏状态 如果直接发给LLM会报错 第三层是max turn ace等于1000 不管发生什么 1000轮以后停止询问用户是否继续 这是最后的安全网 确保系统在任何情况下都不会无限运行 三层机制各司其职 覆盖了不同类型的失控情况 repetition inspector的检测逻辑其实很简单 就是比较当前调用和上一次调用的name和parameters 是否完全相同 如果相同 repeat count加一 如果不同 重置为一 超过阈值时拒绝执行 触发require approval流程 也就是要求用户介入这个机制 专门针对一种典型的agent死循环 反复尝试同一个工具调用 但每次都失败或得到相同结果 却没有改变策略 在实际场景中 这通常意味着agent卡在了某个局部问题上 需要人介入 这里用rust实现的好处是类型安全 没有空指针 inspect方法的参数和返回值都定义的很清楚 Fix conversation 这个函数值得单独讲一页 因为它代表了一种很重要的工程思维 防御性设计 在一个理想世界里 agent主循环产生的对话历时总是合法的 但现实中很多情况会破坏这个不变量 用户在工具执行中途按了取消这个工具请求 没有对应的响应 LLM协议要求 每个to request必须有对应的to response缺失 会导致API报错 agent产生了两条连续的assistant消息 某些provider在流逝场景下会这样 ANTHROPIC的API不接受连续的assistant的消息 to执行成功 但返回了空内容 ANTHROPICAPI拒绝接受空的to result fix按顺序处理这八种情况 确保不管对话历史处于什么状态 在调用LLM之前都会被修复成合法状态 这个设计的核心哲学是不 假设系统总是处于正确状态 在关键路径上主动验证和修复 就这里有一个设计巧思 我要单独提一下 fix conversation函数不仅修复状态 还返回issues列表 记录我做了什么修复 这既是日志 方便调试时知道系统经历了什么 也是测试工具 测试用例可以精确断言哪些修复被触发了 比如测试可以检查是否输出了 removed orphaned to request search1这条记录 而不只是检查结果消息数量 这种设计让防御性修复变得可观测可测试 这是很值得学习的工程实践 另外非可见消息通过shadow map机制 在修复过程中保持相对位置不变 不参与agent visible层的合并和删除逻辑 这确保了修复不会破坏内部状态 1000轮是一个非常高的阈值 正常任务几乎不会达到 但它的存在 确保了系统不会在任何情况下无限循环 这是最后的安全网 不是主要的循环控制机制 可以通过环境变量goose max turns或session config max turns覆盖默认值 还有一个max compassion attempts等于out的限制 压缩本身是对上下文溢出的响应 但如果压缩后摘要加最近消息仍然超出上下文 又会再次触发压缩 如果不加限制 理论上可以无限压缩循环 硬上线两次的逻辑是 如果两次压缩还不够 说明任务本身需要重新设计 切分为更小的子任务 而不是继续压缩 多次压缩会丢失越来越多的上下文 导致agent忘记重要背景 继续执行已无实际意义 一诺的容错思路和其他项目都不一样 他不只是出错了 怎么处理 还提供了从中断点精确恢复的框架言语 interrupt resume机制的典型场景是 agent在执行高风险操作时发邮件 修改数据库 执行代码需要暂停等待人工审核 审核通过后继续没有框架支持时 开发者通常的做法是 把需要审核的操作写入数据库 另起轮询任务 等待审核结果 审核完成后重新启动agent 把审核结果作为新的上下文传入 问题在于agent丢失了执行上下文 可能重复执行已完成的步骤 INNO的做法是 任何节点可以发出interrupt信号 执行引擎序列化完整的执行状态 也就是checkpoint保存到checkpoint store 然后通知用户 用户完成审核后 通过runner resume with params传入审核结果 从中断点精确继续 之前已完成的节点不会重新执行 这样这里我展示一下checkpoint的数据结构 让大家看看框架及恢复需要保存哪些状态 channels是每个channel的当前状态 inputs是每个节点收到的输入 用于rrun state是图的共享状态 skip prehandler记录哪些节点跳过PREHANDLER ren notes是恢复时需要重新执行的节点列表 Interrupt id r adder和interrupt id 2state是中断id到执行地址和组件状态的映射 支持精确恢复每个中断点 关键设计细节 runner在发送中断事件给用户之前 先保存checkpoint 这确保了不存在用户看到中断通知 但checkpoint还没保存的静态窗口 Composite interrupt 支持多个并行工具调用各自产生中断 恢复时为每个中断点提供独立的审核结果 这个设计比大多数框架都要精细 对比一下 一诺的恢复是从中断点继续执行 不是从头再来 ghost的richard manager在recipe重视时会重置整个消息历史 两者面向不同的场景 前者适合需要保持执行状态的长流程 后者适合需要干净状态重新执行的任务 综合五个项目死循环检测有三种方式 各有不同的覆盖范围和可靠性保证范式 一是调用模式检测goose的repetition inspector 比较相邻工具调用的name加parameters 连续相同 超过阈值就拒绝 优势是精确 能告诉你具体卡在哪里 局限是指检测完全相同的重复语义相似 但参数略有变化的循环检测不到泛式 二是硬技术上限 goose的max turns等于1000 i know的max compassion attempts等于二 这是最可靠的机制 无论发生什么 超过阈值就停止 代价是粗力度 触发时 说明所有其他机制都没有阻止循环范式 三是行为策略约束 Oh my open agent 在HEAVISTERS的提示词里写验证失败 回到第一步 最多三次迭代三次后咨询oracle superpower is a systematic debugging鞋 三次修复失败 停止质疑架构 这不是代码逻辑 是约束agent决策策略的提示词 优势是能处理语义层面的循环局限 是依赖LM遵守规则 可靠性低于代码层面 三种范式是互补的 成熟的设计应该三层都有 注意三这个数字在两个项目里独立出现 这不是巧合 而是一条功臣经验 如果在同一个方向上连续失败三次 通常不是执行力度不够 而是方向错了 需要上升一个抽象层面 这条规则在oh my open agent的half fastus 深度执行agent里出现 三次迭代失败后不再在同层面继续 而是上身咨询oracle或询问用户 也在superpowers的systematic debugging skill里出现 三次修复失败 停止质疑 架构独立出现 说明这是工程规律的收敛 不是特定设计选择 背后的逻辑是如果正确理解了根音 通常1~2次修复就能解决问题 失败三次说明要么根音判断错了 要么问题在更深的架构层面 继续在同一层面尝试 是在做无效功 Superpowers The systematic debugging skill 代表了完全不同的思路 不是出错后怎么处理 而是怎么不出错 核心铁律 No fixes without route Cause investigation First 先找根音再修复 随机尝试是浪费时间 四阶段流程阶段一根音调查 先仔细阅读错误信息 错误信息通常包含解决方案 然后稳定复现 能稳定复现才能验证修复 最后检查近期变更 get diff head到五 看看是什么改动引入的问题 阶段二模式分析 找到代码库里类似的已工作实现 然后找出你的代码和参考实现的差异 阶段三假设与测试单一假设每次只改一个变量 这避免了一个改动 修复了问题 但不知道是哪个改动的尴尬阶段 四实现修复加上防御性加固 四层defense in depth 这和hermes goose inno的错误处理是互补关系 不是竞争关系 super powers的四层防御模型要求修复bug的同时 在相关层面补充验证和日志 岑衣室入口验证 用户或外部数据进入系统时进行校验层 二是业务逻辑验证 在核心逻辑中验证业务规则是否符合预期层 三是环境验证检查文件是否存在 数据库连接是否正常 服务是否可达 辰巳是调试日志记录足够的信息 以便未来调试 这解决了修复了bug 但没有防止类似bug的问题 单点修复和系统性加固是两种不同的质量目标 单点修复只是让当前问题消失 系统性加固是让整个类目的问题都难以出现 技术恢复处理的是已经发生的外部错误 API限流对话 脏状态上下文一出 这些不在agent的控制范围内 只能被动响应 行为预防处理的是可以避免的逻辑错误 随机修复 而不找根音 跳过验证 在错误方向上重复尝试 这些是A卷自己的行为选择 可以通过约束策略避免 如果只有技术恢复 没有行为预防系统很健壮 但agent会不断制造可以避免的错误 靠恢复机制托底 效率低 错误率高 如果只有行为预防 没有技术恢复 遇到真正的外部错误时 系统会崩溃 因为没有优雅处理 API限流 网络中断的机制 成熟的agent系统需要两者 superpowers告诉agent怎么做 对hermes goose ino保证做错了也能撑住 这张表格总结了五个项目 在各个维度上的设计选择 没有一个项目在所有维度上都最优 每个项目解决了自己最关心的问题 HERMES最关心多provider场景下的精确错误分类 因为他要支持200加种模型 goose最关心对话状态一致性 也就是fix conversation 以及防止执行失控 也就是max turns 因为它面向桌面用户的产品不能崩溃 引导最关心长流程中的状态恢复 因为它是企业场景框架任务 可能需要跨越多个会话 继续 super powers最关心行为质量 因为他的目标是 让cloud code成为一个系统化思考的助手 最终结论错误处理不是一个单点设计 而是多层防御 每一层覆盖其他层覆盖不到的失败模式 理解每层在防御什么 为什么这一层是必要的 比记住具体实现更重要 接下来我们来看场景选型 每个项目都有自己的优先级 错误处理的复杂度应该与错误代价匹配 个人用工具出错代价低 简单重视足够生产服务 影响所有用户 需要精确分类和多层防御 防止agent陷入死循环 需要repetition inspector加max turns 避免没有死循环防御 导致agent可能永久循环消耗资源 对话历史损坏需要fix conversation检测并修复 不一致 避免直接截断消息历史 导致关键状态丢失 长流程中段需要checkpoint精确恢复 避免从零重启 让已有进度全部丢失 多提供商场景需要错误分类加优先级队列 避免单提供商硬依赖 导致提供商故障时 服务整体中断 防止agent犯可避免的错误 需要行为预防 避免纯技术恢复 让agent不断制造可以避免的错误 这里给出一个决策流程 帮助你在实际项目中做选择 第一步稳分类层需要区分不同类型的错误 并给出不同响应吗 如果是加上hermes error classifier或等价的分类机制 如果否简单重试三次上线就够了 第二步问检测层agent可能陷入死循环吗 如果是加上repetition inspector 追踪重复模式 加上max turns硬上限 如果否至少加一个max turns兜底 第三步问恢复层需要恢复执行状态吗 长流程不能接受从零重跑吗 如果是加上i know checkpoint或等价的状态持久化 如果否从零重跑即可 第四步问预防层 agent有可能犯能力范围内的错误吗 比如随机修复 不找根音 跳过验证这些行为问题 如果是加上systematic debugging行为约束 如果否纯技术恢复已经足够 第五步问对话修复层有对话历史损坏的风险吗 长对话加压缩加工具调用可能产生脏状态吗 如果是加上fixed conversation 检测不一致和修复逻辑 如果否不需要专门的修复机制嗯 最后总结三个跨项目借鉴建议 第一三次规则是可以立刻引入的通用原则 这条规则在all my open agent和super powers中独立验证 说明这是工程规律 而非特定设计 选择任何agent的系统 都应该把这条规则加入错误处理逻辑检测 连续三次相同失败自动上升 通知用户换策略 暂停执行 第二技术恢复和行为预防是互补的 不是替代关系 hermes goose1诺的技术恢复处理 已经发生的外部错误 super powers的行为预防处理可以避免的内部错误 完整的agent系统 两者都需要技术恢复 保证系统韧性 行为预防 提高执行质量 第三错误处理的复杂度应该与错误代价匹配 个人工具出错代价低 简单重视足够生产服务出错影响所有用户 需要精确分类和多层防御 企业及工作流出错 可能有真实业务损失 需要checkpoint精确恢复加人工审查 不要为所有系统都设计企业级错误处理

## 标签
[]
