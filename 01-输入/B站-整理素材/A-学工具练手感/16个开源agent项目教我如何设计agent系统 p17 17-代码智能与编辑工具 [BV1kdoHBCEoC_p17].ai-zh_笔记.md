---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p17 17-代码智能与编辑工具 [BV1kdoHBCEoC_p17].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p17 17-代码智能与编辑工具

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

今天我们来聊一个问题 同样是AI编程助手 为什么有的用起来感觉聪明 有的用起来总在犯低级错误 表面上看 大家都是调用cloud或GPT都能读文件写代码 但在工具层面 这些项目走了完全不同的路 从最简单的把代码扔给LLM分析到tree sitter 语法解析到完整的LSP语言服务器集成 这不是渐进改进 是三种截然不同的设计哲学 我们今天要横向对比六个项目 Dear flow Goose Open code Oh my open agent cloud code 还有一个看起来无关的agent browser 它们分别代表了 理解代码和修改代码的不同路径 最后我们会发现 agent browser在浏览器领域做的那件事 和代码分析工具做的是同一件事 首先来看理解代码这件事 有三条截然不同的路径 第一条路是deer flow走的 直接把文件内容扔给LOLM 让他自己理解 好处是简单 坏处是贵 一个大文件可能就吃掉你几万tokens 第二条路是goose走的 用TRYSITTER解析代码的语法数 只提取函数签名类 定义这些关键信息 好比你看一本书 别人给你的是整本书 你只要的是目录和章节标题 第三条路是open code集成LSP语言服务器 LSP就是vs code用的那种协议 它能告诉你这个变量是什么类型 这个函数被谁调用 这是编译器级别的理解精度最高 代价也最大 但理解了代码 下一步就是编辑代码 这里也有三种方式 最常见的是字符串匹配 你告诉工具把这段old star换成new star 工具就在文件里找完全相同的字符串 Cloud code goose都是这么干的 简单是真简单 但有个致命问题 如果AI复现字符串时多了一个空格 或者在你读取之后 文件被格式化工具改过了 那就匹配失败 第二种是oh my open agent的hash锚定 给每行代码加个哈 希指纹定位的时候 不仅靠行号 还验证这行内容有没有被改过 匹配失败时 它不简单报错 而是告诉你正确的引用是什么 让AI自动重试 第三种是open code的l s p document edits 用语义符号定位编译器 知道代码结构定位最精准 修改后还能自动感知编译错误 这三条路稳定性一个比一个高 复杂度 也一个比一个大 好我们逐个深入看每个项目 先从最简单的dear flow说起 DEFLOW没有专门的代码分析工具 理解代码这件事完全委托给LLM 它的工具就两个 read file和street place 一个读一个改 工作流程极其简单 LLM读取原始代码 在上下文里自己理解 然后生成old star和new star是place负责执行替换 这个方案的优点是实现成本趋近于零 对于短文件五六百行以内 LLM的理解能力确实够用 不需要什么花活儿 但他有两个天花板必须得说清楚 第一个是上下文爆炸 一个一百千字节的Python文件大约是25K Tokens 很快就称满上下文了 你想啊 你的对话上下文才多少 光代码就吃掉一大半 魔仙还怎么思考 第二个是没有任何错误 恢复极值strip pla 找不到old as ta时 他什么也不做就失败了 比如格式化工具在你读取之后 改了一个尾随空格或者模型 复现代码时多了一个换行 那对不起 没有任何补救 所以dear flow的定位是研究和分析助手 不是代码修改工具 这些限制在他的场景里是可以接受的 但换成别的场景就不行了 接着来看goose 它的analyze扩展是目前开源agent里最完整的 TRITTER代码分析实现 来看看它的核心数据结构 File analysis 它包含language 告诉你这是什么语言 lock告诉你文件有多少行 然后是functions classes imports和co 这些就是AI理解代码最需要的信息 symbol结构体里每个函数或类都有name lime parent和detail 特别是detail 它保留的是函数签名 AI看到签名就知道这个函数要什么参数 返回什么类型 不需要看完整实现TRACCA的好处是 它解析的是语法术 通过S表达式query精确提取这些信息 不需要正则 不会遗漏 也不会误判 goods的analyze支持三种模式 非常灵活 你给一个目录路径 它就进入structure mode 用rayon并行解析所有文件 告诉你这个代码库里有什么文件 每个文件有多少函数和类 这适合概览场景 你给一个文件路径 它就进入semantic mode 解析这个文件的AST输出详细的函数列表 类定义导入和调用关系 这适合深入了解某个文件场景 你给一个文件路径加上focus参数 指定某个函数名 它就进入focused mode 构建完整的调用图 这个函数被谁调用 它又调用了谁 最多追踪N层 这适合我想理解这个函数的上下文场景 三种模式 三种力度按需使用 非常实用 效果究竟如何 来看实测数据 一个500行的Python文件 这是典型web应用的路由文件大小 直接读取是4000tokens 但用trace theter提取呢 十个函数签名大约200tokens 五个类定义50tokens 15个导入100tokens 调用图摘要 100tokens加起来才450 tokens压缩比将近十倍 一个2000行的中型模块文件直接读取 可能16000tokens接近上下文限制 但TRACITOR提取后只需要600token s 压缩比达到27倍 这不只是省钱的问题 对于大文件直接读取 可能根本塞不进上下文 你根本没法处理 而TRACCEITOR提取 让你能在有线上下文里处理无限大的代码库 不说完goose 我们来看cloud code 这个项目是这个领域的基准实现 它的工具集覆盖了编程所需的所有基础能力 Read write edit 这三个是文件操作的标配 bash执行shell命令 Globe 按模式匹配文件 grab做内容搜索agent可以启动此agent 重点说说edit的工具 他有个关键约束 old string必须恰好出现一次 如果出现多次工具直接拒绝执行 这是为了防止误改 如果AI复现的字符串和原文略有不同 比如多了一个空格 那匹配就失败了 这个约束很严格 但也很合理 他逼迫AI必须精确复现代码 减少了改错地方的风险 但也增加了AI生成的难度 现在来看open code 他做了其他项目都没做的一件事 在agent里集成了完整的LSP客户端 LSP是微软发明的协议 vs code就是用它和type script language server Rust analyzer pirate这些语言服务器通信的 Open code 把这套能力直接接给了AI 看看它的架构 select不到ts是主服务 每种语言有一个独立的LSP实例 client t s是JSONRPC客户端负责进程间通信 server teeth管理语言服务器的生命周期 Launch dot teeth 从系统path里自动发现语言服务器 language dot teeth定义了14种语言的映射关系 最贴心的是 它不需要你手动配置 只要你系统里装了type script language server或PYROID 它就能自动发现并使用 暴露给AI的工具是九种代码导航操作 Go to definition 就是跳转定义AI想知道某个变量在哪里定义的 一调就过去 Find references 查找所有引用位置 AI想改一个函数 但又怕影响其他地方 一查就知道 hover获取类型信息 就是你在vs code里鼠标悬停看到的那种提示 Document symbol 列出文件里所有符号 Workspace symbol 跨文件搜索 Go to implementation 跳转到接口 实现prepare call hierarchy 准备调用层级 然后incoming course 告诉你谁调用了这个函数 Outgoing cause 告诉你这个函数调用了谁 使用时需要提供文件路径 加行列号 精确定位你想查的地方 这些操作让AI拥有了编译器级别的代码 理解能力 不是靠猜 是直接问编译器 但最关键的不是这些主动查询 而是诊断反馈闭环 看看这个流程 AI修改了一个文件 LSP服务器检测到变化 立刻推送diagnostics事件 这个事件被诊断处理模块收到 写入bus session 监听bus 感知到新的变异错误 下一轮AI调用工具时检查诊断 发现错误就自动修复 这意味着open code的AI在修改代码后 会自动知道我是不是破坏了什么 其他所有agent都不具备这个能力 他们修改代码后是盲修 只能靠用户手动运行测试才能知道有没有出错 这是一个真正的反馈闭环 修改验证再修改 open code的AI不是蒙着眼睛干活 他有眼睛不 现在来看oh my open agent 这个项目的hash line工具 解决了一个被普遍忽视的问题 传统的old strew news star方案 有三个典型的失败场景 第一个场景文件在AI读取后 被格式化工具修改了 比如AI读到第42行是CONSTX等于一 但格式化工具在后面加了个尾随空格 变成了CONSTX等于一 然后AI发出的old star还是constant x等于一 匹配就失败了 第二个场景模型记忆失真 AI读到了复杂的函数题 准备修改其中一行 但生成old s它时稍有不同 多了一个换行或者少了一个空格 结果匹配失败 第三个场景相同内容出现多次 文件里有三处 return new AI意图修改第二处 但old as turn等于return neil 无法区分 改了第一处 这些问题很常见 但大多数项目都没有认真对待 hash line的解法是给每行附加稳定标识符 看这个格式 EI井号v k function Hello 11是行号 VK是二字符哈 希是分隔符 后面是实际内容哈 希是怎么算的 用xx hash32计算 然后取模256 得到二字节 再查一个256字符的字典 排除了容易混淆的字符 转换成二个可读字符 这样每行都有一个唯一的指纹 65536种可能的哈 希值碰撞概率只有0.001% 几乎不可能重复编辑时 AI写pose是一一井号VK工具 先验证第11行的内容哈 希是否确实是VK 只有验证通过才执行修改 如果哈希对不上 说明这行被改过了 AI需要用新的哈希重试第 但最妙的设计是错误恢复极致 当哈希不匹配时 工具不是简单报错 匹配失败 他会告诉你预期的哈希是ZQ 但第42行 现在的哈希是AB 你可能想用四二井号AB直接给答案 这意味着AI可以用正确的引用自动重试 不需要人工介入 这是old stir new stir方案完全做不到的事 这个设计哲学很重要 工具的错误响应方式 直接决定了agent的自动化程度 报错只告诉你失败了 给答案才能让agent自我修复 现在要说一个让人震惊的数据 Oh my open agent的red me里记录了一个反直觉的测试结果 同一个模型grow code fast一直换了编辑工具 从传统的字符串匹配换成hash锚定 成功率从6.7%跳到68.3% 模型没变 任务没变 prompt没变 唯一变的是工具实现 这是一个里程碑式的结论 很多A证的失败不是模型太弱 而是工具太脆 这个数据改变了我们对如何提升agent能力的认知 大家都以为要换更强的模型 但有时候换一个更可靠的工具 效果比换模型好得多 现在我们来看一个 看起来和代码完全无关的项目 Agent browser 他要解决的是让AI操作网页 最直观的方案是把完整HTML喂给AI 让他写CSS选择器点击元素 但问题来了 一个GITHUB仓库页面的完整HTML可能超过20K tokens里面大部分是class名SVG路径 事件处理器 AI根本不需要这些 看看这个按钮的HTML有多复杂 各种data属性300字的事件处理器SVG路径 但最终用户看到的就是一个notifications4的按钮 agent browser的方案是把这些转换成无障碍数 原始HTML超过200 tokens转换后只有七个token Button notifications4at e七 压缩比30比一 这和trc or的思路完全一样 把原始形式转换成AI真正需要的信息 但这不只是压缩 每个可交互元素 都被分配了一个at t e n的短DAI说 click at e3就完成了操作 不需要写CSS选择器 更重要的是双层查找机制 当页面动态变化 react重渲染路由 跳转at t e3背后的DOM节点id可能失效 系统由语义回退 先尝试快速路径 用back in node i e的直接定位失败了 就重新扫描无障碍数 找到roll等于button name等于star ends等于零的元素更新缓存 这和ommy open agent的哈希验证加回退建议 有相似的设计逻辑 主路径快速失败后 由羽翼兜底 稳定的引用是可靠执行的前提 at t e n就是一个专门为AI设计的 高稳定性地质系统 定现在把这三个放在一起看 你会发现一个惊人的事实 代码领域 goose用tree sitter解析源文件 提取函数 签名和类定义网页领域 Agent browser 用无障碍数解析HTML提取可交互元素 shell领域 Open code 用TRACITOR解析bash命令 判断是读操作还是写操作 三个完全不同领域的工程师 各自独立的得出了同一个结论 直接把原始形式喂给AI是错的 需要提取语义层 这背后有一个统一的原则 AI需要的不是完整信息 而是正确层次的抽象 完整的HTML包含AI不需要的所有信息 语义提取后的结构 包含AI需要的所有信息 精确性没有损失 token消耗降低了一个数量级 那么问题来了 goose应不应该加LSP收益是真实的类型 感知意味着AI知道这个变量是什么类型 修改时不会犯低级错误 诊断反馈闭环意味着 修改后自动知道有没有破坏编译 Find references 让AI知道我改这个函数会影响哪些调用方 改之前就能预判风险 但代价也是真实的 语言服务器需要额外安装冷启动 可能需要10~30秒 进程管理复杂 14种语言需要分别测试和配置 所以结论是 对于需要修改大型typescript或rust 代码库的场景值得集成 对于通用工具LSP的边际价值可能不够高 需要权衡好面对一个新的编程agent任务怎么选择 技术路径 先看文件大小 如果是小文件 500行以内直接read file 让LLM理解就行 零成本 如果是大文件 需要TRYSITTER提取结构 token效率是直接读取的10~30倍 再看代码理解需求 如果只是想知道文件里有什么函数和类 tree sitter足够 如果需要知道这个变量是什么类型或改 这里会影响哪些代码 那就需要LSP 最后看编辑可靠性要求 如果可以接受 偶尔失败重试old stool new star最简单 如果需要高可靠性 特别是弱模型或复杂编辑任务 hash锚定有压倒性优势 如果需要修改后 立即知道是否出错 那就只有lsp diagnostics能做到 来做一个横向对比 看看每个项目的能力边界 读取文件内容 四个项目都有没问题 提取函数列表 dear flow和cloud code靠LLM推断准确度 依赖模型能力 goose和open code是精确提取 靠tree sitter或LSP查找定义和引用 goose和open code有专用工具 效率更高 dear flow和cloud code用grab也能用 但慢一些类型信息调用图编译错误感知 只有open code有 其他三个都没有 这是一张能力图谱 可以清晰地看到每个项目的定位 为最后从这六个项目里提炼一个核心洞见 Oh my open agent的数据 揭示了一个在AI agent领域常被低估的规律 agent的失败往往是工具问题 不是模型问题 具体体现在三个维度 第一语义提取的质量 tray theter的精确度 VSLLM的推断 ghost analyze给出了明确的token效率 数据用正确的工具做结构提取 不只是审token 是让AI看到正确层次的信息 第二引用的稳定性 ARTIAN引用系统和hash锚定解决的是同一类问题 AI生成的引用在执行时是否还有效 CSS选择器会失效 行号会漂移 字符串匹配会因为一个空格失败 稳定的引用是可靠执行的前提 第三错误的可恢复性 old star找不到就失败 是一种设计 hash不匹配就告诉AI正确的hash是另一种设计 前者需要人工介入 后者AI可以自动重试 这不是小细节 是直接影响任务完成率的关键设计 来总结一下代码智能的演化路径 最简路径是dear flow的方案 LLM直接读原始代码 靠模型本身理解成本最低 但大文件有上限 第二步是goose的AST增强tree sitter 提取结构 大幅压缩token保留语法精确性 适合大型代码库的结构分析 第三步是open code的语义理解集成 LSP获得编译器级别的代码理解 代价是架构复杂度 收益 是真正的语义感知和修改后的错误反馈闭环 第四步是oh my open agent的编辑可靠性专项优化 hash锚定解决了字符串匹配的脆弱性问题 6.7%到68.3%的数据证明 工具可靠性比模型能力更关键 第五步是agent browser的同构思路 无障碍数是DOM领域的 tree sitter at e n引用系统是为AI设计的稳定地址 核心洞见一句话 AI编程助手的质量瓶颈 往往不在于模型有多聪明 而在于工具设计是否足够可靠 最后给一个场景选型决策框架 方便你在实际工作中参考小文件 简单脚本用deer flow的最简路径实现成本最低 不存在token上限问题 不要用大炮打蚊子 大型代码库需要结构分析 用goose的tree sitter方案 压缩token的同时保留关键语法信息 需要真正的语义理解类型信息 跨文件引用 编译错误感知 用open code的LSP集成 这是最接近IDE体验的方案 高重复代码环境测试文件样板代码 用oh my open agent的hash锚定成功率提升十倍 浏览器和DOM自动化 用agent browser的无障碍数语异化表示 加at t e n稳定引用 告别脆弱的CSS选择器 记住正确的工具选择 可以让弱模型达到强模型的效果 错误的工具选择再强的模型也发挥不出来

## 标签
[]
