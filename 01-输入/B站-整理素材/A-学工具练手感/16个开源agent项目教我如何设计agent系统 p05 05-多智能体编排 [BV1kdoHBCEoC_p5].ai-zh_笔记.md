---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p05 05-多智能体编排 [BV1kdoHBCEoC_p5].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p05 05-多智能体编排

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

好今天我们进入第五讲 多智能体编排 这可能是整个系列里最烧脑的一章 因为我们要同时横屏六个项目 而且每个项目的设计哲学都不一样 但别担心所有的差异都归结为一个核心问题 谁来决定并行度 我们看到六个项目分成了三种路线 代码决定就是编排逻辑写死在图结构里 LLM只是执行节点 LLM决定是框架提供工具 LLM自己判断哪些任务可以并发 规则决定是人类在skill里写下严苛条件 LLM指在所有条件满足时才能并行 这三种策略不是好坏之分 而是面向不同的可预测性需求 关键是你的场景里 你更信任代码还是更信任LLM 在深入各项目的实现之前 我们先明确多agent的系统通常涉及的三个层次 调度层 通信层 隔离层 调度层决定谁来发号施令 通信层决定agent之间怎么传递数据 隔离层决定一个agent的挂了会不会拖累其他agent 这三个层次的选择不同 整个系统的行为就完全不同 完好我们先看第一个项目 Dear flow 他用land graph的state graph来描述多智能体协作 Coordinator Supervisor researcher coder是图里的节点条件 编决定路由逻辑全部用Python代码写死看这个图 当supervisor决定需要并行搜索 它输出特定格式的JASON图结构 根据这个JASON触发多条边 同时激活多个researcher节点并发执行 这不是LLM运行时做的决策 LLM只是输出了符合预期格式的数据图结构 负责路由 人机交互节点通过esa clarification工具 实现agent的执行 到这个工具图暂停等用户输入 然后带相同thread id重新调用 从最近的CHECKPOINTER快照恢复 这里有一个细节 line graph的中断是节点级的 而一诺的中段可以精确到任意组件级 这个力度差异在实际使用中 会影响你能不能做更细粒度的暂停恢复 接下来看ENOENO的ADK层 提供了三种预构建的multi agent模式 deep agent是单agent 带有循环执行能力 适合反复调用工具 直到满足条件的场景 supervisor是一个manager 管多个执行者 plan execute是把思考和行动分离到不同agent 类似人类的事前规划加事后执行 这三种模式在编译期就确定了图结构 但a NO最核心的贡献是agent two 把任何agent包装成标准的工具 从负agent的视角调用search to和调用code agent完全一致 发一个工具 调用JASON等一个工具 结果字符串 这消除了调工具和调agent的区别 multi agent的协作不再需要特殊处理路径 有一个关键细节 自agent发出的结束自己或把控制权给别人 对父agent是透明的服务 agent只会看到这个工具调用完成了 不会知道内部发生了什么路由 这是封装的关键 他真正把子agent变成了黑盒工具 supervisor模式还有一个关键机制 Agent with deterministic transfer to 它规定sub agent只能把控制权转回supervisor 不能横向传给其他sub agent 这是在框架层强制执行的 不依赖prompt约束 为什么这么强调 因为如果sub agent可以横向转移 会产生无法预测的路由 循环限制到只能转回supervisor 就保证了整个控制流的确定性 你知道每次执行路径是什么样的 INNO还有一点值得强调 graph泛型带来的类型 安全图的输入输出类型在编译期就已知 节点之间的数据流类型也被框架验证 这是go语言类型系统的直接收益 Python框架很难做到同等程度的编译器检查 在大规模工程里 这个价值非常大 你永远不想在生产环境里才发现 类型不匹配的问题 好我们看第三个项目 Goose goose的ORCHESTRATOR是一个platform extension 提供spawn agent工具 主agent调用这个工具传入任务描述和工具白名单 goose启动一个完整的子agent实例 看这个图 主agent派发了三个子任务 并行执行 关键点子agent是完整的 goose agent不是轻量的子任务 它有自己的工具调用历史 可以递归调用spawn agent 再启动自己的子agent 形成任意深度的树形结构 这里我要强调goose和superpowers的最大差异 goose允许树形递归 superpowers不涉及递归 ghost的设计哲学是给LLM最大能力 让他自己决定需要多少层 super powers的哲学是限制LLM的自由度 确保每一步都可审计 这是两种完全不同的信任模型 没有对错之分 取决于你的场景更需要灵活性还是可控性 goose orchestrate的三个层次很好理解 调度层由主agent通过spawn agent工具发起 LLM自己判断需要派几个词agent 通信层通过工具调用的返回值传回复agent 隔离层靠独立的session保证每个子agent有自己的历史 但灵活性的代价是 每次运行的并发模式可能不同 相同任务这次可能派二个子agent 下次可能派五个 这对可重现性是个挑战 好现在看superpowers 他是三种路线里最保守的 Dispatching Parallel agents 这个skill规定并行的前提 四个条件全部为真 才能并行 缺一不可 这四个条件看起来简单 但每一条都有深刻的功臣教训在里面 为什么条件这么严苛 因为并行agent且同一个文件会导致覆盖修复 相互依赖的bug 可能导致一个agent的修复 让另一个agent的测试从红变绿 第二个agent误以为自己也修好了 这些不是假设风险 而是真实发生过的场景 Sub and driven development 这个skill更严格 task k必须完成 并通过双轮审查才能发出TASCAR 这不是技术限制 而是策略选择 Dispatching parallel agents 还引入了预隔离的概念 好的域划分是每个agent专注一个独立的业务域 比如一个修认证模块 一个优化订单性能 一个修邮件服务三个月 代码不重叠 没有冲突 坏的划分是按数据库或性能这种维度来分 agent a和agent b可能都要修改同一个函数 产生冲突 预隔离的本质就是操作范围不重叠 superpowers要求在派出并行agent之前 就确认这种不重叠 并行agent的完成后汇总也有明确要求 首先确认没有冲突 两个agent是否修改了同一个文件 然后运行完整测试套件 注意是合并后的全集 不是每个agent单独跑的测试 因为即使每个agent独立验证了自己的修复 多个修改叠加后可能产生意外的交互效果 这是一个经常被忽视的步骤 superpowers把它写进skill 强制执行 现在看第五个项目 Oh my open agent 他的编排层次最丰富 三层架构PROMETHEUS 做规划访谈 用户需求 生成计划文件 它是只读的atlas做编排读取计划 并向执行层委派任务 csus junior做执行实际写代码 而且被框架限制 不能再次委派任务 这个三层分离和IO的plan execute加supervisor模式在概念上类似 但实现方式完全不同 i know是框架原生支持 oh my open agent是通过prompt工程实现 atlas有一个很独特的设计 wisdom积累每个子agent完成任务后 atlas提取经验 成功模式失败教训项目约定写入一个markdown文件 然后注入到所有后续此agent的prompt 这实现了任务间的经验传递 不需要共享状态 不需要数据库设计 哲学是有损 但高效信息在提炼过程中可能丢失细节 但能适应上下文窗口限制 而且对每个子agent都是全新的 上下文 没有前序任务的噪音 这和IO的session共享数据是两种完全不同的思路 还有一个特色是类别系统 阿特拉斯可以用语义描述任务需求 比如这个任务需要深度推理或快速执行框架 负责把类别映射到具体模型 Visual engineering 用带视觉能力的模型 quick用又快又便宜的模型 这比dear flow里手动指定model name高了一个层次 艾特拉斯不需要知道每个模型的具体能力 只需要描述任务的性质 第六个项目nano clock 他的多agent的概念跟前几个完全不同 它不是协作模型 而是隔离模型 每个group运行一个独立的agent实例 group之间完全隔离文件系统 记忆session各自独立 这是一种横向扩展的swan模型 比如一个M机器人服务多个用户或群组 每个用户有自己独立的agent的实力 好现在我们来横向对比一下这五个项目 deer flow和ANNO是代码决定 goose和o my open agent是LLM决定 superpowers是规则决定 从递归深度看 goose是理论上的无限递归 其他都有限制 从并行度可预测性看 代码决定最高LLM决定最低 规则决定高 但需要人工写条件 从状态共享看 Dear flow 用lg graph state I know 用session or my open agent 用wisdom notepad 其他不共享 我们来深入分析几个问题 首先是goose的树形递归 理论上goose允许无限深度 但实践中受三个隐性约束限制 第一个是上下文窗口 每个sport agent调用和返回结果都占用for agent的上下文 递归越深 累积越多 第二个是成本 每个词agent都是完整的API调用序列 树形递归的token消耗是指数级的 第三个是工具白名单 副agent通常给此agent比自己更少的工具 这从根本上限制了递归的意义 第二个深度问题是inode session i know the session是一个在单词run around调用范围内 所有agent的共享的键值存储 它和deer flow的threat state有什么区别 threat state跨请求持久化到SQLIT 每次带thread eid调用都能看到历史状态 session仅限单次运行结束后就消失 除非你用checkpoint序列化 所以a NO的多agent的协作有两层共享 短期共享用session 长期持久化用checkpoint Dear flow 只有一层goose的session存在 SEQUI是对话历史 不是专门的agent兼共享 第三个深度问题是super powers的双轮审查 为什么是两轮规范合规审查 回答实现了吗 对比原始规格 验证所有要求点被覆盖 不评价代码写的好不好 代码质量审查回答写得好吗 评估可读性 健壮性与现有模式的一致性 不评价功能是否完整 为什么要分两轮 因为混在一起审查容易产生功能完整 但代码很烂的通过或者代码很好 但少实现了一个要求的 通过两轮分开 确保两个维度都达标 都好 进入核心洞察环节 第一个洞察代码定义并行vs LLM决定并行 本质是信任边界的选择 DEFLOW信任代码不信任LLM的运行时判断 goose把判断权完全交给LLM 前者适合已知任务类型 后者适合位置任务类型 第二个洞察superpowers的四个条件 A l l must be true 是最保守的设计 但这不是过度设计 而是来自对真实生产环境的深刻认识 宁可串行慢一点 也不要两个agent打架 第三个洞察 agent的agent two是整个系列里最干净的抽象 for agent不需要知道自己调用的是工具还是agent 编排层的等价性 在其他项目里没有做到这个程度 都 继续核心洞察 第四个洞察 oh my open agent的wisdom积累 不通过共享状态传递经验 而是通过文本提炼后注入prompt 这是有损但高效的设计信息会丢失细节 但每个词agent都有干净的上下文 第五个洞察 goose的无限递归更多是设计哲学 不是实际使用场景 上下文 窗口和token成本会在很早阶段就阻止无限递归 第六个洞察 也是最本质的一个 多agent的编排的本质是信息隔离和共享的平衡 完全隔离 保证安全 但没有协作 完全共享 有协作 但要解决并发冲突 好现在进入实操环节怎么选方案 固定批处理任务 选deer flow或i know的DAIG图结构 因为拓扑静态确定可重现 探索性开放任务选goose orchestra 因为需要LLM实时判断任务类型和并行度 有影视依赖的分布任务 选superpowers的强制串行加双轮审查 需要并行 但担心文件冲突 用superpowers的四条件检查 human in the loop审批场景 INNO的checkpoint加interrupt是最佳选择 多用户隔离场景 nano cloud group隔离模型是最合适的 最我们把决策流程整理成一个清晰的路线图 首先问任务模式是否固定且重复 如果是选代码定义拓扑 如果不是选LLM决定并行 如果选了代码定义拓扑 再问是否需要human in the loop 审批需要就选INNO 不需要就选DEFLOW 如果选了LLM决定并行 先问任务步骤是否有影视依赖 有就选superpowers强制串行 没有就继续判断是否担心文件冲突 最后我们来聊聊跨项目互借的可能性 GHOS如果引入superpowers的四条线检查 在orchestra的system prompt里加入并行 前置检查清单 要求主agent在决定并行之前 明确回答这四个条件是否满足 这不是代码约束 是prompt层的结构化验证 改动成本低 但效果显著 DEFLOW如果需要处理动态任务类型 可以在supervisor节点加入任务类型 路由逻辑已知类型走固定路径 未知类型走GHOSTYLE 动态派发agent如果引入wisdom积累 在agent two完成回调里加入提取器 每个子agent完成后把经验写入session的wisdom键 后续子agent启动时注入prompt pro好 我们来做个学习验证 读完这章 你应该能回答六个问题 第一个dear flow的supervisor怎么决定并发几个researcher 答案是条件边路由Python代码决定 不是LLM 第二个agent with deterministic transfer to 解决什么问题 防止sub agent横向转移 只能转回supervisor 第三个goose的spawn agent和agent to有什么区别 一个是运行时动态启动 一个是编译期注册的工具包装 后面三个问题留给你自己思考 答案都在这章内容里好 最后做个总结 六个项目 六种设计哲学 Dear flow 用代码定义静态拓扑 靠line graph的节点及中断实现人机交互 a NO用agent two实现了最干净的agent及工具抽象 配合类型安全 goose把调度权完全交给LLM支持树形递归 但受限于成本和上下文 superpowers用最保守的四条件确保不打架 双轮审查 保证每步都扎实 Oh my open Agent 用三层分离和wisdom积累 实现了独特的知识传递 nana claw则干脆放弃了协作 走完全隔离的路线 没有最好的方案 只有最适合场景的方案

## 标签
[]
