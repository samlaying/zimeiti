---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p14 14-工作流编排引擎对比 [BV1kdoHBCEoC_p14].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p14 14-工作流编排引擎对比

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

大家好 今天我们来聊一个很有意思的话题 工作流编排引擎对比 在开始之前 我想先让大家意识到一个关键问题 这五个项目里提到的工作流 根本不是同一个东西 DIFLOW用Python代码画图 INO有双引擎自动切换 goose用YAO声明意图 Oh my open agent 直接用markdown当工作流 superpowers则用两个skill处理不同的上下文场景 要真正比较 他们必须先问他们到底在解决什么具体问题 Dear flow 解决的是固定拓扑的多agent的协作 goose解决的是可参数化的可重复任务 on my open agent解决的是人类写的计划如何被agent执行 不同设计自然不同 这就是我们今天要深入探讨的核心 好我们先来看dear flow 他的工作流是命令式的 你告诉land graph 节点A执行完 根据条件X去节点B或节点C 这就像写程序步骤是你规定的框架 只是执行者 我们来看这段代码 你看到了吗 ADD conditional edges接收的是一个Python函数 Rt to agent 这个函数可以读取完整的thread state 做任意复杂的判断 这就是表达能力最强的地方 你可以写 如果搜索结果质量低于阈值 重新派一个researcher这样的逻辑 CHECKPOINTER在每个节点执行后保存完整state 快照到SQLIT 这是节点及的检查点 精确到哪个节点 执行完了 命令式的优势是可预测 可重现可审计代价是灵活性受限 要改变执行模式 必须改代码DEFLOW选择这条路 因为研究报告生成有固定模式 总是需要researcher搜索coder处理代码 supervisor协调 这个模式是业务知识 写在代码里是合理的 刚才我们讲了DEFLOW的图结构 这里我想特别提一下make land agent这个工厂模式 它是DEFLOW工作流的灵活性入口 你看每次请求时调用这个工厂函数 根据请求参数动态组装工具列表和中间件列 然后编译图 这意味着什么呢 同一套工作流逻辑 可以根据配置产生不同的agent的实例 比如你可以用不同的模型 可以用不同的并发策略 可以开或关子agent工作流的拓扑结构是固定的 但工具链是可以动态替换的 这是一种很聪明的设计 既保持了图的确定性 又给了足够的参数化能力 接下来我们看i know 这是一个很有意思的设计 它在同一个框架里提供两种执行语义 先说第就是有向无环图数据从输入流向输出 每个节点只执行一次 适合ETL流程和固定步骤的pipeline PGGO呢节点可以多次激活 当输入channel有数据时就执行适合reactor循环 就是LLM调用工具工具返回结果 再调用LLM这样反复迭代的场景 关键设计在这里 当你往graph里加入循环边编译器 检测到环自动切换到PRODULE引擎执行 你不需要声明我要用哪种引擎编译器 帮你做选择 graph泛型是go类型系统的直接收益图的输入 输出类型 在编译期确定节点之间的数据流类型也被验证 Python的类型注解只是提示 go的泛型是约束在大型工程里 这个差异巨大 好既然说到了INNO 我们来深挖一下他和LANGRAPH的本质区别 表面上看他们都是有向图 但执行语义完全不同 line graph是状态机 它的循环是通过条件边返回之前节点实现的 在逻辑上仍然是deck 同一个节点可以多次被激活 每次激活是一次新的步骤 所有节点共享同一个state对象 而inner preggo是真正的迭代计算模型 节点有激活条件 当输入channel有数据时才执行多轮迭代中 同一个节点可以收到不同批次的数据 做出不同输出 这更接近数据流处理的思想 类似FLINK的流处理 实际影响是什么 inner preggo更适合复杂的并发激活模式 比如多个词agent的结果 同时汇聚到同一个chat model节点 如果你需要这种场景 innode pro引擎更合适 现在我们来聊一个很关键的话题 容错 这是一诺比lg graph更强大的地方 langraph checkpoint只支持节点及回复 如果某个节点失败 从上一个成功节点的快照重新执行 整个节点重新跑 而i know的checkpoint支持精确恢复到任意中断点 包括组件内部的中断 比如某个工具执行到一半的状态 i know都能记住 这是怎么做到的 看这个结构体 checkpoint不仅保存state 还保存每个节点的input字段 恢复时可以精确重放特定节点 不需要重跑整个节点状态 通过GOP序列化持久化到checkpoint store 在生产环境中 这个差异意味着 line graph恢复时可能浪费大量已经完成的工作 INNO则可以最大化复用已有的计算结果 好现在我们来聊一个完全不同的风格 Goose recipe 先看这段YAMMO 这是什么 这是一个工作流的定义 但注意看他没有写任何执行步骤 他只写了你是安全专家 要分析SQL注入XSS密钥硬编码问题 要分析的代码路径是什么 输出格式是什么 这就是goose recipe的核心理念 描述意图 不描述实现 你告诉agent他要什么 不告诉他怎么做怎么做 由LLM决定 instructions定义了系统角色 prompt是触发消息 parameters是参数化输入 Response jason schema定义了输出格式 retry配置了任务及重视 这是声明式的最纯粹形式 和dear flow的告诉框架怎么走 完全相反 百刚才我们讲了goose recipe的理念 现在深入看看它的三大机制 第一个是final output tool 这是goose实现结构化输出的绝招 当recipe设置了response jason schema goose向agent注入一个final output工具 agent的必须调用这个工具传入 符合schema的JASON才能停止 不符合schema就报错agent 修正后重试 这个设计不依赖任何provider的structured output API 适用于所有LLM 第二个是retry config 如果整个任务完成了 但verification prompt的LLM验证判定质量不达标 重置绘画从零开始重新运行 整个recipe 这是推倒重来的策略 不带任何之前的上下文 比节点及修复更彻底 但也更低效 第三个是sub recipe 副recipe可以调用子recipe 形成树形任务结构 Sequential when repeated 控制同一个子recipe被调用多次时 是串行还是并行执行 这里我想特别提一个 goose recipe里容易被忽视的设计 recipe安全检查 goose在加载recipe时 会扫描instructions和prompt中的UNICODE标签字符 这是防止什么的 防止通过发布恶意recipe来攻击故事用户 UNICO的攻击 你知道吗 比如零宽字符可以注入隐藏指令 同行字符可以替换混淆语义 这些在recipe里尤其危险 因为recipe是可以被分发和引用的 GOOOSE的recipe parameter支持六种输入类型 其中file类型禁止设默认值 就是为了防止意外读取敏感文件 这是一个很好的安全意识 工作流系统应该从一开始就把安全考虑进去 而不是等出了问题再补救再好 现在我们来看一个更极端的设计 Oh my open agent 他的工作流引擎是什么 一个markdown文件 atlas读取执行 Border jason 跟踪进度没有yo schema 没有图结构 没有类型检查 LLM天然善于读取和生成markdown 人类也天然善于读取和编辑markdown 这就是全部的理由 atlas的执行是线性的 读取计划 从第一个未完成任务开始 委派给junior验证结果标记完成下一个计划 文件里可以有并行提示 但并行与否由atlas这个LLM决定 不是框架保证 这是最务实的设计哲学 引入专门的工作流格式 是额外负担 前提是你相信atlas能正确解析和执行markdown计划 而实验证明他确实能做到 接下来是superpowers 他用两个skill处理两种完全不同的上下文策略 executing plans是跨绘画的 你在绘画A写好计划文件 关闭会话 然后打开会话B读取计划文件 从零开始执行 AI没有绘画A的任何记忆 只有计划文件 适合的场景是计划已经完成 现在要在新环境执行 Subean driven development 是同绘画的 写计划后 在同一会话里立即执行每个任务 给新鲜的sub agent 每个SABEN的上下文从零开始 不受前序任务干扰 完成后还有双轮审查 更妙的是 Sub agent driven development 允许根据任务复杂度选择不同模型 机械型任务 用廉价模型架构决策 用最强模型 这在单一模型执行所有任务的系统里是没有的 优化维度不刚才说的两种skill 它们的本质区别是什么 是上下文边界 executing plants里 AI的上下文会随着任务积累 后期任务面对越来越长的上下文窗口 可能影响推理质量 Subbugine driven development 让每个任务的SUBBUGAN从零开始 不受前序任务干扰 质量更稳定 这揭示了一个经常被忽视的维度 工作流的状态在哪里 是在AI的上下文里易受污染 还是在外部文件里持久可恢复抗干扰 superpowers明确支持跨绘画的工作流 任何时候打断都可以恢复 另外当慈悲JON报告blocked时 superpowers不强制重视 因为重试大概率得到同样结果 而是上升给用户决定 这是失败时停止并报告的策略 不是失败时重试爱好 这里我想做一个有趣的思想实验 如果跌flow把工作流定义从Python代码改成goose Recipe yo 会发生什么 我们来分析一下会失去什么和会得到什么 会失去的确定性 并行recipe的执行路径由LLM决定 你无法保证researcher节点一定并发执行 精确条件路 由at conditional edges的Python函数可以做复杂判断 recipe instructions只能引导LLM并发状态安全 Threat state reducer 保证并发写入安全 recipe没有等价机制 精确可观测性 Land graph the state 快照可以精确重放 recipe执行过程难以重现 会得到的非技术人员可以定义工作流 YAO比Python门槛低得多 recipe文件可以像配置文件一样 分发和版本管理 parameters字段 让工作流变成可复用模板 Final output two 确保格式不靠 Prompt engineering Retron fake 比手动重跑更优雅 那么什么时候用recipe 什么时候用line graph 结论很清楚了 recipe适合意图明确步骤 由LLM决定的工作流 需要非技术人员可编辑 工作流 需要可分享和版本管理 line graph适合步骤固定 需要精确控制的工作流 需要确定性并行 需要精确的状态管理和可观测性 具体到da flow的研究报告生成 它的拓扑是固定的 Coordinator Supervisor Research encoder 这个模式是业务知识写在代码里合理 ghost的代码安全审查步骤不固定 LLM自行决定检查什么 用recipe更合适 所以你看没有绝对的好坏 只有场景的匹配 选错了 痛苦的是 开发者和用户选对了工具会帮你省很多力气 一点好 我们来做个横向对比 从五个维度来看 这五个项目定义方式 Dear flow 用Python代码INO 用go代码 Goose 用yao o my open agent 用markdown superpowers 用skill加markdown执行引擎 dear flow是lang graph状态机 INNO是deg或PGGO自动选择 goose是agent 自由执行 on my open agent是atlas线性执行 superpowers是cloud内置task 工具类型安全只有INO有强类型 保证go泛型 其他都是无或弱并行原语 dear flow和ELO有框架级的并行支持 goose和all my open agent的并行完全由LLM决定 结构化输出 只有goose有final output two这种可靠的机制 看到了吗 每个系统的设计都反映了他的核心取舍 没有银弹 我们继续看完整对比矩阵 从四个核心问题来看 状态持久化 Deer flow 用SQLITE做节点及快照 INO用GP加checkpoint store 支持精确回复 goose没有工作 留级的持久化 Oh my open agent 用boulder jason跟踪进度 super powers没有框架级的状态管理 容错策略 Deer flow 用条件边路由到修复节点 INO用checkpoint精确恢复 Goose 用RETRACONFIG任务及重跑 all my open agent做任务及重试 superpowers在BLOCAKE时上升给用户可观测性 Dear flow 有length smith trace ino 有RAMPPATH调用列 goose和all my open agent基本没有superpowers 也没有参数化 Deer flow 用runnable configure I know 用图输入类型 goose用parameters字段 其他没有可观测性 是被低估的维度 当agent的出错时 在哪一步出的问题和工作流能不能跑起来 同等重要 现在我们来深入分析一个有趣的话题 RETCONFIG和lg graph条件边都在处理 失败后怎么办 但力度和语义完全不同 line graph条件编是节点及的路由决策 一个节点执行完 根据状态决定下一步去哪 可以去修复节点 可以去重试节点 可以去END 这是工作流内部的流控制 由开发者在代码里定义逻辑 关键是可以利用之前积累的上下文 state里的消息历史还在 goose retract config是任务集的整体重视 整个agent的会话 执行完verification prompt 判定质量不达标 重置绘画从零开始重新运行 不带任何之前的上下文 这是推倒重来的策略 更彻底 但也更低效 选哪个 取决于你对失败模式的假设 如果你相信失败是因为中间某一步出了问题 需要修补 用line graph条件编 如果你相信失败是因为整体方向错了 需要完全重来 用rich config 两种假设都可能正确 取决于任务类型 我们来聊聊可观测性 这是被低估的维度 DEFLOW通过link smith做到了工作流执行的完整追踪 每个节点执行都有trace 包含输入输出 耗时模型 调用详情 land graph server的get端点支持随时查看当前state 给定threaded和时间点可以重现任何历史执行状态 AO的wrong path字段 记录了从根agent到事件来源的完整调用链路径 call back可以过滤特定词agent 事件力度不如lank smith 但足够追踪多agent执行路径 goose recipe呢基本没有工作留级的可观测性 能看到A证的工具调用历史 但没有recipe级别的执行追踪 On my open agent 最接近可观测性的是atlas写入的notepad文件 Learning decisions Issues verification 分类记录 这是人类可读的执行记录 优点是系统崩溃也能事后重建 缺点是无法程序化查询或重放 在生产环境里 能知道出错在哪一步 和工作流能跑起来同等重要 好我们来总结几个核心洞察 第一个是一诺的正交分解设计 pipeline和A阵循环是性质完全不同的计算 pipeline数据从A流到B只走一次 A证循环是LLM调用工具 再调LLM可能无限次共用一个执行器 效率损失 INNO的解决方案是双引擎 DG处理无环数据流 pro处理迭代循环 编译时自动检测环自动选择引擎 更妙的是 这个选择是自动的 你不需要声明我要用哪种引擎 编译器帮你做决定 这是框架设计层面的正交分解 把执行什么和怎么执行分开 让开发者专注于业务逻辑 而不是底层执行细节 第二个核心洞察是工作流的配置化 Goose recipe 把工作流从代码变成了数据 这带来一个权衡表达能力 降低了 你不能写条件循环这些复杂逻辑 但是收益是什么 可分享recipe文件可以像配置文件一样 分发和版本管理 可参数化parameters字段 让同一个工作流变成可复用的模板 非技术人员可编辑YMO 比Python代码门槛低得多 不需要会编程就能定义工作流 这是一种工程哲学的选择 配置化和编程化不是对错之分 而是适用于不同场景业务专家主导的工作流 用配置化程序员主导需要复杂逻辑的工作流 用编程化 硬要把业务专家拽来写代码 或者让程序员用亚某写复杂条件判断 都是自找麻烦 第三个核心洞察来自superpowers 上下文边界的隐藏设计 superpowers揭示了一个经常被忽视的维度 工作流的状态在哪里 是在AI的上下文里 还是在外部文件里 如果在AI的上下文里容易受污染 一旦上下文出问题 工作流就乱了 如果在外部文件里可以持久化 可以恢复抗干扰能力强 executing plans的计划文件就是唯一上下文会话 可以随时关闭 随时恢复 Subbajan driven development 让每个任务从零开始 不受前序任务干扰 这给我们的启示是什么 设计工作流系统时 如果你需要支持随时中断 跨绘画恢复状态必须存在于外部 而不是依赖AI的上下文记忆 AI的记忆是脆弱的 外部文件才是可靠的 这好现在我们来聊点实用的 怎么选 我们先看快速决策表 已知固定步骤需要可分享和可复用 推荐goose recipe的YAMMO声明 是因为非技术人员可编辑参数化模板 版本管理 探索性研究步骤不固定 推荐DEFLOW或on my open agent 让LLM自主决定步骤 灵活适应未知情况 大量固定流程需要类型安全 推荐innode dag或PRAGGO 双引擎编译时验证dag自动并发 PRAGGO支持循环动态决策步骤 依赖LLM判断 推荐line graph状态机或atlas条件编 让LLM决定下一步跨会化 长期任务随时可中断恢复 推荐superpowers executing plans 计划状态在外部文件绘画无关 同绘画高质量执行需要双重审查 推荐sub agent driven development 计划者和执行者分离 提高质量 我们来看决策流程 这个流程图可以帮助你一步步做选择 第一步问自己任务步骤是否已知且固定 如果是goose recipe是不二之选 还可以根据需要开启final output two 保证结构化输出配置 retry config加verification prompt 做质量门控 如果否是探索性任务步骤由LLM决定继续 第二步 且需要编译时类型安全和高并发 如果是INNO的tag或PRAGGO双引擎是最佳选择 如果否继续 第三步 比如超过两小时 或者需要人工审查 如果是superpowers的executing plans 支持计划文件 跨会话持久 如果否继续 第四步 是否需要同会话内高质量执行加双重审查 如果是sub agent driven development适合 如果都不是 那就用DEFLOW的lg graph 或者oh my open agent的markdown工作流 我们来具体看几个场景 帮助大家把刚才的决策框架用起来 场景A是知识密集型固定流程 比如竞争分析报告 推荐用goose recipe的Emo声明式 为什么 因为报告生成的步骤是已知的 数据收集分析撰写格式化 这个模式不会变 YAMARECIPE可以参数化一个模板 可以分析不同公司的报告 只需要改company name参数 Final output two 保证输出的报告符合schema 不会出现差不多的输出 RETRACCONFIG自动重跑 质量不达标的结果 质量有保障 如果选错了 用dear flow and graph 工作流需要代码定义 团队协作门槛高 分析师改不了代码 用all my open agents的markdown工作流步骤不确定 报告质量不稳定 不适合生产化场景 场景B是复杂技术 研究步骤未知 需要LOLM自主探索 这时候推荐deer flow的lg graph 加多agent的协作 为什么研究任务的步骤无法预先完全定义 Coordinator supervisor researcher的分层 允许动态分配子任务 LLM根据中间结果决定下一步探索方向 条件边让路由完全灵活 可以处理发现新方向这种意外情况 SQLITCHECKPOINTER支持长时间研究的断点续传 你可以随时暂停 随时恢复 不用担心上下文溢出 link smith提供完整的执行trace 用于事后分析出问题了 可以精确找到是哪个节点 哪个步骤出了问题 如果选错了 用goose recipe固定步骤无法处理 发现新方向的场景 研究质量受限 用oh my open agent 缺乏精确的状态管理和可观测性 长时间研究的进度难以追踪 懂 场景C是企业级go服务 高并发数据流处理 推荐yo的tag加pro双引擎 为什么go团队天然石和INNO 不需要引入其他语言的运行时 tag引擎处理无环数据流 比如文档解析分析报告深沉自动并发执行 没有依赖的节点 PREGGUO引擎处理迭代循环 比如agent的工具调用循环 有循环就自动切换到PREGGUO引擎 编译识图验证 消除运行时类型错误 图里有环会在编译期报错 不会等到运行时才发现问题 如果选错了 用line graph的Python实现go企业服务 需要跨语言调用 增加网络开销和部署复杂度 依赖管理也麻烦 单一line graph状态机做数据流处理 并发效率不如deck 因为状态机不是为高并发数据流设计的 最后我们来聊聊跨项目借鉴建议 这是我从这五个项目里提炼出来的通用经验 第一结构化输出终止条件比prompt engineering更可靠 Goose recipe final output tool 把完成定义为可验证状态 LLM必须调用特定工具提供JASON输出 而不是靠LLM自己判断 差不多了 任何需要可靠终止条件的agent的任务 都应该考虑这个模式 它比prompt engineering稳定得多 第二声明是工作流和代码 工作流的选择依据是修改者是谁 如果工作流需要业务专家或非程序员参与维护 YELMO或markdown是正确选择 如果工作流只有程序员维护 且需要复杂逻辑代码 工作流更合适 不要让业务专家写Python 也不要让程序员用YAO写复杂条件 第三可观测性应该是工作流系统的一等公民 Dear flow 通过length smith获得完整trace 事后可以精确重放任何执行状态 在设计工作流系统时 从第一天就应该规划如何获取每个节点的输入 输出和执行时间 而不是等出问题了才想办法加嗯好课程的 最后给大家一份源码索引 方便大家深入研究 dear flow的make lead agent 工厂和中间件组装在lead agent的agent dpi理图注册和checkpointer page 在lang graph after jason innode graph 泛型定义和DGPREGO的编译逻辑 在compose graph dog go check point 结构体和GB序列化 在compose checkpoint dot goose the recipe结构体 Her recipe parameter 在recipe mod r final output tool Her retrconfigures Are agents agent r s o My open agent the atlas jing逻辑 在agents atlas目录下 Super powers the executing plans和supagent driven development的完整指导 分别在两个skill文件里 这些源码都开源 大家可以对照今天讲的内容去读 会理解的更深 嗯最后我们来做个学习验证 读完这篇对比文章 您应该能够回答这六个问题 第一个 INNO的deck引擎和PGLE引擎分别适合什么场景 记住dag适合无环数据流 PREGGO适合有循环的迭代计算 编译器自动选择 第二个 goose recipe的final output two解决了什么问题 解决了强制结构化输出的问题 不依赖provider的structured output APILLM必须调用工具提供JASON才能停止 第三个 如果dear flow用goose recipe定义工作流 最大的损失是什么 确定性并行和精确状态管理 recipe的执行路径由LLM决定 状态管理没有reduce保障 第四个 superpowers的两种skill在什么情况下选哪个 跨绘画有现成计划 用executing plans同绘画实时执行 且质量要求高 用sub agent driven development 第五个 oh my open agent的markdown工作流和deer flow的land graph图 各自的核心优势是什么 前者人类可读 可编辑 后者精确可追踪可审计 第六个 为什么goose recipe的RHETCCONFIG 重视时不带之前的上下文 因为他重置绘画 避免上下文污染 代价是重新消耗token好 今天的课就到这里 希望这些内容对大家有帮助

## 标签
[]
