---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p21 21-调试与可观测性 [BV1kdoHBCEoC_p21].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p21 21-调试与可观测性

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

欢迎来到调试与可观测性这一讲 我是你们的架构师导师 今天我们来聊一个生产环境 AI系统最容易被低估的能力 很多团队在搭建agent系统时 把80%的精力放在功能实现上 却发现上线后出了问题 根本不知道怎么调试 为什么agent的调试比普通程序难 因为agent的行为是非确定性的 同样的输入不同时间可能产生不同输出 你无法通过重新跑一次来稳定复现问题 当agent调用了一个错误的工具 或者给出了一个奇怪的回答 你需要知道它的推理过程是什么 他看到了什么上下文 他的中间状态是什么 我们来看六个项目在这个维度上的差距 从几乎没有调试能力 到完整的切面系统和快照回放 在分析具体项目之前 我们先建立一个评估框架 可观测性分四个层次 L1能看到agent说了什么 这个所有项目都有 但意义有限 L2能看到A证做了什么 也就是工具调用记录 大多数项目做到了这个程度 L3能看到agent为什么这么做 也就是中间推理过程 这个只有少数项目有 L4能回放和重现agent的完整执行过程 及少数项目达到这个层次 L1解决结果是什么 L2解决路径是什么 L3解决决策是什么 L4解决能重现吗 我要特别强调 真正的生产调试需要L3以上 如果只能看到agent的做了什么 但不知道为什么做 那调试本质上还是在踩 好框架建好了 我们来看第一个项目EOEO的call back Ks interface go 定义了五个call back timing 第一个是on start 组建开始处理前 第二个是on end 组件成功完成后 第三个是on error 出现错误时 还有两个是留相关的 on start with stream input和on and with stream output 这不是普通的日志钩子 这五个时间点 覆盖了组件生命周期的所有关键节点 不只是成功和失败两个状态 流式输入和流式输出也各自有独立的时间点 这意味着你可以精确的知道数据 什么时候进入组件 什么时候流出组件 我们继续看他的高明之处 handler接口有一个关键设计 每个方法返回context context 而且这个返回值只传给同一个handler的 下一个时间点 不传给其他handler 这意味着什么 一个追踪handler 可以在on star时创建一个open telemetry span 然后在on send时取到这个span来关闭它 整个操作对业务代码完全透明 不需要修改任何组件代码 只需要注册一个handler就行 这就是分布式追踪的自然接口设计 你不需要在业务代码里手动传递 trace context框架帮你搞定 这个设计非常优雅 我们接着看他的另一个精妙之处 妙另一个精妙之处是timing checker 接口流相关的时间点需要复制流副本 并启动GROOUTINE来消费副本 这是有代价的 如果handler声明我不需要这个时间点框架 就跳过这个开销 不是盲目给所有handler复制所有流 而是先问你需要吗 只有需要的才付代价 在高吞吐场景下 这个机制 让call back c系统的性能影响可以接近于零 这才是工程上真正负责任的设计 不给你用不到的东西 还要让你能精细控制 INNO的call back x系统能达到L3 通过on start记录完整输入 on end记录输出on error 记录错误细节 全部不需要修改业务代码 不每次call back调用都携带run info 包含三个字段 name是业务名称 也就是图解点的名字 type是实现类型 比如OpenAI还是OOMA component是组件类别 比如cheat model还是to 在一个复杂的agent to里 可能有十几个节点 通过component先过滤类型 再通过name区分具体节点 可以实现非常精确的观测点 比如你只想看所有chat model的调用 或者只关注搜索节点的具体行为 都可以精确配置 这是IO的设计思路 接下来我们看dear flow 走的是完全不同的路线 DEFLOW采用了一种与IO完全不同的策略 不构建内部call back系统 而是集成外部追踪工具 lank smith配置极其简单 设置line train tracing v2等于true 环境变量 所有line graph节点执行自动上传到length smith 在make led agent的工厂函数里 每次创建agent时注入原数据 Agent name Model Name Thinking enabled is plan mode等等 这些字段在lank smith界面里变成过滤标签 你可以查询所有使用cloud 且开启了thinking模式的执行 平均花多少token 这是很实用的能力 但DEFLOW的第二个可观测性优势来自架构本身 我们接着看这land graph的状态化架构 是deer flow的第二个可观测性优势 Land graph 强制每个节点接受state对象 返回state更新 这意味着每个节点的输入和输出 天然是结构化的 可记录的状态 不是黑盒里的隐变量 而是一等公民 你知道每个节点进来是什么状态 出去是什么状态 Length meat 优势是有成熟的可视化界面开发 调试阶段价值巨大 劣势是依赖外部服务 在离线环境或隐私敏感场景下无法使用 有趣的是 一诺的内部call back和deer flow的length smith集成 这两种策略并不互斥 一诺完全可以实现一个handler 把数据发给length smith 本地实施加云端可视化双保险 接下来看hermes agent 他面临一个其他项目不太关注的问题 它既是一个生产agent 也是一个用来生成训练数据的系统 这带来了一个独特的设计张力 调试信息的受众是工程师 但在AIA阵场景里 调试信息往往出现在消息历史里 而消息历史会被LLM读取 如果LLM在训练时看到大量BUGITERATION 590这样的scaffolding信息 他可能学会在回复里包含类似格式 破坏了模型的自然对话能力 HERMES的解法是 运行时注入持久化时剥离调试信息 活在Python对象的内存里 写入训练数据前被清理掉 这是个很精妙的设计 Home is the batch runner pi 将每次完整执行保存为JSON L格式的trajectory文件 batch underscore star jsno是每个批次的原始记录 用于调试trajectories JSNO是合并后的干净版本 用于训练每次agent执行的完整历史 包括所有工具调用推理过程 最终答案都在这里 既是训练数据 也是执行审计日志 在convert to trajectory format时 只保留尖括号里的推理内容和工具 调用scaffolding信息被清理掉 这提出了一个更通用的架构问题 如何设计对LOLM透明的调试机制 三种方向 第一种是带外传输 调试信息完全不进LM上下文 通过独立渠道传输 比如open code bus总线 第二种是运行时注入加持久化清理 这是HERMES的方案 第三种是用特殊标记在消息里 用特殊格式标记调试信息 训练时用正则过滤 open code bus总线是带外传输的体现 事件完全走独立的pop sub流 不污染消息历史 这个设计非常干净 我们接下来看goose项目 goose的可观测性设计有两个特色点 第一个是message metadata 每条message都携带两个独立的布尔标志 user visible是否在UI里展示给用户 agent visible是否发送给LLM 这两个维度正交形成四个象限 工具调用的技术细节可以是agent visible等于true user visible等于false LLM需要知道 但用户不关心给用户的提示信息 可以是agent visible等于false user visible等于true 用户需要看到 但不能影响LLM的推理 从调试角度 这个设计让你可以精确还原LLM实际看到的 上下文 只查询agent visible等于true的消息 就是LLM眼里的世界 这是一种L3的近似 不需要特殊的追踪机制 消息本身就携带了可见性信息 第二个是SQLITE存储 Goose 用SQEX库将所有绘画消息持久化到SQULITE 即使经过上下文压缩 原始消息仍然保留在数据库里 当history placed事件发出时 下游消费者可以感知到压缩发生了 而历史记录本身不受影响 agent event流返回三种事件消息 MCP通知还有history replaced History replaced 特别有意义 当上下文压缩发生时 会发出这个事件 下游消费者可以感知到历史被替换了 而不是悄悄发生 一个值得关注的对比 goose是事后分析导向的问题 发生后查sickle lite dear flow的length mah是实时监控导向的问题 发生时看界面 对于A证调试实时性其实更重要 agent的执行往往是一次性的 如果能实时看到推理过程 可以在问题扩大前干预 但 open code是所有项目里可观测性能力最强的 达到了完整的L4 先看bus总线 open code使用effect库的pub sub 构建了一个类型安全的事件总线 提供publish Subscribe subscribe all3个接口 每个关键事件都通过bus发布 任何组件可以订阅 相比于直接调用日志函数 这种架构让监控系统和业务逻辑完全解耦 bus的特殊之处在于 它是带外传输事件 不进入LLM的消息 历时彻底避免了调试信息污染模型输入的问题 snapshot系统更有意思 我们接着看 snapshot系统更有意思 它使用一个独立的git仓库 跟踪工作目录的文件变化 get dear与work tree分离存放在用户目录的local share open code snapshop下面 在每次工具调用前 track方法创建快照 返回hash 出错时 restore方法恢复到那个时刻的文件系统状态 DIF方法可以查看两个快照之间 所有文件的变化 这让open coo具备了真正的L4能力 不只是记录发生了什么 还能回放和撤销 当agent误删了文件或者做了一系列错误修改 revert可以精确回滚工程细节 文件大小限制二兆字节 避免快照大二进制文件自动GC7天清理旧快照 SAMFORCE锁防止并发快照操作冲突 这些细节决定了snapshot在生产环境里是否实用 还是只是个演示功能 我们在看super powers 他的systematic debugging skill 提供了一个不同维度的可观测性 不是工具层面的日志 而是引导AI进行结构化思考的行为规范 核心是条石铁律 先找根音再修复 随机修复的陷阱在于 每次尝试都可能引入新问题 而且你永远不知道是哪一步真正解决了问题 三次修复失败是一个明确的信号 不是执行问题 是方向问题 需要质疑架构 而不是继续在同一层面尝试 四阶段流程 根因调查模式 分析假设与测试 实现修复 纵深防御的视角更有价值 我们接着说 四阶段调试流程 根因调查 读错误信息稳定复现检查近期变更模式分析 找能工作的类似代码 找差异 假设与测试 每次只改一个变量 实现修复 先写失败的测试 再修复纵深防御的视角更有价值 修复一个bug后 在四个层面补充防御入口 验证业务逻辑 验证环境验证调试日志 这是让系统变得可观测的积极方式 不只是被动记录 而是主动设计为可调试的系统 这个skill的局限是 它只对AI在调试代码的场景有效 对于agent的本身的行为异常 你仍然需要工具层面的可观测性来提供数据 两者是互补关系 前者告诉你如何思考调试 后者告诉你执行过程中发生了什么 好我们汇总一下这五个项目 i know达到L3 通过原神call back的五个时间点 在不修改任何业务代码的情况下 注入追踪逻辑 Timing checker 保证了性能可控 deer flow也达到L3 通过lank space集成零代码成本开启追踪 但依赖外部服务 lg graph的状态化架构 让每个节点的输入输出天然可观测 HERMES达到L3 但主要射击目标是训练数据 而非实时调试 它解决了一个独特问题 调试信息和训练数据共存时 如何保证数据清洁 goose达到L2 通过SQLIT持久化完整历史 message metadata的双维度设计 提供了还原LOLM视角的能力 是一种精巧的L3近似open code 达到L4 bus总线加get snapshot的组合 让每次工具调用都可以追溯回放 撤销superpowers是原层面的行为规范 不在L1L4的技术维度里 但他回答的是如何系统化思考调试的问题 我 两个维度的对比值得记住 维度一是实时监控 对事后分析 dear flow的length smith是实时的问题 发生时在界面上实时看到执行状态 ghost的SQLITE是事后的问题 发生后通过查询历史记录还原经过维度 二是内部机制 对外部工具INU的call back是内部的 自包含低延迟类型 安全无外部依赖 dear flow的length smith是外部的 有成熟可视化界面 零代码成本 两个维度上的最优解不一样 实际选择取决于你的场景需求 开发阶段可能更依赖外部工具的可视化能力 生产环境可能更依赖内部机制的可靠性 嗯我们来看几个跨项目借鉴的场景 第一个 如果给HERMES加入INO的call back系统 HERMES目前的可观测性依赖 Step call back 每步调用一次的粗力度回调函数 只有每步结束 没有工具调用开始工具调用结束 LLM开始等细粒度时间点 引入阿诺风格的call back 最直接的好处是 能区分LLM调用延迟和工具调用延迟 目前要定位这一步为什么慢 需要在代码里手动加计时 覆盖不全 维护成本高 有了call back系统后 一个timing handler 就能自动统计所有组件的延迟分布 第二个 如果给goose加入open code snapshot机制 goose是终端工具 频繁进行文件读写 如果加入snapshot 在每次工具调用前记录文件系统状态 就可以实现撤销最近N步操作 当前ghost的history replaced事件 只能感知到消息历史被替换了 但无法还原被替换前的文件系统状态 有了snapshot调试能力会从L2提升到L4 代价是磁盘空间和性能 每次工具调用前的get it快照操作有额外开销 Open code 通过二兆字节 文件大小限制和七天自动清理来控制这个代价 我们来看场景选型决策表 开发阶段需要快速定位bug 推荐homes trajectory文件 因为详细轨迹不影响主流程 事后可完整重放 生产监控需要性能分析推荐inno call back c系统 毫秒级精度区分LLM延迟和工具延迟无侵入 需要知道agent为什么做某个决策 推荐goose message Metadata reasoning和notes字段记录决策 依据多agent系统的调用链追踪推荐 I know wrong path 从根agent到事件来源的完整路径 代码编辑历史支持任意回滚 推荐open code snapshot 每次工具调用前记录文件状态 可精确回滚 数据收集 用于训练或分析 推荐hermes trajectory 系统轨迹与主逻辑结构可选择性保存 低成本集成 外部可观测性推荐deer flow 加long unsmith 无侵入自动trace可视化工作流执行 我们来梳理一个决策流程 首先问自己 你处于什么阶段 如果是开发调试阶段需要什么级别 只需要知道agent调用了哪些工具 那就L2工具调用日志 如果需要知道agent的为什么做某个决策 那就L3推理可见性 如果能够精确重放任何历史执行 那就L4需要snapshot加bus 如果是生产监控阶段 需要性能分析 就用i know call back系统需要多agent调用链追踪 就用anal run pass或外部trace 需要异常告警 就在queback on error里触发 如果是数据收集阶段 用于训练或分析 推荐hermes trajectory系统 最后我们总结三个值得带走的设计原则 原则一调试信息需要带外传输 调试信息一旦进入LLM的消息 历时就成了一个定时炸弹 它可能影响模型行为 也可能污染训练数据 open code bus总线 IO的call back系统都走的是带外路径 HERMES的运行时注入加持久化清理 是退而求其次的方案 在设计新系统时 尽量让调试信息走独立通道 原则二可观测性层次要与调试需求匹配 L1和L2能回答agent做了什么 但大多数生产问题需要L3才能回答 agent为什么这么做 如果你只有L1和L2 调试的主要手段就是猜测 而不是追踪 在agent的系统正式上线前 确认你具备L3能力原则 三调试行为本身也需要规范化 有了完整的可观测性数据 下一个问题是如何有效利用这些数据 superpowers的systematic debugging提供了一个框架 先找根音再修复 用单一假设测试 不要同时改多个变量 三次失败就停止质疑架构方向 这个框架适用于所有agent的框架 总结一下 可观测性是AI agent的系统 从能用到可运维的关键跨越 大多数项目在L1和L2停步 但生产环境的agent调试几乎都需要L3 不知道agent为什么做出某个决策 就无法有效改进prompt或系统设计 IO提供了最完整的内部call back架构 open code实现了最完整的外部状态追踪 也就是snapshot deer flow 展示了如何通过外部工具低成本获得可观测性 goose的message metadata双维度设计 提供了一个精巧的L3进四 HERMES的trajectory剥离机制 展示了调试信息和训练数据的共存之道 没有一个项目在所有维度都做到最好 但每个项目的设计决策都值得借鉴 核心问题始终是同一个 当agent的行为不符合预期时 你能以多快的速度搞清楚发生了什么 感谢大家 我是你们的架构师导师 下次见

## 标签
[]
