---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p11 11-中断恢复与检查点 [BV1kdoHBCEoC_p11].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p11 11-中断恢复与检查点

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

好我们今天来聊一个很有意思的话题 中断与恢复 我先问大家一个问题 你知道这五个项目里的中段说的是同一件事吗 INNO说的是精确恢复到中断点 DEFLOW说的是用户审核后继续执行 goose说的是用户想停就停 HERMES说得是检测agent卡死了吗 oh my open agent说的是任务做到哪了 你看同样叫中段描述的 其实是五个完全不同的东西 把这五样混在一起比较 就像把水果和蔬菜放一起比甜度 根本不是一类东西 怎么比 今天我们就来好好拆解一下 这些中段到底有什么区别 什么时候该用哪一种 该在深入每个项目之前 我们先把这三个维度的问题区分清楚 这三个维度经常被混淆 第一个维度是执行状态的精确恢复 问题是我在第五步按下了暂停键 怎么从第五步继续 而不是从第一步重新来 这是一诺在解决的 第二个维度是用户取消和优雅退出 用户不想让agent的继续了 怎么干净的停下来 这个很简单 goose的cancellation token就能搞定 第三个维度是运行时健康检测 agent还活着吗 有没有卡死 多久没动静了 这个是监控问题 HERMES在干这个 第四个维度是跨绘画任务进度跟踪 上次做到哪了 下次从哪开始 这是任务级的问题 记住这四个维度 我们再来看每个项目到底在解决什么问题 就不会把它们混为一谈了 好说完了整体框架 我们重点来看 I know 为什么我要把INO放在第一个讲 因为他是这批项目里 唯一一个在框架层面原生支持暂停执行 序列化状态精确恢复的yo 提供了三种中断函数 各有各的用途 第一种是最简单的interrupt 就相当于按了个暂停键 附带一条消息告诉用户 我停在这儿 等你确认这个info不会持久化 就是个通知恢复的时候 组件通过get resume context知道自己是被恢复的 第二种是state for interrupt带状态的 这就不一样了 状态数据会被写入checkpoint 比如一个审核工具中断了 他记录 我正在等待审核id等于xxx恢复的时候 通过get interrupt state取回这个状态 就知道该去查哪个审核结果 不需要重新发起请求了 第三种是composite interrupt 这是INNO最关键的差异 当多个并行子流程各自产生中断的时候 这个机制把它们合并成一个信号 负层级 只看到一个中断时间 恢复的时候呢 每个子中段可以收到各自独立的恢复数据 其他项目都没有做到这一点 这是INNO的核心护城河 好我们再深入看看INNO的checkpoint是怎么设计的 这个checkpoint结构体存储的不只是当前在哪个节点 而是整个执行数的完整快照 channels是每个channel的当前状态 inputs是每个节点收到的 输入state视图的共享状态 SUBGRAPHS处理嵌套图的递归序列化 重点来了 interrupt id or other和interrupt id2state这两个字段 一个记录中断id到执行地址的映射 一个记录中断id到组件状态的映射 那这个address是什么 它是精确的执行位置 描述格式是这样的 Runnable route node chat model to search to 意思是在跟RUNNABLE的chat model节点里 search to这个工具执行时中断了 注意是精确到工具级别 不是节点级别 这意味着 如果你有多个工具在同一个节点里调用 印度 能区分出具体是哪一个工具在执行时中断的 用GB做序列化存入checkpoint store 可以是内存 可以是文件 可以是任意持久化 后端灵活的很 还有一个关键设计点 runner在发送中断事件给用户之前就保存了checkpoint 这很重要 确保用户收到中断通知的时候 checkpoint已经可以用于恢复了 如果先通知用户再保存 用户可能在保存完成之前就触发恢复操作 那就乱套了 那么问题来了 中断之后怎么恢复 INNO给了两种回复方式 第一种是runner resume叫影视恢复 所有中断点都当做曾经被中断过的状态恢复 但是不传任何恢复数据 这种适合什么场景 适合最简单的是否继续确认 用户点个同意按钮就够了 不需要给每个中断点传特定的数据 第二种是runner resume with params 精确目标恢复 这就很厉害了 他通过targets映射 把中断点的地址对应到恢复数据 我来举个例子 假设有三个工具并发执行 发邮件修改数据库 发通知 三个都需要人工审核 用户可以说邮件和通知同意数据库修改 我再看看resume with parents the targets就能精确表达这个意图 邮件和通知的地址匹配到了 就收到已确认的数据 数据库的地址没匹配到 那就让他继续等待 这就是精确控制的威力 影视恢复是全有或全无 精确恢复是你决定每个中断点的命运 还有一个很贴心的设计 with interrupt before nodes和with interrupt afternodes 这个是干什么的 它是透明中断 你看这个代码graph compile的时候传了两个参数 dangerous of节点之前插入中断 code exec节点之后插入中段 这意味着什么 节点本身不需要主动调用interrupt 执行引擎会自动在这些节点的前后插入中断点 这个设计的好处是什么 对于那些已经写好的组件 你不需要修改他们的代码 就能接入中断机制 只要在编译图的时候声明一下 哪些节点需要中断就行了 这才是框架该有的样子 让接入成本尽可能低 让框架的使用者少写代码 checkpoint结构里还有一个细节我要单独拿出来讲 因为它很重要 skip prehener这个字段解决的是什么问题 在ANO里 graph节点可以有PREHANDLER 就是在节点正式执行之前运行的钩子 通常用于前置处理 比如发送通知记录日志 检查权限 问题来了 从中断点恢复的时候 某些节点需要重新执行 因为它们的输出依赖中断点之后的数据 但如果重新执行这些节点 他们的PREHANDLER也会重新运行 这下麻烦了 可能重复发送通知邮件 可能重复检查权限 可能重复扣费 skip prehandler就是来解决这个问题的 恢复的时候 这个字段记录哪些节点在重新执行时 应该跳过PREHANDLER 这是幂等性控制 保证恢复执行不会重复产生副作用 你说这个细节重不重要 非常重要 如果一个节点的前置动作是通知审批人发邮件 恢复的时候不应该再发一次 但这个细节在简单的重新执行方案里 是不存在的 因为从零开始 副作用自然只执行一次 精确恢复 比重新执行复杂的多 就复杂在这些细节上简好 我们换个方向来看看deer flow line graph是怎么处理中断的 dear flow的中断是通过ask clarification工具实现的 agent执行到这个工具就调用command go to等于in的停止图执行 等用户输入 用户输入之后呢 带相同的thread id 重新调用lg graph 从最后一个checkpoint快照恢复 听起来跟i know差不多对吧 但这里有个本质区别 deer flow其实是重新执行 而不是精确恢复 agent会重新进入coordinate节点 重新读取之前所有的消息历史 然后根据新的用户输入做决策 它不是从中断点继续执行 而是从节点入口重新来一遍 一诺的语义是精确恢复到中断点 跳过重新决策 DEFLOW的寓意是从快照重新跑LLM重新决策 哪种更好 没有绝对答案 在跌flow的使用场景里 比如研究报告生成 重新执行的代价是可以接受的 因为消息历史完整 但对于长时间运行的企业及工作流 一诺的精确恢复能省不少 Token DFLOW还有两种不同的人机交互路径 第一种是ask clarification工具 显示调用agent呢 在执行过程中主动调用这个内置工具传入问题 文本工具触发command go to等于END停止图执行 这是主动的确定性的 开发者决定在哪里停下来问用户 第二种是clarification midware 影视触发 这个中间件是中间建立的最后一个 每次LOLM响应之后 他都会检查 如果LLM的输出是提问 也就是说没有工具调用 就是单纯的输出了一段问题 那它就自动触发等待用户输入的逻辑 这个厉害在哪 agent不需要显示调用工具就能自然的和用户交互 是LLM自己决定何时需要问用户的 两种路径的区别就在这里 显示调用是开发者主导的 影视触发是LLM主导的 前者确定性强 后者灵活性高 跟一诺比的话 DEFLOW的恢复是重新跑 LLM重新决策 一诺的回复是精确回复 跳过重新决策 对于长对话 一诺的token消耗更低 d flow的重新决策反而可能走出更好的路径 因为答案可能影响整体方向啊 好我们再来看goose Goost Cancellation token实现起来特别简单 就是这样三行代码 用户按下取消按钮 调用cancel agent 主循环检测到取消信号 返回错误 终止执行就这么简单 没有状态序列化 没有resume 取消就是终止 这在故事的场景里够用吗 我说基本够用 为什么 因为goose的定位是本地开发工具 用户就在屏幕前面 session一般不长 几分钟到几10分钟 遇到问题直接取消重来 用自然语言重新描述需求 这是合理的工作流 对个人开发者来说 重来的代价不高 ghost的session历史存在SQL lit里 下次启动可以继续之前的对话上下文 但注意这是继续对话 不是恢复执行状态 你可以在新会话里接着上次的话题聊 但agent不是从上次中断的那一步继续执行的 这两个有本质区别 别搞混了 搞那问题来了 goose为什么不做一个特风格的interrupt resume 我们来算算成本和收益 成本方面 goose的工具系统是基于MCP协议的 工具是外部进程 你很难序列化外部MCP工具的执行状态 MCP工具根本没有暴露我在哪一步的接口 就算你在goose内部做到了状态序列化 MCP层也会成为障碍 收益方面呢 goose的主要场景是帮我分析这段代码 这类任务如果中途取消了 重新发一遍就好了 精确恢复的收益不大 但引入这套机制的复杂度可不小 所以结论是什么 cancellation token是与goose场景匹配的设计决策 不是功能缺失 评估一个设计好不好 必须先看它解决的是什么问题 goose要解决的是用户想停就停 不是精确恢复到某一步 那cancellation token就是最好的选择 你不能因为它不能精确恢复 就说它不行 这就跟抱怨汽车不能飞一样 方向就错了 再来看hermes agent 这里要特别澄清一点 hermes agent的checkpoint跟ELO的checkpoint完全不是一回事 他这里说的是批处理任务的进度保存 不是agent执行状态的序列化 hammies agent有一个活动追踪机制 Tucked activity 在每次工具调用API 调用流式响应的时候 更新一下时间戳 Crown schedule 检测agent的空闲时间 超过配置阈值就判定为卡死 然后调用agent点interrupt强制中断 你看他解决的是agent还活着吗 这个问题不是如果停了 怎么继续 我打个比方 一个医院的每30秒检查病人心跳和外科手术 中断后精确恢复到哪条血管是完全不同的问题 前者是监控 后者是事物语义 所以HERMES和INO解决的不是同一个问题 不能说HERMES没有一诺的功能 就是落后 他们在解决不同的问题不 那我把这两个维度放在一起对比一下 你就清楚了 HERMES回答的问题是agent还活着吗 这是监控维度 语意是检测到了 现在来处理 ANNO回答的问题是如果停了 从哪里继续 这是恢复维度 语义是状态保留了 从这里继续 那一个完整的生产及系统应该是什么样子的 首先你需要一个心跳监控机制 直到agent什么时候停了 这个用HAMES的方案 然后你需要checkpoint机制 知道从哪个位置恢复 这个用INNO的方案 两者结合才是完整的解决方案 理想情况下 一个生产及系统应该是这样运作的 心跳监控检测到agent卡死了 然后checkpoint告诉你从哪个精确的位置恢复 A no 有恢复能力 但没有健康检测 HERMISS有健康检测 但没有精确回复 两者都有各自的短板 组合起来才是完美的 再来看oh my open agent 他用的是border jason 实现任务及回复 border jason记录的是当前正在执行的计划和会话id 当新绘画执行start work的时候 hook检查boulder jason 找到未完成的计划 注入继续执行第N个任务的提示 然后atlas就从中断处继续了 但这个是什么级别的恢复任务集 不是执行状态集 我来解释一下 如果task3在写了一半代码的时候 agent的崩溃了 下次恢复会从task3的开始重新执行 Atlas 不知道task3写到哪一步了 只知道他没有被标记为完成 那为什么这样设计就够了 因为代码文件是外部状态 在文件系统里 上次写的代码还在那里 下次重新写文件系统天然保证幂等性 重新写的代码和上次写的应该差不多 所以对于代码开发场景任务及重视基本够用 1no级别的精确恢复反而是大材小用大好 我再回答一个关键问题 为什么只有INO有composite interrupt 其他项目为什么不做 根本原因是并发模型和目标场景不同 dear flow是Python ascenio 多个节点可以异步并发 但人机交互通常还是串行的 一次澄清 回答一个问题 goose呢没有多个工具 同时等待人工授权这种需求 本地开发工具不需要这个superpowers的 并行task中断根本不传播给负agent i know 面向的场景是企业级工作流 一次请求里可能多个工具并发执行 发邮件要财务审批 修改数据库 要DBA审批 发通知 要运营审批 这三个授权是独立的 需要分别处理 composite interrupt就是来解决这个问题的 把三个并发审核合并为一个等待点 用户看到的是一张审核表 不是三个独立的弹窗 这个场景在个人开发工具里根本不存在 但在企业及工作流里是真实需求 设计决策反映了目标用户群体的差异 INO面向构建企业级AI应用的工程师 其他工具面向个人开发者或研究者 嗯好我们来做个横向对比 把这五个项目放在一起看看 从中断类型来看 一诺是框架 源于最底层 dear flow是工具调用触发的 goose是用户取消HERMES是超时检测 ommy open agent是任务完成状态跟踪 从恢复力度来看 INNO可以恢复到任意组件或工具级别 dear flow是节点集 但本质是重新执行 goose和HERMES不支持回复 oh my open agent是任务起 从多点并发中断来看 只有INO支持 其他项目都不支持 或者说不适用 从状态持久化来看 一诺用gob加checkpoint store 最灵活 叠r flow 用SQLIT存state快照 其他三个没有框架级的状态持久化 从恢复数据注入来看 yo可以通过resume with parents 精确注入每个中断点的数据 Dear flow 靠消息历史让LL重新决策 其他三个没有这个机制 从透明终端来看 只有INNO有with interrupt before notes 其他项目都没有 那实际工作中我们应该怎么选 我来给一个快速决策表 如果你的任务是短任务 5分钟以内就能跑完 失败了 直接重跑就行 那不需要检查点 用1no就是过度设计 如果你的任务是长任务30分钟以上 中间还有副作用 比如发邮件扣费 那用inno checkpoint加skip prehandler是必须的 防止重复执行导致副作用重复 如果需要人工审查批准某个步骤 用inno interrupt或者deer flow轮询 外部状态太复杂了 如果你是生产部署agent崩溃不可接受 那需要anal checkpoint加HERMS心跳监控 如果是研究实验场景 dear flow line graph checkpointer很合适 他的重新决策可能走出更好的路径 如果是批量密等任务 比如代码文件批量生成 用on my open agent的boulder jason模式就够了 文件系统天然保真密的 最后如果只是需要用户能取消当前操作 用goose cancellation token就行 注意i know interrupt的语义是暂停待续 不是取消 不要用错了 我再给大家一个决策流程图 方便在实际工作中做判断 第一步问自己任务失败后能直接重跑吗 如果是幂等的 没有明显副作用 那就用文件或数据库标记已完成状态 这是boulder jason模式 第二步 任务有人工审批或审查节点吗 如果有 必须用inno interrupt做精确恢复 必须防止副作用重复 简单重跑会重复发邮件 重复扣费 第三步 任务运行时间超过30分钟 或者生产环境不允许重跑 如果是 那需要anal checkpoint做节点及序列化 实现精确回复 第四步 研究或实验场景需要随时终止 并从某个点继续 如果是dear flow line graph checkpointer更合适 他的重新决策可能带来更好的结果 如果以上都不是用户 只是需要立即取消 不需要回复 那就用cancellation token 按照这个流程走 你基本上不会选错方案 嗯最后我给大家三点跨项目借鉴的建议 第一心跳监控和checkpoint是互补的 不是替代关系 HERMES的心跳监控 回答agent还活着吗 a NO的checkpoint回答 如果停了 从哪里继续一个完整的生产机系统 两者都需要先检测到agent的 停了再用checkpoint决定从哪里恢复 第二任何实现精确恢复的系统 都必须处理副作用幂等性 你必须区分已执行过副作用的节点 和还没执行的节点 innode skip prehandler是这个问题的框架及解法 如果你自己在实现类似系统 也需要这个机制 否则恢复的时候可能重复发邮件 重复扣费 第三对是否需要checkpoint的判断标准是什么 我给一个公式 任务执行时间乘以失败概率乘以重跑代价 这个值如果大于实现checkpoint的成本 才值得引入 对于短任务和密等任务 border jason模式已经足够了 不需要过度设计 这好我们来做个总结 第一个核心洞察 一诺的框架及中断是真正的差异化特性 不是因为它功能多 而是因为它解决的问题级别不同 其他项目的中断要么是应用层的工作流控制 要么是用户体验的取消按钮 要么是健康监控 I know 是唯一在框架层面提供执行状态的精确 序列化和恢复的系统 第二个核心洞察 这个差异反映了目标用户的不同 一诺面向构建企业级AI应用的工程师 他们需要处理并发审核 长时间中断后恢复这类生产及问题 Dear flow Goose Superpowers 面向个人开发者或研究者 他们的工作流更短 更互动 重新执行 在大多数情况下是可接受的 第三个核心洞察 skippy handler是框架及精确恢复必须解决的细节 精确恢复不只是从哪里继续 还必须保证已完成的副作用不被重复执行 这个问题在简单的重新执行方案里不存在 但在精确恢复方案里必须显示处理 记住这三个洞察 你就能理解为什么这些项目做不同的选择 以及在什么场景下应该选什么方案

## 标签
[]
