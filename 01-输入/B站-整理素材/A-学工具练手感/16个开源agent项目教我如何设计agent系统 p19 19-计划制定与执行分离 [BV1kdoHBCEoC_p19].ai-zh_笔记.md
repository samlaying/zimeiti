---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p19 19-计划制定与执行分离 [BV1kdoHBCEoC_p19].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p19 19-计划制定与执行分离

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

大家好 今天我们来聊一个 每个做agent的人都会遇到的问题 agent呢在执行任务之前 应该先完整的想好整个计划 还是像人做事一样边做边想 随机应变 这个问题听起来像是风格偏好 但实际上它会直接影响三个关键维度 任务失败时从哪里恢复 人类能在哪个节点介入审查 以及任务能不能跨会化持续推进 今天我们通过五个真实项目 Superpowers Dear flow i know all my open agent和goose 来看这个问题的五种答案 这五种答案共同构成了一个完整的计划 执行分离光谱从最弱到最强 我先给大家一张全景图 最左边是完全融合 LLM在每一步都在实时决策下一步做什么 没有任何显示的计划阶段 ghost的默认模式就是这样 react循环推理行动 推理行动往右一点是软分离 l ll m内部会有计划 但这个计划不是一个外部可见的文档 它存在于LLM的推理链里 执行时可以随时改变 DEFLOW基本在这个位置 再往右是严格分离 计划 是一个独立的可审查的文档 计划写完后有人工审查窗口 审查通过后才开始执行 super powers是这个方向的代表 最右边是静态计划 计划在执行之前就完全确定 执行期间不能修改 goose recipe和anna plane execute 在这个位置 今天我们逐一深入每个档位 入这张表格 总结了这四种模式的差异 最左边是完全融合 计划 完全不可见 执行控制力最低 优势是适合快速探索 往右是软分离计划部分 可见DEFLOW 通过TODOMADOWR把计划外显化了一些 再往右是静态计划 计划完全可见 YAMMO可读 但执行时不可修改 适合重复性自动化 最右边是严格分离 计划是一个独立的markdown文件 human可以在执行前审查质量最高 但最慢这四个档位没有好坏之分 关键是匹配任务特征 先来看光谱右侧的superpowers 他在计划执行分离上做的最精细 设计原则也最值得深挖 第一个原则叫零上下文读者原则 writing plants技能里有一句话 为一个零上下文 品位可疑的熟练开发者创建计划 为什么是零上下文 因为计划的执行者通常是另一个AI实例 在另一个会话里 对计划编写时的上下文一无所知 你在写计划时觉得这里显然应该这样做 但执行者看不到显然背后的理由 所以计划必须自给自足 不依赖任何隐含的上下文 加第二个原则 占位符是毒药 什么是占位符 TBD是占位符 Add error handling 也是占位符 Similar to task 二也是占位符 这些词的共同特征是 他们把判断责任留给了执行者 而执行者没有足够的上下文来做出正确判断 占位符会导致一个代加列计划有模糊词 执行者做了合理但错误的选择 人类审查发现方向偏差 需要返工 返工的时间远超当初把计划写清楚所需的时间 所以superpowers要求 即使是重复任务也要完整写出来 不留任何模糊地带 第三个原则 二到5分钟力度为什么是二到5分钟 太粗的话 比如写实现用户认证系统这么一句话 执行者根本不知道第一步做什么 会被迫即兴发挥 太细的话 比如每行代码一步计划时间会浪费 计划文件变得不可读 二到5分钟是甜点区 每部有清晰的完成标准 可追踪 可回滚 失败点定位准确 这也意味着每步完成后可以get commit 失败可以回滚到确定状态 任务总时间可预估 计划写好之后 superpowers提供了两种执行路径 第一种叫executing plans 跨会化执行会话A里写好计划 生成plan m d 然后关闭会话A会话B打开AI 对绘画A的一切一无所知 只有plant md文件 他读取计划 先做批判性审查 在开始执行前把所有关切列出来 然后逐步执行 这就是零上下文读者原则的实际运用场景 计划必须完全自给自足 因为绘画B不能向绘画A求助 遇到阻塞时 superpowers的原则是停止并报告 不猜测并继续猜测 并继续会造成偏差 累积越到后面越难修复 第二种叫sub bajin driven development 同会话执行在同一个会话里 Writing plans 写完计划后 sub agent driven development接管 关键区别是 每个任务由一个新建的独立sub agent执行 而不是主会话直接执行 这防止了什么 防止task1的执行细节污染 task2的推理 每个SUBSPENT有干净的上下文 每个任务完成后还有双轮审查规范 合规审查对照原始规格 然后代码质量审查两轮都通过才标记任务完成 这比executing plans质量更高 但也更复杂更慢 选哪种计划已稳定 想稍后执行 用executing plans 计划刚写好 立即执行 且质量要求高 用sub agent driven development 这一页讲一个有意思的工程决策 V5.06版本之前 super powers的writing plans在计划写完后 会启动一个子智能体来审查计划质量 这个子智能体读取计划 检查覆盖度 一致性 占位符 然后汇报结果 V5.06把这个步骤替换成了30秒内 连自我审查计划写完后 AI自己检查四个维度规格覆盖占位符 扫描类型 一致性 遗漏覆盖 release notes里记录了测量结果 质量分数完全相同 但每次节省了约25分钟 这25分钟花在哪里了 花在了子智能体的启动上 下文传递和汇报流程上 这些开销本身不产生额外的质量提升 这个案例体现了super powers的工程哲学 测量先于假设子智能体审查应该更好 是一个直觉 但直觉需要测量来验证 测量说它不更好 那就换掉这个思路可以推广到任何地方 你觉得某个设计更好 先测量再决定 不要凭直觉 坚持一个实际上没有带来额外价值的设计 占位符检查和覆盖度验证 不需要独立的智能体计划者 自己就能做好 成本最低 现在来看光谱最右端静态计划 先说goose recipe recipe是goose的声明式工作流系统 一个yo文件定义了任务的instructions 触发prompt输入参数 支持六种类型输出的jason schema 最大重试次数以及验证prompt执行时 agent读取yao and instructions工作 最终必须调用final output two输出 符合jason schema的结果 最重要的特征 这个计划在执行前就完全确定了 执行期间agent只能在计划框架内工作 不能修改计划本身 这是一种完全静态的计划模式 这里要区分一下 recipe和writing plans的本质区别 recipe是人工预先编写的 静态的适合重复性任务 riding plans是AI动态生成的 每次任务不同 recipe在执行时不可变 只能按Emo走 writing plans在执行中可以调整 recipe支持参数化 有六种输入类型 writing plants每次都是定制的 没有参数化 recipe有结构化的 jason schema验证writing plans没有 这是一个根本性的选择 您想固化一个已知的最佳流程 用recipe 你想分解一个复杂的一次性任务 用writing plans 再来看一诺的plan execute EO在addict prebuilt plan execute 提供了一个框架级别的plan execute模式 流程是这样的 input进入planner 由chat model生成plan plan 交给executor执行第一步 步骤完成后 REPPLANNER评估是否需要重新规划 如果还有步骤 executor继续执行 下一步全部完成后 输出结果核心是plan这个接口 他定义了first step方法 返回第一个代执行步骤 还实现了JASON的序列化和反序列化 默认实现只有steps列表 但接口设计允许自定义更复杂的plan 比如包含依赖关系的deck 这张表对比了plan execute和deep agent的差异 deep agent是agent自主决定 下一步规划是影视的推理 过程不透明 可追踪性低 但灵活性高 plan execute是框架 按plan顺序控制 规划是显示的 plan是可检视的步骤列表 可追踪性高 灵活性低 偏离需要显示REPLAN 这个对比揭示了一个取舍 你想让agent自由探索就选deep agent 你想让执行过程可审计 可追踪就选plan execute REPLANNER在每部执行后评估是否需要重新规划 提供了一定的适应性 不像纯静态计划那样完全死板完 Oh my open agent SYPHUS是一个有意思的混合案例 它有显示的阶段结构 但计划是动态的 嵌入在调度逻辑里的 不是一个独立的文档 phase0是关键 在做任何事之前 SISYPHUS必须先说出来他理解的用户意图 这个说出来有三个功能 防止误执行 让用户能看到AI的推理链 如果错了 可以立刻纠正 Phase4 验证失败时 CSFS回到face haze2 重新规划整体 而不是局部重试失败的那个步骤 为什么 因为局部失败往往意味着计划整体有问题 不是执行有问题 在原计划框架内重试 大概率得到相同的失败 CSFS的命名来自希腊神话西西弗斯推石头上山 石头滚落再推 永不停止 这不是比喻 是设计原则 CSFS不停的分解委托验证 直到所有子任务都是等 如果face4验证失败 返回face2重新规划整体 而不是局部重试失败的那个步骤对比 I know plan execute plan execute是REPLANNER做局部调整 SYFS是整体重置 Plan execute 用单一execute agent SYFS用专业化词agent 比如HAVISTERS执行代码 plan execute的计划是显示步骤序列 THEIFS的计划是影视的 通过意图判断来实现 还有一个关键点 SYPHUS尽量少干活 多委托编排工作 由最贵的模型做执行工作 由便宜的子agent做总成本远低于一个模型 做全部不 Dear flow 用line graph构建有向图图的结构是静态的 哪些节点 哪些边 但执行路径由LLM 在运行时决定走哪条条件边 这是软分离的典型形态 框架层面有结构 但计划层面是影视的 存在于LLM内部 dear flow有一个is plan mode配置项 当它为true时 total middle ware激活agent 会用right todos工具把待办任务写下来 这把计划从LLM内部外显化了一些 但仍然是执行中动态产生的 不是执行前完整制定的 land graph带来了软分离框架之外的额外能力 CHECKPOINTER在每个节点执行后保存状态 支持中断后从断点恢复图结构可以可视化 方便调试和理解执行流程条件 边支持并发节点执行 这些都是直接写react循环得不到的 这也解释了为什么DEFLOW选择lg graph 而不是一个简单的while循环 对比deer flow和super powers deer flow的计划更像LM思考过程的记录 更灵活 执行中可以随时调整 super powers的计划是独立于执行的 前置弓箭更可靠 跨会化也能使用 选哪个 取决于你的任务是否可预期 执行 失败的代价有多大 接下来我们总结五条 可以跨项目借鉴的设计建议 第一条 用零上下文读者原则评估计划质量 superpowers的核心原则 适用于任何有显示计划的系统 写完计划后问自己 如果把计划发给一个没看过对话记录的工程师 他能执行吗 如果不能 计划里有影视依赖 需要补充占位服务 检查清单搜索 Tbd toto similar to add error Handling Follow the existing pattern 占位符是计划质量的核心敌人 必须在执行前全部消灭免 第二条 计划阶段比你预期的更值得投入 super powers的无占位符规则揭示了一个普遍规律 计划阶段节省的时间 比如写个TODO会在执行阶段以更高代价偿还 戴教练是这样的 计划 有占位符 执行者在歧义处做了合理但错误的选择 人类审查发现方向偏差 需要返工 返工的时间远超当初把计划写清楚所需的时间 正确做法是 即使是重复任务 也要完整写出代码 示例中的这个例子 task3写的像task ca2那样详细 看起来费时 但实际上避免了后续的反攻 总时间是赚的 第三条 根据任务规模动态选择分离程度 任务在30分钟内完成 失败代价低 不需要显示计划 用goose默认或DFLOW就行 任务需要多步骤 质量要求高 用同绘画严格分离 Superpowers Sub agent Driven development 任务超过二小时或需要人工审查批准 用跨会话严格分离 Super powers executing plans 任务是重复性的 需要多次运行 用静态计划 goose recipe或I/O plan execute 四个档次按需选择 不要一律用最复杂的方案显示计划本身有token 成本对简单任务是净负担 复杂任务如果没有计划agent的 容易在执行到第五步时 忘记最初的整体目标 做出局部正确但全局错误的决策 第四条 遇到阻塞停止并报告 而不是猜测并继续 这是superpowers的原则 适用于所有agent系统猜测并继续的 问题在于 agent会基于错误假设建立一系列后续行为 最终偏差比原始阻塞更难修复 测量证明 基于错误假设建立的后续行为链 纠错成本是原始阻塞的3~10倍 正确做法是遇到不确定如何继续 优先选择暂停和上身 super powers的executing plans模式 就是这样 遇到阻塞停止并报告 而不是猜测并继续 这个原则在多个项目中独立验证 值得作为所有agent的系统的默认行为 第五条用结构化输出约束完成的定义 goose recipe的final output two机制 解决了agent怎么知道自己完成了的问题 传统做法是LLM自己判断差不多了 但这不可靠 final output to的做法是把完成定义为可验证状态 通过jason schema来约束agent必须调用final output to输出 符合schema的结果 否则任务不算完成 这个机制可以移植到任何需要可靠 终止条件的场景 显示的输出要求 把完成变成了一个可验证状态 而不是依赖LLM的主观判断 第六条 用REPLANNER解耦计划 修订和执行innolan execute的REPLANNER模式 提供了一个优雅的中间方案 计划不是一次性的 这避免了静态计划遇到意外无法处理的问题 但修订有明确的出发点 每步完成后评估是否需要重新规划 这避免了随时修改导致执行混乱的问题 在设计agent的系统时 可以把每完成一步评估是否需要重新规划 作为标准节点加入工作流 这比完全固定计划灵活 又比完全动态规划可控 这张表汇总了七个项目在八个维度上的对比 计划显示性从无到显示 dear flow是影视通过工具记录 superpowers是显示markdown文档 CSERS是意图 口头话计划产生时机 从无到每条消息前 goose recipe是人工预先编写 i know plan execute是执行开始时计划可修改时间 从随时到不可修改 super powers的严格分离模式 只能在执行前人工审查时修改 这些维度的差异决定了每个项目适合的场景 再看对比表的第二部分 跨会话能力上只有super powers executing plans和goose recipe原生支持跨绘画 他们都依赖外部文件作为载体 人工审查窗口上 superpowers executing plans有执行前审查 goose recipe有编辑YAO时的审查 SISYPHUS在evaluation意图时有审查机会 阻塞处理上各有各的方式 goose默认是LLM自行处理 dear flow是ask clarification 向用户提问 superpowers是停止并报告 goose recipe是retry configure 重视yo是REPLANNER 重规划 SYPHUS是FACSIR整体重置最适用场景 这列直接给出了结论 快速探索 用goose默认复杂单次任务 用dear flow 需人工审批的任务 用executing plans高质量开发任务 用sub agent driven development 重复性自动化 用recipe可审计的任务 用plan execute 企业级编排 用SISYPHUS 用决策流程图来总结选型逻辑 第一个问题 任务是否简单 30分钟内单一目标步骤已知 如果是无需显示计划agent直接执行 第二个问题 任务是否涉及高风险操作 删除文件 修改生产数据库 发送外部通知 如果是用super powers writing plans 先写计划 人工确认后再执行高风险操作 需要人工把关 第三个问题 任务是否跨越多个会话超过二小时 如果是用superpowers executing plans 外部计划文件支持跨会话 回复第四个问题 任务中途可能需要调整方向 是探索性的 如果是用inner plan Execute replanner 每部评估重规划 如果都不是用superpowers subagent driven development 同会话执行质量优先 最后核心结论 计划与执行的分离程度 本质上是在灵活性和可靠性之间做权衡 完全融合 最灵活 但可靠性最低 静态计划最可靠 但灵活性最低 软分离和严格分离 提供了中间选项 没有通用最优解 正确的选择取决于任务的可预期程度 质量要求 以及是否需要跨绘画持续推进 真正成熟的A证系统 比如CSFS会根据任务类型动态选择分离程度 这本身就是更高层次的原计划能力 今天的内容就到这里 谢谢大家 最后附上每个项目的源码位置 方便大家深入研究 superpowers的三个技能 分别对应writing plans executing plans和sub agent driven development3个目录 goose recipe的核心代码 在recipe目录下 I know plan execute that add cake Pre built plan execute目录下 Sisyphus that Oh my open agent The agents mullucia Dear flow the todo middle weres than middle wears mullua 这些源码是理解今天讲的设计理念的最佳途径

## 标签
[]
