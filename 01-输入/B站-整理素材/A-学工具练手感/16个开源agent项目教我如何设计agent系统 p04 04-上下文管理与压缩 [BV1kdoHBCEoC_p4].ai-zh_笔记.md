---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p04 04-上下文管理与压缩 [BV1kdoHBCEoC_p4].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p04 04-上下文管理与压缩

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

好 今天我们来聊一个 所有生产阶AI agent都必须面对的问题 上下文窗口限制 你可以把上下文窗口理解成AI的短期记忆 它能同时记住多少内容是有物理上限的 Cloud sonnet 四是20万 token g p t4O是12.8万 GEMINI1M听起来很大 但也不是无限的 一个长时间运行的agent 要读代码 执行工具对话交互很快就会撞上这个天花板 撞上去会发生什么 两个结果 要么API直接报错 任务崩溃 要么模型开始失忆 早期消息被静默截断 对用户来说 这两种都是灾难 几10分钟的工作前功尽弃 所以所有严肃的agent的框架 都必须回答同一个问题 今天我们来看四个顶级项目的答案 他们的差异比表面上看起来要深得多 这四个项目分别是goose Hermes agent Open code 还有oh my open agent 他们的设计哲学差异非常大 goose偏保守 80%才触发用主模型做摘要 最多压两次 HERMES金的多 50%就触发用小模型做摘要 可以无限压缩 Open code 不按百分比来 他是预留20000token给输出 剩下多少就是可用空间 而且压缩有一个专门的 完全没工具权限的agent执行 oh my open agent又是一套思路 三级警戒90%才真正压缩 但他有个独门绝技 task list状态注入压缩后 还记得做到哪里了 这些差异背后各有各的工程逻辑 没有绝对的好坏 只有适合的场景 接下来我们逐个深入对 先来看goose 他的触发阈值是80% 但触发方式比数字本身要精巧的多 当token数达到80%时 goose不是立刻压缩 他先激活一个叫tuo cut off的机制 停止向对话添加新的工具调用结果 但让当前轮次的LLM正常完成它的响应 等这一轮彻底结束后才真正触发压缩 这个设计解决了一个很实际的问题 如果在LLM正在生成响应的中途阶段 对话历史里会出现一个悬空的to request 有调用请求 没有结果 这种不完整状态会让LLM困惑 Tool cut cut off 保证了每次压缩前对话历时处于干净状态 LM刚完成一次完整的响应工具 结果循环 这是一个看起来简单但非常关键的工程细节 压缩完成后 goose会发送history replaced事件通知 前端原始消息保留在sq lit 用户可以随时回溯查看历史 goose还限制最多压缩二次 两次后还不够 说明任务本身需要拆分了 不应该无限压缩下去 再来看ghost的token技术方案 他用了两层机制 第一层是API返回的精确技术 每次LLM调用后 API告诉他实际用了多少 Input token Output token 这是事后记录 第二层是事前估算 用来决定什么时候该触发压缩 对普通文本用字节数除以四 但对代码内容 goose使用TRACITOR做语法感知的token技术 为什么这个区别很重要 因为代码里的token密度远高于普通文本关键字 function标识符 my function运算符等于大于括号 每个都是独立的token 简单的字节除以四 会明显低估代码的token量 低估了会怎样 agent会在应该压缩的时刻没有触发压缩 下一步API调用直接报上下文超限错误 对于一个主要处理代码的agent 这个误差是实质性的 HERMES就没有这个精度 他用的是estimate messages tokens rough 名字本身就承认是粗估算 对于代码密集型任务 这是一个值得注意的差距 好来看HERMES 他的阈值是50% 比goose的80%机警的多 token使用率刚过一半就触发压缩 为什么敢这么激进 因为HERMES用的是auxiliary client 一个专门负责幕后工作的小模型来执行 摘要深沉 这个auxiliary client能做什么 图像分析 任务分类 Away summary 还有压缩摘要 关键是 它的成本只有旗舰模型的1/50 到一1% 把这些不直接面向用户的工作都路由到小模型 是HERMES成本控制的核心策略 所以当摘要生成本身很便宜的时候 你就可以更频繁的压缩 不用心疼 成本50%的基金阈值 在这个成本模型下是合理的 HERMES还没有max compassion attempts限制 理论上可以无限压缩 对于几个小时的长任务 这是必要的 不能让压缩次数上限成为任务崩溃的原因 另外HERMES有个很聪明的运行时发现机制 对未知模型它会通过查询或者发探针消息 自动找出上下文限制 不用在代码里硬编码每个模型的参数 HERMES有个goose完全没有的特性 叫SUBDIRECTORY Hint tracker 我们来想一个场景 agent正在sa models目录下做重构 读了十几个文件 改了几处代码 token快不够了 触发压缩 压缩完成后 摘要里写 我们做了一些重构 但是在哪个目录不知道了 下次agent要新建一个文件 他可能跑到根目录去 或者找不到之前修改的文件 SUBDIRECTORYHTRACKER就是来解决这个问题的 它会追踪工具结果里出现的文件路径 验证这些路径是实际存在的目录 然后在压缩时 把这些最近涉及的目录注入到摘要前面 就几十个token的成本 但换来的结果是 agent t压缩后仍然知道自己在哪里工作 goose做大量文件系统操作 他需不需要这个答案是肯定的 这是HERMES对goose最直接可借鉴的设计之一 goose目前的压缩摘要里完全没有工作目录信息 这是一个可以改进的地方 来看open code 他的触发逻辑既不是80% 也不是50% 而是达到上限减去预留缓冲区预留多少 默认20000token 这20000token是给LLM输出预留的空间 为什么要有这个预留逻辑 是这样的 如果等到token已经用完了才触发压缩 LLM在压缩过程中 自身也没有足够的输出空间了 他要生成摘要 但没有地方写 这就会陷入一个僵局 要压缩 但连摘要都审不了 所以open code的策略是始终留一块buffer给压缩过程 本身用这个设计很务实 另外open coat区分了四类token Input Output Cash Read cash right 这对支持prompt cash的provider 比如ANTHROPIC很重要 cash store的token也占用上下文空间 但计费不同 分开统计保证了上下文使用量计算的准确性 Open code 还有另一个设计亮点 独立的compassion agent 注意看这个配置 这是一个hidden的agent 没有任何工具权限 deny意味着他连文件系统访问都不允许 压缩过程中 这个agent只做一件事 读对话 生成摘要 写摘要纯粹是文本操作 为什么要这么严格 因为它防止了一个边缘情况 LLM在总结对话的过程中意外执行了操作 比如他说让我先把这个文件删了再总结 那不就乱套了吗 这是一个安全性的基本实践 在压缩这个特殊场景下 执行权限是多余的 应该默认关闭 这个原则应该被所有实现了 专用压缩agent的框架遵守 接下来是oh my open agent 它的触发逻辑是三级警戒 70%以下 正常运行 70%到90%显示警告 90%以上才真正压缩 超过95%就触发session recovery 90%阈值比GOOOSE的80%更晚触发 这看起来更保守 但all my open agent有自己的兜底机制 Session recovery 当API直接返回上下文 超出错误时 它会创建新绘画 从保存的状态摘要恢复继续任务 这个机制 让超过500000token的超长任务成为可能 但ommy open agent最核心的创新不是阈值本身 而是压缩后的恢复方式 这是今天最值得关注的创新之一 普通的上下文压缩只保留历史摘要 已经做了什么 但对于一个多步骤任务的agent 更重要的是还剩什么没做 All my open agents 在压缩前主动保存task list的未完成状态 压缩后注入到新对话 开头完成了什么 待完成什么 从哪里继续 没有这个机制会发生什么 agent压缩后知道自己在做认证相关的工作 但不记得做到哪一步了 他需要重新探索上下文 可能重复已经做过的工作 或者漏掉某些步骤 有了task list的注入 压缩变成了一个无缝的状态延续 而不是一个记忆断层 这个设计成本极低 就几十个token 但根本性的解决了agent 压缩后不知道做到哪里了的问题 我认为这应该是所有多步骤agent的标配配好 这是今天最重要的对比问题之一 goose的80%逻辑是 上下文到80%时 模型响应质量仍然良好 没必要过早压缩 过早压缩会丢失有用的对话历史 而且每次压缩要调用主模型 成本不低 HERMES的50%逻辑是auxiliary client 让摘要成本降到1/50到一1% 可以频繁压缩 早期对话在长时间任务中很快变得不相关 早点压缩反而保持上下文新鲜 谁的更合理 答案是取决于场景 短任务不到30分钟 聚焦的代码修改用80%更合理 减少不必要的压缩 长任务好几个小时 宽泛的探索性工作50%可能更好 因为早期对话两小时后基本没参考价值了 成本敏感场景 HERMES的小模型摘要策略 使得50%阈值的总成本 可能反而低于goose的80%阈值 这不是非此即彼的选择 而是一个需要根据实际场景权衡的参数 goose的tual cut off保证了一件事 压缩时机的干净性 如果在工具调用链中途触发压缩 会产生悬空的two request LLM调用了一个工具 但工具结果还没在对话里 压缩后LLM会对这个不完整状态感到困惑 HERMES为什么没有这个机制 可能的原因有三个 第一Python异步执行模型和rust的流式模型不一样 实现cut off机制更复杂 第2HERMES的50%阈值意味着更早触发 通常不会到达当前轮 正在执行复杂工具链的状态 第3HERMISS的设计哲学更简单 没有特别关注边缘情况的健壮性 实际影响是什么 在高强度工具使用场景下 比如一个任务同时调用多个工具 HERMES有一定概率在工具链中途压缩 导致轻微的上下文不一致 大多数时候LLM能自我恢复 但这是一个潜在的质量风险 改进建议HERMES可以保持50%阈值 但增加一个简单的guard 当检测到最后一条消息是to request 没有对应two response时 延迟压缩到当前工具调用循环结束 再来一个核心分歧 压缩次数要不要设上限 goose设了上限 最多两次 两次后还不够 说明任务本身需要拆分了 不应该无限压缩下去 背后的逻辑是每次压缩必然丢失信息 无限压缩在理论上保证了任务不崩溃 但越来越稀薄的上下文 可能导致LLM在不知道完整背景的情况下 做出错误决策 HERMES和ommy open agent没有这个上限 因为他们有不同的兜底机制 on my open agent有task list状态注入压缩后 agent知道做到哪里了 还有session recovery API 报错时创建新会话 回复工程结论是max compassion attempts 应该和状态保存机制配合设计 有完善状态保存的系统可以无限压缩 没有状态保存的系统应该设置上限 防止过度信息丢失导致的质量问题 goose设上限是对的 因为他的压缩不保留任务状态 但他应该考虑借鉴task list的注入 有了这个机制 次数限制就不是那么必要了 来看横向对比总表 我把核心维度都列出来了 触发阈值 goose 80% hermes 50% open code用上限减buffer oh my open agent 90% 触发方式 goose有tool call cut off 保证干净时机 HERMES是每条消息后检查 open code是is overflow oh my open agent是context monitor 摘要执行者 goose用主模型 HERMES用auxiliary client小模型 open code用专用agent oh my open agent用compact命令最大压缩次数 goose限二次 其他三个无线 且ommy open agent有session recovery状态保留 goose保留最近N条 HERMES保留最近十条 其他两个用摘要替换 但all my open agent有task list 注入目录信息 只有HERMES有SUBDIRECTORY Hint tracker 其他都没有token精确性 goose有tracy or hermes 粗估算open code4类精确统计 历史可回溯 只有goose的SQL light保留了原始消息 其他都是内存替换 这张表揭示了一个有趣的事实 没有任何一个项目在所有维度上都最优 每个都有自己的取舍和特长 好综合四个项目的经验 这五个设计是最值得被借鉴的 第一too co cut off来自goose 保证压缩时机的干净性 防止工具调用链中途截断导致的上下文不一致 任何有工具调用的agent都应该考虑实现这个 第2task list状态注入 来自oh my open agent 压缩前保存待办状态 压缩后注入已完成 待完成 从哪里继续 成本极低 就几十个token 但根本性解决了agent 压缩后不知道做到哪里的问题 第3auxiliary client做摘要来自HERMES 用小模型执行摘要生成成本降低50~100倍 这使得更激进的压缩策略在成本上变得可行 同时改善了整体响应时间 第4subdirectory hint tracker同样来自HERMES 压缩时注入当前工作目录信息 对代码agent来说 我们在哪个目录是最重要的工作上下文之一 几10token的成本绝对值得 第五禁止工具权限的压缩agent来自open code 压缩过程 只做文本生成 不应该有任何执行权限 这是安全性的基本实践 没有一个项目同时具备这五个特性 接下来我们进入实战环节 场景选型决策框架 这一届回答在具体场景下选哪个方案 为什么 先看场景A对话为主的短任务 这类场景的特征是任务时长不到30分钟 主要是聊天问答工具调用少 主要担心的是不必要的压缩 破坏对话质量 而不是上下文爆炸 推荐方案 goose的80%阈值加to call cut off 原因很简单 80%时模型响应质量依然良好 没必要过早压缩 对于短任务 可能整个会话都不需要压缩一次 To call cut off 保证压缩时机的干净性 max2次的保守上限适合短任务 如果选错了方案会怎样 用HERMES的50%阈值处理短任务 回话刚进行到一半就触发压缩 用户最近的十条对话被压缩成摘要 AI回复的上下文质量下降 用户感知到回答不如之前连贯 对于短任务 这完全是不必要的损耗 场景B代码密集型场任务 这类任务涉及大量代码阅读和修改 上下文里有几千行代码 几十个工具调用结果 代码的token密度远高于普通文本 需要精确的token技术来决定何时压缩 任务可能持续1~3小时 推荐方案是跨项目组合goose的tree sitter精确技术 加上HERMES的auxiliary client摘要 再加上HERMES的subdirectory hint tracker 为什么trace ser这么重要 因为代码中的关键字缩进 括号各自占token通用字节除以四的公式误差 可以达到10%到30% 在接近上线时会触发API的上下文溢出错误 而不是预防性压缩 Subdirectory hint tracker 防止忘记在哪个目录代码任务压缩后 最常见的失败模式是AI在错误位置创建文件 因为他忘记了当前工作目录 accelerate client控制摘要成本 代码密集任务压缩频率可能更高 用小模型做摘要成本可控 如果选错了方案 用HERMES的粗估算处理代码 密集型上下文API直接返回context length exceeded 错误任务中断 agent状态丢失 用户需要重新开始 这在生产环境是不可接受的 场景C多轮工具调用列 这是agent执行多步骤自动化的典型场景 一次任务执行20~50个工具调用 每个工具有to request和two response 配对 上下文增长很快 但必须保证每次压缩时工具调用处于完整状态 推荐方案 Goose the tocall cut off 我要强调一下 这个场景里 to call cut off是必须项 不是可选项 悬空to request对LLM是一种异常状态 主流LLM在看到不配对的工具调用时 行为不可预测 可能重复执行已完成的操作 对agent的自动化任务的重要性远超对普通对话 普通对话里的不完整轮次 最多让用户困惑 但agent的不完整 工具调用可能导致重复写文件 重复执行命令的副作用 如果选了错误方案 HERMES没有出口 cut off直接在工具调用链中途 基于50%阈值触发压缩 摘要中可能包含不完整的工具调用序列 压缩后 agent可能重新执行已经完成的工具调用 产生重复副作用 不 场景D长时间运行任务 这类任务可能持续2~8个小时 涉及多个阶段 早期的对话内容 初始背景介绍方向探索在后期已经不重要 需要接近压缩 保持上下文新鲜 同时不能因为压缩丢失任务状态 推荐方案是跨项目组合HERMES的50%阈值 加上HERMES的auxiliary client 再加上all my open agent的task list状态注入 为什么50%激进阈值在这里合理 因为多小时任务中 二小时前的对话背景 在四小时后基本没有参考价值 早压缩不是损失 而是清理auxiliary client是频繁压缩 成本可控 HERMES用小模型做摘要 50%阈值下的总摘要 成本可能低于goose的80%阈值 后者用主模型摘要 Oh my open agent The task list 注入是防止agent在长任务里迷失的关键机制 压缩前保存未完成任务列表 压缩后注入已完成X待完成Y继续从C开始 如果选了错误方案 用goose的max compaction attempts等于二 限制处理多小时任务 压缩两次后 任务继续增长 第三次超限时 goose认为这是任务设计问题并停止 但对于合理的长时间任务 这个停止是错误的 几小时的工作就付之一炬了 好总结一下决策流程 第一步 任务时长预计多久 不到30分钟 对话为主的 用goose的80%阈值 1~3小时 代码密集的 用ghost的trace ser精确计数超过三小时 多阶段任务的用HERMES的50%激进压缩 第二步任务是否包含大量工具调用超过十次 如果是必须有to call cut off 保证压缩时机干净 不是的话 标准阈值触发就可以了 第三步任务是否有明确的多步骤 Total list 如果有引入all my open agent的task list状态注入 没有的话 标准摘要压缩就够 第四步 压缩成本是否需要控制 对于高平常任务 用auxiliary client小模型摘要 成本降低50~100倍 对于偶发短任务主模型直接摘要也无所谓 这是一个渐进式的决策过程 根据实际场景逐步添加必要的机制 最后来看跨项目互借的具体建议 第一个HERMES引入to call cut off 具体实现是在hermes agent的循环里 追踪当前是否有未完成的工具调用 对有to request 但没有to response触发阈值时 如果处于工具调用中途推迟 压缩到当前轮次完成后再执行改动 集中在maybe compress context 方法加一个is in to call sequence 检查大约30行代码 收益是消除工具调用链中途压缩 产生的悬空状态问题 对agent的自动化任务的稳定性有实质提升 第二个goose引入task list状态 注入goose 目前的压缩摘要不包含任务状态 压缩后agent知道历史上做了什么 不知道接下来还要做什么 改造 方向是在check compassion needed里生成摘要前 调用一次LLM提取当前上下文里的total list 附加到摘要末尾 格式参考o my open agent的已完成 待完成 继续从模板改动范围 在agent的压缩逻辑部分 不影响其他模块 第三个open code引入工作目录信息 保留open code的压缩agent 当前生成的摘要不包含工作目录信息 压缩后agent可能在错误目录操作文件 可以借鉴HERMES的subdirectory hint tracker思路 在prompt模板里加一行指令 让它附加上当前工作目录列表 这是一个纯提示词 修改不需要改代码成本极低 但对代码任务的压缩后 稳定性有明显改善 好总结一下 触发层常规压缩 用goose的80%阈值加to call cut off 保证干净时机 对于长任务用HERMES的50%阈值 配合auxiliary client控制成本 压缩层用小模型执行摘要 降低成本 保存并注入task list的状态 这是oh my open agent最独特的贡献 注入关键工作目录信息 这是HERMES的SUBDIRECTORY hint tracker兜底层session recovery 当API报上下文错误时 自动创建新会话 从状态摘要恢复 这是ommy open agent的最后一道防线 技术层代码密集场景 用trree sitter精确技术 多provider场景区分cash rate write等四类 token未知模型 用探针机制自适应 核心结论是什么 没有一个项目同时具备全部这些特性 goose在干净时机和精确技术上最好 HERMES在成本控制和状态保留上有独特设计 Oh my open agents 在任务连续性上最创新 open code在安全性上有示范整合 他们才是这个领域下一步的方向 这个领域仍有整合空间 有待优秀的工程师来填补

## 标签
[]
