---
category: A
source_type: opinion
ownership: external
knowledge_role: reference
mainline_candidate: pending
source_file: 16个开源agent项目教我如何设计agent系统 p13 13-类型安全与接口设计 [BV1kdoHBCEoC_p13].ai-zh.srt
status: reference
---

# 16个开源agent项目教我如何设计agent系统 p13 13-类型安全与接口设计

> 摘要：Agnes 分析失败，使用标题规则归档。

> ⚠️ 外部参考：这是收藏的视频内容，不代表本人经历、成果或能力证据。

## 关键要点


## 可能的参考价值
待人工确认。

## 原始字幕

大家好 欢迎来到今天的课程 我是你们的架构导师 今天我们要聊一个在技术社区里被过度使用 但很少被真正追问的词 类型安全 我们都知道Python有类型注解 Typescript 也有类型系统 go和rust更是以类型安全著称 但是你们有没有想过 在这个语言这个框架里 类型安全到底保证了什么 今天我会带着大家拆解四种完全不同的类型 安全哲学 以及它们背后的代价和收益 这不是一场谁对谁错的辩论 而是要帮你在自己的项目里做出更清醒的技术 选型决策 我们先来理清楚 这四种语言的类型安全到底说的是什么 在go的yo框架里 类型安全意味着 Graph i o 在构建阶段就验证每条边的类型兼容性 写错了类型compile直接报错 在rust goose框架里 类型安全还额外包含所有权系统 你根本不可能写出数据竞争或悬空指针的代码 这是比类型检查更强的保真 在typescript open code项目里 类型注解本身是不够的 运行时仍然可以绕开 他们用了effect这样的框架来强制不变量 并且用穷举match替代try catch 在Python的dear flow和hermes agent里 类型注解是可选的 建议PDENTIC是运行时验证 mp是离线检查工具 没有什么能在你运行代码之前 阻止你犯类型错误 这四种保证的强度差异 在小型项目里几乎感受不到 但是一旦进入AI agent这种场景节点多 数据流复杂 LLM输出格式不稳定 这个差距就会被放大十倍 好我们先来看yo 这是字节跳动开发的go AI框架 它的核心抽象是RUNNABLEIO接口 注意这个泛型参数I和OI是输入类型 O是输出类型 这个接口定义了四种执行模式 invoke是普通调用 stream是流式输出 collect是把流聚合成批量 transform是流到流的转换 关键在于这四种模式在接口层面是统一的 如果你上游是流式的 下游期望批量数据框架会自动帮你 collect反过来也会自动stream 这种转换在编译时就已经确定了 不需要运行时判断 现在我们来看i know 最核心的价值graph eo的类型推断 注意graph compile compiles这一行它看起来只是准备运行 但实际上在这一行 框架就在验证整个图的类型正确性 什么意思 如果你把一个返回string的节点A 连接到期望message的节点b compile会直接失败 你在实现阶段在IDE里就能发现这个问题 而不是在三个月后生产环境的日志里 我们来对比一下Python的等价情况 用line graph 你写graph doll ADD edge node a node b的时候 不会有任何错误提示 只有当数据真正流经这条边 node b收到了他不期望的类型 才会在运行时崩溃 如果你的测试没有覆盖这条路径 这个错误可能一直潜伏到生产环境 这就是编译时类型安全在AI框架里的实际价值 把运行时才能发现的错误提前到构建时 这嗯i know还设计了stream read t来规范流失处理 这里有个有趣的对比 为什么不用go原神的channel channel有什么问题 第一你close之之后继续往里面send会panic 这是运行时错误 第二你忘记close会造成GIROUTINE泄漏 这个问题更难发现 可能运行好几天内存财报 innode stream reader怎么解决 他把必须显示关闭和明确的 EOF语义编码进了类型接口 rick方法返回terror EVELF是一个正常的结束信号 不是异常 clothes必须被调用 这意味着什么 IDE的类型检查就能发现你遗漏了 Defer as r clothes 代码审查阶段就能发现不需要的内存泄漏 监控报警 这是把生命周期规则编码进类型系统 的一个很好的例子 好接下来我们看goose 这是用rust写的AI agent框架 goose用rust写的理由 表面上看是桌面应用性能 但更深层是所有权系统提供的保证 先看核心的provider trait 它定义了LLM provider应该实现的方法 complete返回provider response或provider error goose用arc小于dying provider 大于来存储这个trade对象 这一行包含了三层保证 arc代表引用技术可以多线程安全共享 而且不可能出现悬空指针 dean provider是运行时动态分发 允许不同的provider实现OpenAI anthropic 本地模型都可以send加sync 是编译时 保证这个对象可以安全的在线程间传递 AI agent的核心操作是并发工具 调用GOOOSE的多工具并发执行依赖 Tokyo is sync runtime 在rust的所有权约束下 根本不可能写出共享可变状态被并发修改的bug 我们来具体看看 rust的所有权系统是怎么工作的 这段代码无法编译 我把provider赋值给provider2 发生了所有权转移 然后我尝试用provider调用complete编译错误 use of moved value Python的等价代码会怎么样 可能会在运行时崩溃或者更糟 产生难以调试的数据竞争症状 完全随机 但rust的编译器在编译时就消除了这类问题 再说错误处理 rust the result小于TE大于配合match 强制你穷举所有可能的错误情况 看这段代码 provider error有三种可能的变体 Context length exceeded rate limited 还有一个你没写的 如果你遗漏任何一个编译直接失败 Python的except exception pass可以吞掉所有错误 你根本不知道发生了什么 rust的穷举match 保证你不会遗漏任何一种错误处理路径 在AI应用里 错误路径特别复杂 Rate limit context Overflow off Fail network time out 每种错误需要不同的策略 RUSS的穷举match 让你不可能遗漏 说了这么多rust的理论 我们来看看ghost rust的选择 在AI agent场景里的实际价值 第一个是桌面应用 用TORI打包 不需要electron 内存占用低80% 启动速度也快得多 第二个是单二进制分发一条命令 Curl HTTPS冒号斜杠斜杠install 点goose 点AIH就安装好了 不需要预装Python环境 不需要node环境 不需要任何运行时 第三个是真正的并发 TOKYOASSICRUNTIME处理多provider并发 没有gal限制在Python里 A thank you 能避免很多并发问题 但依赖开发者正确使用ASINILOCK 这是运行时机制 不是编译时保证 第四个是长期运行 A证可能运行好几个小时 rust没有GC 没有垃圾回收 暂停延迟是可预测的 这对需要稳定响应的agent场景很重要 好现在我们来看open code 这是用typescript写的 所有后端服务都用effect框架 这是整个项目最重要的架构选择 也是最容易让人困惑的地方 我们先搞清楚effect在解决什么问题 看传统typescript的函数签名 promise小于user 大于你知道它会返回user 但不知道他会抛出什么错误 也不知道它依赖哪些外部服务 这两个问题的答案藏在实现细节里 affect the等价版本 Affect effect is less than user User not found db Erra database is greater than 这里有三个类型参数 A等于user 返回值类型 已知E等于user not found DB e error 穷举的错误类型 已知你必须处理这两种错误 否则编译不通过R等于database声明的依赖类型 已知你必须确保运行环境提供了database服务 这三个类型参数吧 可能返回什么 可能出什么错 需要什么依赖 全部编码进了类型签名 这是typescript编译器可以验证的信息 不需要读文档 不需要看实现 effect不只是穷举错误类型 它还有一套依赖注入系统 看这段代码 yield成config service 不是调用某个函数 而是声明此服务依赖config service 这是一个非常重要的语义区别 yield在这里不是从某个地方取值 而是声明我需要这个依赖 我的初始化依赖于它 layer系统在应用启动时构建完整的依赖图 如果A依赖BB依赖C他们会串行初始化 如果两个服务没有互相依赖 雷尔系统会并发初始化 他们更关键的是 如果依赖有循环 A依赖BB依赖A启动阶段就会报错 而不是等到运行时才崩溃 这就是把依赖关系编码进类型系统的好处 很多运行时才会暴露的问题 可以在启动时就发现有点effect 在AI应用里的价值特别明显 传统的try catch方式 你catch到的E是any类型 你根本不知道这是rate limit error还是network error 还是context length error 每种错误需要完全不同的处理策略 在AI应用里LLM调用可能遇到rate limit 需要退避重试 可能遇到context溢出 需要压缩历史 可能遇到认证失败 需要刷新凭证 这些错误处理逻辑完全不同 如果你的类型系统不帮你穷举 开发者很容易遗漏某一种情况 然后这个错误就变成一个未处理的异常 effect的方式 effect点catch tag 让你对每种错误类型写专门的处理逻辑 Typescript 编译器会验证你处理了所有可能的错误 遗漏任何一个变体编译直接失败 这就是把错误类型一显示暴露在签名里的威力 他迫使调用者穷举处理 而不是写一个catch all的try catch黑洞 但是effect不是免费的 如果你第一次看open code的源码 满屏的yield layer Effect Context service 对不熟悉函数式编程的开发者来说 这是一道非常陡峭的学习曲线 我第一次看的时候也是懵的 我问自己为什么要把简单的await写成这样 open code的团队显然认为这个代价是值得的 原因有几个 第一项目预期长期演化类型 安全的投资回报期足够长 第二核心团队有函数式编程背景 能快速上手 第三错误处理的正确性是产品的核心需求 AI agent的错误场景太复杂了 穷举错误处理的收益很大 什么时候effect值得引入 当你的错误处理需求复杂 而且团队有函数式编程背景 或者愿意投入学习 对于快速原型项目 这个成本几乎肯定超过收益 open code的选择说明了一种功臣哲学 为长期可维护性付出前期学习成本 这没有对错 只是取舍 好现在我们来看Python生态 dear flow和hermes agent都是Python项目 但他们对类型安全的态度不同 先看deer flow的状态定义 他用type dict定义了thread state 这个在map或者PYRIGHT里做类型检查是有意义的 但是注意运行时Python不强制执行这个类型 约束你随时可以给字典加任意字段 PIDITY是更强的类型工具 看这个research output模型 PDANTIC在实例化时会验证字段类型 如果类型不匹配 比如title传了数字123 而不是字符串会抛validation error 但是这是运行实验证 不是编译实验证 你写代码的时候不会收到任何错误提示 只有跑起来才知道 这和ANO的编译时泛型有本质区别 一诺在你写代码的时候 在IDE里 在go build的时候就能发现类型问题 DEFLOW需要实际运行才能发现 Python动态类型在AI应用里的实际成本 因项目规模而已 对于小型项目小于十个节点 快速迭代的价值远超类型错误成本几乎无感 对于中型项目10~50个节点 需要严格的测试覆盖来补偿类型安全的缺失 你必须写覆盖所有数据流路径的测试 这个测试成本往往远超加类型注解的成本 对于大型项目50个节点以上 多团队协作运行时 类型错误开始成为显著的维护负担 我们来模拟一个典型场景 节点A把context字段设为string 然后节点B期望context是list 尝试便利它 在Python里 这会在运行时崩溃 Object is not iterable 在EO里 这类错误在compile阶段就被发现 在dear flow里 他出现在生产环境的运行日志里 再看home is agent 这是NAS research的项目 它的代码里类型注解很少 几乎是pure Python 这是刻意的选择 不是疏忽 为什么 因为hermes agent的设计目标是支持200加种模型 每种模型的接口可能有细微差异 严格的类型注解会让这种灵活适配变得困难 它的场景是研究原型 RL训练 数据收集是核心目标 快速支持新模型比类型安全更重要 用户是研究团队有能力应对运行时错误 但是代价也摆在那里 两个巨型文件run agent p将近1万行 clay p也9000多行 随着规模增长 没有类型约束的代码越来越难维护 这是灵活策略的天花板 hermiss agent的选择说明了一个道理 没有赢弹 每个技术选择都有它的边界 当项目规模突破某个阈值 曾经正确的选择会变成负担 我们来做一个思想实验 如果跌flow改用go重写会怎么样 最大受益 第一编译时类型验证 运行时类型错误从生产日志里消失 第二真正的并发GIL消失 GORROUTINE原神并发多agent并发执行不受限制 第三单二进制分发无需Python环境 ping store依赖地域消失 最大代价 第一line graph是Python框架构里没有直接对应的 一诺提供了图编排 但API不一样 需要重新学习 第2land chain的Python生态太丰富了 Tavii gina 各种LLMSDK都是Python的 要在go里重新实现一遍 工作量巨大 第三团队重新学习成本 大多数ml工程师熟悉Python 不熟悉go 结论对于DFLOW这种以研究迭代为目标的框架构 重写的类型 安全收益不足以抵消生态割裂的代价 更合理的路径是继续用Python 但加强PDENTITY覆盖率 这不是Python比go差 而是在DEFLOW的约束下 Python是更合理的选择 好让我们把四种类型安全方式放在一起 做一个横向对比 检查时机 ELO和goose在编译时就发现问题 最早open code在编译时加effect 运行时Python identic在实例化时 也就是运行时错误处理 穷举性 rust match强制穷举 最强effecatch tag 强制穷举也很强制 Gold error handling 靠惯例中等Python的try except 最宽松并发安全 rust所有权在编译时保证最强 google routine加race detector工具中等 Effect fiber管理中等 Python assenio 靠开发者纪律 若学习曲线 Python最低 go中等 typescript加effect高 rust最高迭代速度 Python最快 go和typescript中等 rust最慢 适合团队 Python适合ml研究背景 go适合企业团队 effect ts适合有函数式背景的团队 rust适合系统软件团队 没有最安全的选择 只有在你的约束下 最合适的选择 我们聊完了类型安全 再来聊接口设计 好的接口设计不是把所有参数都声明清楚 而是清晰的表达调用者需要知道什么隐藏 他不需要知道的接口是一种承诺 INO的RUNNABLEIO承诺给我I类型的输入 我保证返回O类型的输出 调用者不需要知道内部是chat model还是retriever 不需要知道是流失还是批量 不需要知道有没有状态 goose the provider trait承诺给我消息和工具定义 我保证返回provider response或一个枚举化的错误 具体是OpenAI还是ANTHROPIC 调用者不需要知道OPENCODE的effect小于A ER大于承诺 我会返回A可能出现E列举的错误 我依赖R声明的服务 所有这些都在签名里 不需要读文档 也不需要看 实现接口管理里最难的问题是版本变更 open cloud plugin SDK是显示版本化的稳定合同 goose在统一仓库里统一处理INO的分离 仓库协调成本高 ECC靠人工审查保持一致 open cloud处理最成熟 breaking change需要明确版本号 给第三方插件足够的迁移时间 这是商业产品有外部开发者的正确做法啊 现在我们来总结实践 建议原型阶段小于三个月 小于五个节点 Python动态类型迭代速度大于类型 安全生产服务十个节点以上 多团队协作 go加INNO泛型或者typescript加effect 编译时发现错误 需要桌面应用加agent rust加goose 泛式单二进制跨平台没有gill限制 ml研究快速验证想法 Python加pyontic ml生态无可替代 框架开发需要给第三方提供插件接口 显示版本化 Plugin sdk breaking change 有版本号 多人同时修改节点 强类型节点I/O schema接口合同清晰 并行开发不互相阻塞 记住类型安全是需要随项目成长逐步加强的 不需要一开始就做到go泛型级别的类型安全 但应该从原型阶段就用PDENTITY等工具 建立类型保护的习惯 随着项目规模增长 逐步收紧类型约束 我们再深入看一下眼镜路径 原型阶段小于三个月 动态类型的迭代速度优势明显 频繁重构时类型注解反而是负担 当系统节点数超过十个 应该引入PANTIC强制节点 I o schema 把运行时错误迁移到启动时 当团队超过五人 且项目有长期预期 评估是否值得迁移到静态类型语言 go或typescript 这里有一个决策流程 首先判断项目阶段 原型阶段直接用Python不需要犹豫 然后判断是否需要桌面应用或单二进制分发 如果需要rust是最佳选择 最后看团队主要语言 go团队选go泛型 tab script团队选effect Python团队选PDTAC强类型 如果选错了会怎样 原型阶段引入go泛型 前期类型设计消耗大量时间 但需求变化快 导致类型频繁重构 净收益为负 大型项目继续用无类型Python节点间bug的追踪成本 随项目规模指数增长 最后我们来做一个总结 类型安全不是免费的 它需要更严格的类型定义 更陡峭的学习曲线 有时候更慢的迭代速度 但它是有回报的 编译时发现的错误比运行时发现的便宜十倍 运行时发现的比生产环境发现的便宜100倍 类型安全是一种把错误发现时机提前的投资 投资什么时候值得项目预期长期演化 回报期足够长的时候 系统规模中大型节点多 数据流复杂的时候 多团队协作 接口文档化 更重要的时候 错误代价高是企业系统 而不是研究原型的时候 投资什么时候不值得快速验证研究想法的时候 Python动态类型的迭代速度是真实优势 不是假象 单人项目类型错误的调试成本相对低 ml生态密集依赖的时候 Python生态割裂的代价超过类型安全的价值 hermes agent选择最少类型 注解是在支持200加模型的 灵活性和类型安全之间选择了灵活性 这在他的约束下是对的 I know 选择编译时 泛型是在字节跳动 企业及购生态和快速迭代之间 选择了企业稳定性 这在他的约束下也是对的 没有错的选择 只有不了解自己约束的选择 感谢大家 我们下次再见好 这是一诺关键设计的速查页 方便大家课后参考 runnable eye o定义了四种执行模式 invoke stream collect transform框架 根据上下游需求自动转换 不需要开发者手动判断 graph I/O的compile方法 在编译阶段验证整个图的类型兼容性 把运行时错误提前到构建时 stream read t提供了明确的生命周期管理 reader返回EOF是正常结束 close必须显示调用 把生命周期规则编码进类型接口 这是ghost关键设计的速茶叶provider trait 定义了LLM provider应该实现的接口 send加sync约束 保证编译时线程安全 arc小于dying provider 大于组合了三层 保证引用技术 动态分发 线程间安全传递 result小于TE大于配合match强制穷举所有错误情况 遗漏任何一个变体编译直接失败 这是open code关键设计的速查页 effect小于AER大于三元组 同时表达返回值类型 错误类型 联合依赖类型 Tab script 编译器或验证 所有情况都被处理 Context service 配合layer effect提供依赖注入系统 yield不是调用函数 而是声明依赖关系 Effect 点catch tag 让你对每种错误类型写专门的处理 逻辑类型系统保证穷举

## 标签
[]
