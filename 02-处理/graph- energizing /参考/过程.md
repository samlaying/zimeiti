Xiaomi MiMo Studio

三年图工程实践：LangGraph智能体构建
Graph Engineering with LangGraph
人生三运难全
闲谈能力决定人生远度
产品经理JD拆解与面试准备
Kimi冲击智谱股价与估值重构
贵阳银河左岸音乐节购票指南
直播带货话术学习笔记
直播带货话术：券叠加速下单
搏击身材训练与饮食指南
0726新对话
居家徒手薄肌训练指南
居家徒手力量训练完整计划
薄肌身材居家训练指南
搏击身材训练与饮食笔记
搏击身材训练全解
新手健身训练与饮食指南
Pi Agent极简Harness系统详解
抖音健身内容学习笔记
窦文涛的性格与职场趣事
Codex技能描述精简说明
产品经理面试项目价值拆解

MiMo Claw 正式版上线
旗舰模型 + 金山办公生态，订阅服务全新开启



帮我翻译下这篇文章
Docs
Pricing
Try LangSmith
Get a demo
Open Source
LangGraph
Agent Architecture
3 Years of Graph Engineering with LangGraph


S. Runkle,
H. Chase
July 22, 2026

7
min
Go back to blog
Modeling agents as graphs
When to represent agents as graphs
When not to use graphs
What building LangGraph taught us
What's actually new
The bigger idea
Share

"Graph engineering" surfaced this weekend, kicked off by this tweet:


It's the latest term to come out of X's AI content factory, joining prompt engineering, context engineering, harness engineering, and loop engineering. While it’s both tempting and accurate to call these terms buzzwords, they exist and emerge for a reason: they do describe real challenges and design decisions builders face.

At the end of the day, the goal is to harness the power of LLMs to do useful things for us. Whether you use prompting or agents or loops or graphs, those are implementation details. The reason so many terms exist that getting LLMs to do work is hard.  They are a new type of non-robust, non-deterministic software and we’re constantly trying new strategies to get them to work. And these new strategies lead to new buzzwords.

Buzzwords aside, representing agentic systems as graphs (”graph engineering”) is a very reasonable way to harness the power of LLMs. Specifically, it allows you (as the builder) to impose your preconceptions of how the system should work into more constrained paths, not relying solely on the judgement of the LLM. More concretely, it lets you more tightly control behavior when you want the agent to follow specific paths.

This intuition drove us to build LangGraph three years ago, as a framework to help build these types of agentic systems. Today, LangGraph is downloaded 65M+ times a month, and used by startups and enterprises alike.

Compared to the myriad of other agent frameworks out there, the reason LangGraph rose in popularity is because of the balance it strikes between deterministic paths and agentic steps.

Here’s what we’ve learned from years of building agentic systems as graphs.

Modeling agents as graphs
A graph gives you a concrete way to define the workflow an agent follows.

In LangGraph, nodes do work. A node can be deterministic code, a single LLM call, a tool call, or a full agent with its own internal loop.

Edges define what happens next. Some edges are deterministic. Others are conditional, based on the result of a node, the current state, or some external signal.

You can think of this as a state machine. The graph defines the workflow, the state that moves through it, and the transitions between steps.

When to represent agents as graphs
Real-world agent workflows often have predictable structure: a support agent classifies an issue before answering or escalating, a coding agent inspects the repository before proposing a change, and a compliance workflow requires approval before taking an external action.

Graphs let you encode that structure directly: the valid paths, where the model gets to choose, and where the system should enforce deterministic behavior instead of hoping the model makes the right call every time.

By representing the system as a graph, you are encoding your world knowledge of how this system should work. Just as prompts contain domain knowledge that separates your agent from generic ChatGPT, so can these “cognitive architectures”.

Take a knowledge base agent that uses three subagents for search: a GitHub agent for code, issues, and pull requests, a Notion agent for internal docs and wikis, and a Slack agent for relevant threads. The workflow has three fixed stages: classify, search, synthesize.


The result is code and model reasoning working together: the model reasons where it adds value, code handles the rest, and the agent gets cheaper, faster, and more predictable.

When not to use graphs
Some tasks are more agentic by nature, and forcing them into deterministic paths is the wrong move. In these cases, you don’t want to represent the system as a graph but rather just use an agent harness (like Deep Agents).

Generic deep research is a good example: a research agent needs to plan, delegate, search, read, and synthesize in ways that are hard to pin down ahead of time. We built early deep research on predefined LangGraph workflows, then moved to a more agentic core loop. GPT Researcher, a popular deep research implementation, made the same move, swapping its graph-shaped multi-agent pipeline for Deep Agents so planning, delegation, and context management emerge in the harness rather than being hardcoded in the graph.

What building LangGraph taught us
We've been building agents powered by graphs for the last three years. Here's what we've learned.

First, agent graphs are usually not DAGs.

Production agents need cycles: retrying failed tool calls, asking users for missing information, revising answers after validation, calling tools repeatedly until they have enough context, and pausing for human input before resuming. Looping is a core part of agentic systems, so they are likely not DAGs.

Second, loops are simple graphs.

Loop engineering isn't an alternative to graphs, so much as a simple version of them. As David Khourshid put it, a loop is just a directed, cyclic graph. In fact, the LangChain framework, which is based on a simple agentic loop, is built on top of LangGraph.

Third, dynamic transitions matter.

You do not always want to define every edge up front. Sometimes a node decides at runtime how much work to create. Map-reduce is the classic case: split an input into pieces, send each to a worker, then combine the results. The number of workers depends on the input, and you do not know that number in advance.

LangGraph handles this with Send, which lets a node route work to one or more downstream nodes dynamically, without statically defining every transition.

This is important because useful agent systems mix known structure with runtime variability. You might know research should fan out and then synthesize, but not how many sources there will be. You might know a supervisor should delegate to workers, but not know which specific workers to use until the task starts. Graphs still need flexibility at runtime.

What's actually new
Representing agentic systems as graphs isn't new, we've been doing it for three years! Has anything changed in this new wave of “graph engineering”?

A generous interpretation would say that what's changed is what you can put inside a node. Early on, nodes were deterministic code or a single LLM call. Now that agents themselves are reliable enough to trust with real work, a node can be a full agent run — you're orchestrating agents, not just LLM calls.

Coding agents are a good example of this. They're some of the most effective and impactful agents in production today, and embedding one as a node inside a larger graph is a newly practical pattern.

Consider a docs agent that turns a slack request, like this:


Into a ready for review pull request:


Each node in this graph sits at a different point on the deterministic-to-agentic scale:

Fixed steps: the slack and linear operations are powered by set code and API calls.
Model steps: the classifier and the synthesize step use a single LLM call with no tools.
Agent steps: the reference docs agent and the conceptual docs agent complete more open ended work in their relevant codebases.
The mix of determinism and agency here is what makes this docs agent predictable, powerful, and efficient.

The bigger idea
Graph engineering isn't a new idea. It's the latest name for a well established approach to building reliable agents.

It's the same idea behind loop engineering and harness engineering: building putting model reasoning in the right places, with the right context, at each step.

If you want to try out graph engineering, try out LangGraph.

Acknowledgements
Thanks to @huntlovell and @nfcampos for thoughtful review.

Related content

Open Source
Observability & Evals
How We Benchmark Deep Agents


N. Hollon,
H. Chase
July 23, 2026

4
min

Deep Agents
Agent Architecture
Open Source
How to Use RLMs in Deep Agents

Sydney Runkle
July 1, 2026

8
min

Agent Architecture
Deep Agents
Open Source
Running Untrusted Agent Code Without a Sandbox

Hunter Lovell
June 30, 2026

6
min

Sign up for our newsletter to stay up to date
john.doe@acme.com
See what your agent is really doing
LangSmith, our agent engineering platform, helps developers debug every agent decision, eval changes, and deploy in one click.

Try LangSmith
Get a demo
Products
LangSmith Platform
LangSmith Observability
LangSmith Evaluation
LangSmith Deployment
LangSmith Fleet
LangSmith Sandboxes
Deep Agents
LangChain
LangGraph
Resources
Blog
Customer Stories
Guides
Community
Changelog
Docs
Support
LangChain Academy
Company
About
Careers
Partners
Trust Center
Marketing Assets
Events
Sign up for our newsletter to stay up to date
Your email
All systems operational
Privacy policy
Terms of service

使用 LangGraph 进行三年图工程的实践总结

S. Runkle, H. Chase
2026年7月22日
7 分钟阅读



将智能体建模为图

何时将智能体表示为图
何时不使用图
构建 LangGraph 教会了我们什么
真正的新东西是什么
更大的理念


"图工程"（Graph Engineering）这个概念在本周末浮出水面，起源于这条推文：


它是 X（推特）AI 内容生态中诞生的最新术语，继"提示工程"（Prompt Engineering）、"上下文工程"（Context Engineering）、"框架工程"（Harness Engineering）和"循环工程"（Loop Engineering）之后又一个新词。虽然把这些术语称为"流行词"既诱人又准确，但它们的出现是有原因的：它们确实描述了构建者面临的真实挑战和设计决策。


归根结底，我们的目标是利用大语言模型（LLM）的能力来为我们做有用的事。无论你使用的是提示、智能体、循环还是图，这些都只是实现细节。之所以存在如此多的术语，是因为让 LLM 真正干活并不容易。它们是一种新型的非稳健、非确定性软件，我们一直在不断尝试新策略来让它们正常工作。而这些新策略又催生了新的流行词。


抛开流行词不谈，将智能体系统表示为图（即"图工程"）是一种非常合理的方式来驾驭 LLM 的能力。具体而言，它允许你（作为构建者）将你对系统应如何运作的先验认知注入到更受约束的路径中，而不是完全依赖 LLM 的判断。更具体地说，当你希望智能体遵循特定路径时，它可以让你更紧密地控制行为。


正是这种直觉驱动我们在三年前构建了 LangGraph，作为一个帮助构建此类智能体系统的框架。如今，LangGraph 的月下载量超过 6500 万次，被初创公司和企业广泛使用。


与市面上众多其他智能体框架相比，LangGraph 能够脱颖而出的原因在于它在确定性路径和智能体自主步骤之间取得了良好的平衡。


以下是我们多年来将智能体系统构建为图所积累的经验。



将智能体建模为图

图为你提供了一种具体的方式来定义智能体所遵循的工作流。


在 LangGraph 中，节点（Node）负责执行工作。一个节点可以是确定性代码、单次 LLM 调用、一次工具调用，或者一个拥有自身内部循环的完整智能体。


边（Edge）定义了接下来发生什么。有些边是确定性的，有些则是条件性的——基于节点的执行结果、当前状态或某些外部信号。


你可以把它理解为一个状态机。图定义了工作流，状态在其中流转，以及步骤之间的转换。



何时将智能体表示为图

现实世界的智能体工作流通常具有可预测的结构：客服智能体在回答或升级之前先对问题进行分类，代码智能体在提出修改建议之前先检查代码仓库，合规工作流在执行外部操作之前需要审批。


图可以让你直接编码这种结构：哪些是有效路径、模型在哪些环节可以自主选择、以及系统在哪些环节应强制执行确定性行为，而不是寄希望于模型每次都做出正确的决策。


通过将系统表示为图，你实际上是在编码你对这个系统应如何运作的领域知识。正如提示词中包含了能将你的智能体与通用 ChatGPT 区分开来的领域知识一样，这些"认知架构"也能起到同样的作用。


以一个知识库智能体为例，它使用三个子智能体进行搜索：一个 GitHub 智能体处理代码、Issue 和 Pull Request，一个 Notion 智能体处理内部文档和 Wiki，一个 Slack 智能体搜索相关讨论。该工作流有三个固定阶段：分类、搜索、综合。


最终的结果是代码和模型推理协同工作：模型在能发挥价值的地方进行推理，代码处理其余部分，从而使智能体变得更便宜、更快速、更可预测。



何时不使用图

有些任务本质上更适合智能体自主执行，强行将其纳入确定性路径反而适得其反。在这种情况下，你不应该将系统表示为图，而是应该使用智能体框架（如 Deep Agents）。


通用深度研究就是一个很好的例子：研究型智能体需要以难以事先确定的方式进行规划、委派、搜索、阅读和综合。我们早期在 LangGraph 上用预定义的工作流构建了深度研究功能，后来转向了更偏向智能体自主的核心循环。GPT Researcher，一个流行的深度研究实现方案，也做了同样的调整——将其基于图的多智能体流水线替换为 Deep Agents，使得规划、委派和上下文管理在框架中自然涌现，而不是被硬编码在图中。



构建 LangGraph 教会了我们什么

过去三年，我们一直在用图来构建智能体系统。以下是我们的心得。


第一，智能体图通常不是 DAG（有向无环图）。


生产环境中的智能体需要循环：重试失败的工具调用、向用户询问缺失的信息、在验证后修正答案、反复调用工具直到获取足够的上下文，以及在恢复执行前暂停等待人工输入。循环是智能体系统的核心组成部分，因此它们通常不是 DAG。


第二，循环本身就是简单的图。


循环工程并非图的替代方案，而更像是图的一种简化形式。正如 David Khourshid 所指出的，一个循环就是一个有向有环图。事实上，LangChain 框架（基于简单的智能体循环构建）本身就是建立在 LangGraph 之上的。


第三，动态转换很重要。


你并不总是想预先定义所有的边。有时一个节点需要在运行时决定要创建多少工作。Map-reduce 就是一个经典案例：将输入拆分成多个部分，将每个部分发送给一个工作节点，然后合并结果。工作节点的数量取决于输入，而你事先并不知道这个数量。


LangGraph 通过 Send 机制来处理这种情况，它允许一个节点在运行时动态地将工作路由到一个或多个下游节点，而无需静态定义每一个转换。


这一点很重要，因为有用的智能体系统需要将已知结构与运行时可变性相结合。你可能知道研究应该先发散再综合，但不知道会有多少个数据源。你可能知道监督者应该委派给工作节点，但直到任务开始时才知道具体应该使用哪些工作节点。图仍然需要在运行时具备灵活性。



真正的新东西是什么

将智能体系统表示为图并不是新事物——我们已经这样做了三年！在这一波"图工程"的新浪潮中，有什么真正改变了吗？


一个宽泛的解读是，变化在于你可以在节点中放入什么。早期，节点是确定性代码或单次 LLM 调用。现在，智能体本身已经足够可靠，可以被信任来完成真正的工作，一个节点可以是一次完整的智能体运行——你编排的不再是 LLM 调用，而是智能体。


代码智能体就是一个很好的例子。它们是当今生产环境中最有效、最具影响力的智能体之一，将一个代码智能体作为节点嵌入到更大的图中，是一种新近才变得切实可行的模式。


以一个文档智能体为例，它将一条 Slack 请求（如下所示）转换为一个可供审查的 Pull Request：


该图中的每个节点都处于确定性与智能体自主性之间不同的位置：


固定步骤：Slack 和 Linear 操作由预设的代码和 API 调用驱动。
模型步骤：分类器和综合步骤使用单次 LLM 调用，不涉及工具调用。
智能体步骤：参考文档智能体和概念文档智能体在各自的代码库中完成更开放性的工作。

确定性与自主性之间的这种混合，使得这个文档智能体既可预测、又强大且高效。



更大的理念

图工程并不是一个新想法。它是构建可靠智能体这一成熟方法的最新名称。


它与循环工程和框架工程背后的思路是一样的：将模型推理放在正确的位置上，在每一步提供正确的上下文。


如果你想尝试图工程，可以试试 LangGraph。



致谢
感谢 @huntlovell 和 @nfcampos 的细致审阅。

从上下文来说，这个是什么意思
抛开流行词不谈，将智能体系统表示为图（即"图工程"）是一种非常合理的方式来驾驭 LLM 的能力。具体而言，它允许你（作为构建者）将你对系统应如何运作的先验认知注入到更受约束的路径中，而不是完全依赖 LLM 的判断。更具体地说，当你希望智能体遵循特定路径时，它可以让你更紧密地控制行为。

这段话的核心意思是：用图来给 LLM "画好跑道"，而不是让它自由发挥。


让我用一个具体例子来拆解：



问题背景

LLM 本身是"非确定性"的——你给它同样的输入，它可能给出不同的回答，而且有时会犯错、跑偏、幻觉。如果你完全放手让 LLM 自己决定每一步做什么，结果往往不可控。


"图"做了什么

所谓"将系统表示为图"，就是你作为开发者提前画好一张流程图：


第一步做什么（比如：分类用户问题）
第二步走哪条分支（比如：如果是技术问题，走 A 路径；如果是账单问题，走 B 路径）
哪些步骤由 LLM 自主决策（比如：生成回答）
哪些步骤由代码硬性规定（比如：调用某个 API、必须经过审批）

"先验认知注入到更受约束的路径中"

这句话翻译成大白话就是：


你把"这个系统应该怎么运转"的行业经验和业务知识，提前编码进了流程图里，限制了 LLM 的行动范围。


比如你做一个客服智能体，你比 LLM 更清楚：退款请求必须先查订单、再校验资格、最后才执行退款。你把这些步骤写死在图里，LLM 就不能跳过任何一步。


"而不是完全依赖 LLM 的判断"

对比两种方式：


方式	做法	风险
纯智能体	给 LLM 一个目标，让它自己决定怎么做	可能漏步骤、走错路、乱调工具
图工程	你画好流程图，LLM 只在关键节点上做决策	流程可控，关键环节不会出错


一句话总结

这段话说的是：图工程的本质价值在于，开发者可以用自己的业务经验来约束和引导 LLM 的行为，而不是把所有决策权都交给一个可能犯错的模型。 图是"人的经验"和"模型的能力"之间的桥梁。

也就是说其实它的本质上其实只不过是需要你去提前有一种思维。当我们需要一个产品要用到这个Agent的时候，我们其实对每个地方应该怎么去完成，每个地方应该怎么去做，每个地方怎么去设置，我们预先有一个概念。而不只是说一味地让AI来去自己去探索、摸索，这样子导致人类失去了对这个全局的认知和感受；
好比就是你干一件事情之前，你得先知道这个事情要能怎么干，1234每步需要怎么做，然后只到了不是说让AI一步，从1~2~3也全都让AI来去做。我理解是不是它是更多的提供了一种方法跟思想？我从原文中可以得到答案吗

你的理解非常准确。原文中有多处可以直接支撑你的观点，我帮你一一对应：



你的理解："先想清楚事情怎么做，而不是全交给AI"

原文对应：


"it allows you (as the builder) to impose your preconceptions of how the system should work into more constrained paths, not relying solely on the judgement of the LLM."


它允许你将你对系统应如何运作的先验认知注入到更受约束的路径中，而不是完全依赖 LLM 的判断。


这就是你说的"不是一味让AI自己去探索"。



你的理解："1234每步需要怎么做，提前有概念"

原文对应：


"By representing the system as a graph, you are encoding your world knowledge of how this system should work."


通过将系统表示为图，你实际上是在编码你对这个系统应如何运作的领域知识。


"world knowledge"就是你说的"预先有一个概念"——你知道这个业务该怎么跑。



你的理解："不只是让AI从1到2到3全做完"

原文对应：


"The result is code and model reasoning working together: the model reasons where it adds value, code handles the rest."


最终的结果是代码和模型推理协同工作：模型在能发挥价值的地方进行推理，代码处理其余部分。


不是所有事都让AI做，而是该人定的由代码定，该AI判断的让AI判断。



你的理解："它更多的是一种方法和思想"

原文直接说了：


"Graph engineering isn't a new idea. It's the latest name for a well established approach to building reliable agents."


图工程不是一个新想法。它是构建可靠智能体这一成熟方法的最新名称。


以及：


"It's the same idea behind loop engineering and harness engineering: putting model reasoning in the right places, with the right context, at each step."


它的核心思想是一样的：把模型推理放在正确的位置上，在每一步提供正确的上下文。



所以，原文给出的答案就是：

图工程本质上确实是一种方法论和思维方式。它的核心主张是：


在让 AI 干活之前，你得先想清楚这件事该怎么干——哪些步骤是固定的、哪些可以让 AI 自主决策、整体流程是什么样的。然后把这个"蓝图"编码成图，让 AI 在你划定的框架内发挥，而不是漫无目的地自己摸索。


你的理解完全正确，原文也是这么说的。

这里没有理解
何时不使用图

有些任务本质上更适合智能体自主执行，强行将其纳入确定性路径反而适得其反。在这种情况下，你不应该将系统表示为图，而是应该使用智能体框架（如 Deep Agents）。


通用深度研究就是一个很好的例子：研究型智能体需要以难以事先确定的方式进行规划、委派、搜索、阅读和综合。我们早期在 LangGraph 上用预定义的工作流构建了深度研究功能，后来转向了更偏向智能体自主的核心循环。GPT Researcher，一个流行的深度研究实现方案，也做了同样的调整——将其基于图的多智能体流水线替换为 Deep Agents，使得规划、委派和上下文管理在框架中自然涌现，而不是被硬编码在图中。

这段话和前面说的看似矛盾，其实不矛盾。关键区别在于：你能不能提前想清楚每一步怎么做。



用你之前的比喻来说

前面说的场景：

你很清楚这件事的流程是 1→2→3→4，所以你提前画好路线图，让AI在框架内走。


这里说的场景：

你自己都不知道这件事该怎么干。你只知道目标（比如"帮我研究一下XXX"），但具体要搜索什么、搜几次、中间会发现什么、需要从哪些角度综合——你事先根本想不出来。



用一个生活化的例子对比

适合用图的场景（你知道流程）：


帮客户退款：

1.查订单 → 2. 验证资格 → 3. 执行退款 → 4. 发通知

每一步你都清楚，所以你画好图让AI按流程走。


不适合用图的场景（你不知道流程）：


帮老板做一份行业调研报告：

先搜什么？不知道，得看情况
搜到的东西有用吗？得读了才知道
需要从几个角度分析？得看搜到了什么
中间会不会发现新方向？一定会

这种任务的每一步都取决于上一步的结果，你没法提前画好1234。如果你硬画一张图，反而会束缚AI的手脚，让它没法灵活应变。



原文用的真实例子

原文说的是"深度研究"（Deep Research）：


研究型智能体需要以难以事先确定的方式进行规划、委派、搜索、阅读和综合。


关键词是"难以事先确定"——你连自己都不知道该怎么干，就没法画图来约束AI。


所以他们的做法是：

早期：在 LangGraph 上用预定义的工作流（硬画图）→ 效果不好
后来：转向更开放的智能体循环（让AI自己决定下一步做什么）→ 效果更好


一句话总结

场景	你是否知道流程	做法
退款、客服、文档生成	清楚知道每一步	用图，提前规划好路径
深度研究、开放性探索	完全不知道中间会遇到什么	不用图，让AI自主探索

前面说的"提前想清楚"和这里说的"不用图"并不矛盾——前提是你能想清楚，才用图；想不清楚，就别硬画图。

为什么突然提及到了DAG

这里提到 DAG 其实是有针对性的，不是突然冒出来的。


背景：一提到"图"，技术人员的本能反应

在软件工程领域，说到"把工作流建模成图"，大多数开发者脑子里浮现的第一反应就是 DAG（有向无环图）。


因为 DAG 在工程界非常常见：

数据流水线（Airflow）
编译依赖
CI/CD 流程

这些都是"从 A 到 B 到 C，走完就结束，不回头"的结构。


作者想纠正的误解

所以作者提到 DAG，是为了说：


你们别搞错了——智能体图不是 DAG，不是画好一条流水线从头走到尾就完了。


因为智能体系统天然需要循环：

工具调用失败了 → 重试
信息不够 → 回头问用户
验证没通过 → 修正后重来

这些都是"往回走"的动作，一旦有"往回走"，就不是 DAG 了（因为 DAG 的"A"就是 Acyclic——无环）。


如果你把智能体当成 DAG 来建模会怎样？

你画了一条流水线：1 → 2 → 3 → 结束


结果实际情况是：

第2步失败了，需要回到第1步重来
第3步发现信息不够，需要回到第2步补充

DAG 不允许"回头"，你就处理不了这些情况。


所以作者的核心观点是：

智能体图是带环的图（有循环），不是 DAG。不要用 DAG 的思维来设计智能体系统，否则你会丢失智能体最关键的特性——自我修正和迭代。


这也是为什么它排在"经验总结"的第一条——因为这是最常见的认知误区。

一个宽泛的解读是，变化在于你可以在节点中放入什么。早期，节点是确定性代码或单次 LLM 调用。现在，智能体本身已经足够可靠，可以被信任来完成真正的工作，一个节点可以是一次完整的智能体运行——你编排的不再是 LLM 调用，而是智能体。


代码智能体就是一个很好的例子。它们是当今生产环境中最有效、最具影响力的智能体之一，将一个代码智能体作为节点嵌入到更大的图中，是一种新近才变得切实可行的模式。
# 按照他说的，我要怎么去实现这个内容呢？嗯，我有什么方式去体现用代码呢？还是说在code code在code里面可以去用什么样的方式去体验这个过程
换一个国内比较知名的，就是中国国内可能比较有用的。然后，刚刚那一个flag，大家的感知不是很强烈


你的问题很好，我用一个中国用户最容易感知的场景来帮你彻底理解这件事。



场景：淘宝/京东级的客服智能体系统

想象你是一个电商平台的开发者，要构建一个客服系统。用户来了可能会问各种问题：查物流、退换货、投诉商家、问促销活动……



早期做法：一个节点 = 一次 LLM 调用

text
用户提问 → LLM分类 → LLM回答 → 结束
用户提问 → LLM分类 → LLM回答 → 结束

就像一个"聪明的客服"，什么都靠它自己判断。问题是：它可能搞错退款规则、漏掉审批流程、不知道物流该调哪个API。


现在的做法：一个节点 = 一个完整的智能体

text
用户提问 → 分类节点 → [物流智能体] 或 [退款智能体] 或 [投诉智能体] → 综合回复
用户提问 → 分类节点 → [物流智能体] 或 [退款智能体] 或 [投诉智能体] → 综合回复

每个"智能体"节点内部有自己的推理循环、自己的工具、自己的记忆。它不是调一次LLM就完事，而是一个能自主规划、执行、验证的完整系统。



代码实现

下面用 Python + LangGraph 来实现这个概念：


第一步：定义"物流智能体"——它本身就是一个完整的Agent

python
from langchain_openai import ChatOpenAI
from langgraph.prebuilt import create_react_agent

# 物流智能体：有自己的工具、能自主推理、能循环调用工具
def create_logistics_agent():
    """这是一个完整的智能体，不是一个简单的LLM调用"""

    llm = ChatOpenAI(model="qwen-plus")  # 也可以用通义千问等国产模型

    # 它有自己的工具
    tools = [
        track_package,        # 查物流
        contact_warehouse,    # 联系仓库
        check_return_policy,  # 查退货政策
    ]

    # create_react_agent 会创建一个能 推理→行动→观察→再推理 的循环
    # 这就是"一个完整的智能体"
    agent = create_react_agent(llm, tools)

    return agent
from langchain_openai import ChatOpenAI
from langgraph.prebuilt import create_react_agent

# 物流智能体：有自己的工具、能自主推理、能循环调用工具
def create_logistics_agent():
    """这是一个完整的智能体，不是一个简单的LLM调用"""

    llm = ChatOpenAI(model="qwen-plus")  # 也可以用通义千问等国产模型

    # 它有自己的工具
    tools = [
        track_package,        # 查物流
        contact_warehouse,    # 联系仓库
        check_return_policy,  # 查退货政策
    ]

    # create_react_agent 会创建一个能 推理→行动→观察→再推理 的循环
    # 这就是"一个完整的智能体"
    agent = create_react_agent(llm, tools)

    return agent

关键点：这个 agent 内部是这样的循环：


text
思考："用户要退货，我需要先查订单"
→ 调用 track_package 查到订单信息
→ 思考："订单已签收7天内，符合退货条件"
→ 调用 check_return_policy 确认政策
→ 思考："可以退，我来生成退货单"
→ 返回结果
思考："用户要退货，我需要先查订单"
→ 调用 track_package 查到订单信息
→ 思考："订单已签收7天内，符合退货条件"
→ 调用 check_return_policy 确认政策
→ 思考："可以退，我来生成退货单"
→ 返回结果

它自己决定调几个工具、调几次、按什么顺序——这就是"一个节点是一个完整智能体"的含义。


第二步：定义退款智能体（另一个完整Agent）

python
def create_refund_agent():
    """退款智能体：专门处理退款，有自己的审批逻辑"""

    llm = ChatOpenAI(model="qwen-plus")

    tools = [
        get_order_detail,      # 查订单详情
        calculate_refund,      # 计算退款金额
        request_approval,      # 申请主管审批（金额>500时）
        execute_refund,        # 执行退款
    ]

    agent = create_react_agent(llm, tools)
    return agent
def create_refund_agent():
    """退款智能体：专门处理退款，有自己的审批逻辑"""

    llm = ChatOpenAI(model="qwen-plus")

    tools = [
        get_order_detail,      # 查订单详情
        calculate_refund,      # 计算退款金额
        request_approval,      # 申请主管审批（金额>500时）
        execute_refund,        # 执行退款
    ]

    agent = create_react_agent(llm, tools)
    return agent

第三步：把它们"组装"进一个大图

python
from langgraph.graph import StateGraph, MessagesState, START, END

# 创建各个智能体节点
logistics_agent = create_logistics_agent()
refund_agent = create_refund_agent()

# 分类节点：用一次LLM调用判断用户意图（这个节点不需要是完整智能体）
def classify_node(state: MessagesState):
    llm = ChatOpenAI(model="qwen-plus")
    response = llm.invoke([
        {"role": "system", "content": "你是一个分类器。根据用户消息判断意图，只回复：物流/退款/其他"},
        *state["messages"]
    ])
    return {"messages": [response]}

# 路由函数：根据分类结果决定走哪条路
def router(state: MessagesState):
    last_msg = state["messages"][-1].content
    if "物流" in last_msg:
        return "logistics"
    elif "退款" in last_msg:
        return "refund"
    else:
        return "other"

# 其他问题的简单回复
def other_node(state: MessagesState):
    llm = ChatOpenAI(model="qwen-plus")
    response = llm.invoke(state["messages"])
    return {"messages": [response]}

# ========== 组装图 ==========
graph_builder = StateGraph(MessagesState)

# 添加节点 —— 注意：logistics_agent 和 refund_agent 是完整的智能体！
graph_builder.add_node("classify", classify_node)
graph_builder.add_node("logistics", logistics_agent)   # 这里放的是一个完整的Agent
graph_builder.add_node("refund", refund_agent)          # 这里也是
graph_builder.add_node("other", other_node)

# 添加边
graph_builder.add_edge(START, "classify")
graph_builder.add_conditional_edges("classify", router, {
    "logistics": "logistics",
    "refund": "refund",
    "other": "other"
})
graph_builder.add_edge("logistics", END)
graph_builder.add_edge("refund", END)
graph_builder.add_edge("other", END)

# 编译
customer_service_graph = graph_builder.compile()
from langgraph.graph import StateGraph, MessagesState, START, END

# 创建各个智能体节点
logistics_agent = create_logistics_agent()
refund_agent = create_refund_agent()

# 分类节点：用一次LLM调用判断用户意图（这个节点不需要是完整智能体）
def classify_node(state: MessagesState):
    llm = ChatOpenAI(model="qwen-plus")
    response = llm.invoke([
        {"role": "system", "content": "你是一个分类器。根据用户消息判断意图，只回复：物流/退款/其他"},
        *state["messages"]
    ])
    return {"messages": [response]}

# 路由函数：根据分类结果决定走哪条路
def router(state: MessagesState):
    last_msg = state["messages"][-1].content
    if "物流" in last_msg:
        return "logistics"
    elif "退款" in last_msg:
        return "refund"
    else:
        return "other"

# 其他问题的简单回复
def other_node(state: MessagesState):
    llm = ChatOpenAI(model="qwen-plus")
    response = llm.invoke(state["messages"])
    return {"messages": [response]}

# ========== 组装图 ==========
graph_builder = StateGraph(MessagesState)

# 添加节点 —— 注意：logistics_agent 和 refund_agent 是完整的智能体！
graph_builder.add_node("classify", classify_node)
graph_builder.add_node("logistics", logistics_agent)   # 这里放的是一个完整的Agent
graph_builder.add_node("refund", refund_agent)          # 这里也是
graph_builder.add_node("other", other_node)

# 添加边
graph_builder.add_edge(START, "classify")
graph_builder.add_conditional_edges("classify", router, {
    "logistics": "logistics",
    "refund": "refund",
    "other": "other"
})
graph_builder.add_edge("logistics", END)
graph_builder.add_edge("refund", END)
graph_builder.add_edge("other", END)

# 编译
customer_service_graph = graph_builder.compile()

运行

python
response = customer_service_graph.invoke({
    "messages": [{"role": "user", "content": "我的快递到哪了？订单号123456"}]
})
print(response["messages"][-1].content)
response = customer_service_graph.invoke({
    "messages": [{"role": "user", "content": "我的快递到哪了？订单号123456"}]
})
print(response["messages"][-1].content)


整个系统的流程图

text
用户："我的快递到哪了？"
         │
    ┌────▼────┐
    │  分类节点 │  ← 一次LLM调用（简单，不需要智能体）
    └────┬────┘
         │ 判断：物流
         ▼
  ┌──────────────┐
  │  物流智能体    │  ← 这是一个完整的Agent！
  │              │
  │  思考→调工具  │
  │  →再思考     │     它内部可能循环3-5次
  │  →再调工具   │
  │  →得出结论   │
  └──────┬───────┘
         │
    ┌────▼────┐
    │ 返回结果  │
    └─────────┘
用户："我的快递到哪了？"
         │
    ┌────▼────┐
    │  分类节点 │  ← 一次LLM调用（简单，不需要智能体）
    └────┬────┘
         │ 判断：物流
         ▼
  ┌──────────────┐
  │  物流智能体    │  ← 这是一个完整的Agent！
  │              │
  │  思考→调工具  │
  │  →再思考     │     它内部可能循环3-5次
  │  →再调工具   │
  │  →得出结论   │
  └──────┬───────┘
         │
    ┌────▼────┐
    │ 返回结果  │
    └─────────┘


对比：节点里放"LLM调用" vs 放"完整智能体"

节点里放LLM调用	节点里放完整智能体
能力	调一次模型，出一个回答	能自己规划、调多个工具、循环推理
适合	分类、总结、简单问答	复杂任务：查物流、处理退款、写代码
可控性	高（一次调用就结束）	中（内部有循环，但工具是限定的）
例子	"判断用户问的是什么"	"帮用户完整地处理退款流程"


一句话总结

实现"节点=智能体"的关键就是：用 create_react_agent() 或类似方式创建一个有工具、能循环推理的Agent，然后直接把它当作一个节点塞进更大的图里。 大图负责路由和流程控制，每个节点内部的智能体负责自主完成具体任务。这就是原文说的"你编排的不再是LLM调用，而是智能体"的代码体现。

综合上面的内容来说，我觉得他对产品经理去做这个活有几个意义。第一个是，有了更多的意识吧，就是他意识到是说我们一个node其实现在已经可以变成一个Agent。这个Agent我们可以让它更稳定地接近我们这个系统，而不只是觉得是说一套系统只是一个Agent，它可以进行一个多Agent的一个系统，然后，它能够有这种框架的意识，是说，嗯，整体上会有更多的选择性。以及可能让他们更能够理解说现在怎么去做一个更好的一个系统，而不只是单独的是说做一个单Agent或者多Agent，有可能就是要把它变成一个官夫。说整体上的思维扩展了
第二个，对于产品经理来说的应该是，这是在弄的这边弄的这边其实说明，现在已经可以把变成一个Agent这边来做一个节点了。我觉得是不是只有这两种比较有意义？说对这种新的物质的发展就是新系统新产品形态的感受比较有意义
# 基于上面的理解，和下面的要求，告诉我应该怎么去分析这个新概念
系统音：
其实所谓的加分项本质上就是面试官最最关注的那些关键点，而很多人在准备面试的时候，根本不知道把自己的重心重点放在哪里，导致最明显的问题就是非常平，基本上所有人准备的模子以及它的标准都是类似的，很容易在面试里面被横向对比干掉，所以基于此我大概看了非常非常多，我们内部同学的面试录音，包括也跟自己很多过去做，AI产品leader的同事有过沟通基本上重点就是三个第一类叫能体现最新技术应用的理解这块不是说我们要被很多技术名词，而是要从底层去理解清楚新的技术新的概念到底解决了当前产品的什么问题，对产品设计有什么影响比如说现在最流行的不要只讲概念，对吧？

系统音：
比如说哈里斯包括了上下文工具的调用记忆路由评测权限和异常处理这些东西分别指的是啥，你讲给面试官意义不大他大模型直接问一下他也能有些信息你要给到他的是你的实践的认知我那个比如说如果你想让他变成真正能够完成上任他就需要读取客户的，资料，分析历史的沟通信息啊包括判断商机的阶段生成下一步的，建议并且他有可能可以主动的，去触达用户这部分有权限的设计甚至给这些销售会创建分解的，任务，然后去更新管理系统那要做到这些，你是不是就需要正确的，绩效模型的数据能不能主动去为这些用户他自己能不能之前的信息进行处理这些才是安全的准备进去的时候这肯定是他的问题，他会改变目标，就是实现第二个达到这效果的，案例。

系统音：
这一方面体现了你对业务的理解，另一方面体现了你对AI的理解，你对AI落地以及AI跟业务结合，的理解所以不要把重点放在介绍功能上面，那讲什么呢核心作业为什么这个场景一亮点体现你对模型边界的理解，和需求的理解第二模型为什么一开始做的不好，原因是什么怎么分析的这个一定要讲清楚第三基于上面这些问题你用了什么样的策略。

系统音：
进行优化第四我们怎么通过评测宣传平台比如现在很多人在做这种简易优化的，产品可能的问题，在于我们一开始发现大模型虽然能生成很多建议但用户满意度不高，那不高，的，原因是什么进一步拆解你会发现可能不是文案生成能力，而是更具体的首先模型不知道用户要投什么岗位所以建议太通用没有办法给个性化建议其次高兴会同时提出大量的修改意见，但用户不知道优先改什么改到什么程度，第三模型改出来的内容，相对流畅了一些但是因为幻觉的问题，偏离了用户真实的经历编造了一个这三个体都是我假设的当然是我们存在的梦，吉他着脚这些问题，你的a层架构怎么设计的那如何通过应用识别识别出用户最核心的需求，如何针对核心需求，是确定性给建议，等等，包括后面的评测体系怎么搭建如何推荐的迭代这才是非常完整的逻辑。

系统音：
第三类就要体现对前面敏感深度去讲自己的一款线上的理解和认知就千万不要想体验层或者一些用户的使用数据真正的产品敏感度是你能从一个新产品里快速判断这个点首先大概什么现在出现了其次它抓住了现阶段什么样的新的技术能力，第三什么决定了它的产品它可能有很强，这才是完整的不简单的理解

产品视角：如何分析"图工程"这个新概念

基于你分享的面试逻辑，我按照三个加分维度帮你拆解。



第一类：体现对最新技术应用的底层理解

不是讲"图工程是什么"，而是讲"它解决了产品的什么问题"。


核心问题：当前单Agent/纯LLM系统的痛点是什么？

痛点	具体表现	对产品的影响
流程不可控	用户问退款，LLM可能跳过查订单直接说"已退"	资金风险、客服纠纷
质量不稳定	同一个问题，今天回答好，明天回答差	用户信任度下降
复杂任务做不好	一个Agent同时处理分类、查数据、生成报告，什么都做一点但都做不好	用户觉得"不靠谱"
出错后无法自我修正	工具调用失败了，LLM直接编一个结果	幻觉、数据造假

图工程的本质解决方案

把"人的经验"编码成系统结构，而不是把所有决策权交给模型。


具体来说：

你知道的流程（比如退款必须先查订单）→ 用图的边固定下来，LLM没法跳过
需要LLM判断的环节（比如理解用户意图）→ 作为节点让模型自主决策
复杂任务（比如处理退款全流程）→ 嵌入一个完整智能体作为节点，让它内部自主规划

产品设计上的影响：你不再是设计一个"万能客服"，而是设计一个有明确分工的协作系统——每个环节由最合适的东西来处理（代码处理固定流程，模型处理需要理解的环节，智能体处理需要多步推理的复杂任务）。



第二类：体现实践认知——场景、问题分析、策略、评测

这一类需要你讲一个完整的案例故事。基于图工程的思想，你可以这样组织：


场景选择

不要讲"我做了个客服机器人"，要选一个有明确业务价值、复杂度适中、能让面试官看到你思考深度的场景。


推荐场景：B2B销售智能助手（面试官认知度高，业务复杂度适中）


第一步：模型为什么一开始做得不好（问题拆解）

"我们最初用单Agent模式做销售助手，发现三个核心问题："


问题一：流程跳步

模型经常在没查客户资料的情况下直接给销售建议
原因：模型不知道"先查资料再给建议"这个业务规则

问题二：任务执行质量差

让一个Agent同时做客户分析、商机判断、下一步行动建议、CRM更新
原因：上下文太长，模型顾此失彼

问题三：无法处理多步骤依赖

比如"生成跟进方案"依赖于"商机阶段判断"的结果，但模型经常同时输出两个，导致方案和阶段不匹配
原因：模型没有"先后顺序"的概念，一次性生成所有内容

第二步：你的策略——引入图架构

"基于这些问题，我们重新设计了系统架构。"


text
用户输入客户名称
      │
  ┌───▼────┐
  │ 数据采集 │  ← 固定流程（代码）：读取CRM数据、历史沟通记录
  └───┬────┘
      │
  ┌───▼───────┐
  │ 商机分析    │  ← Agent节点：综合判断商机阶段、风险点
  └───┬───────┘
      │
  ┌───▼───────────┐
  │ 方案生成        │  ← Agent节点：基于分析结果生成下一步建议
  └───┬───────────┘
      │
  ┌───▼────┐
  │ 审核校验 │  ← 固定流程（代码）：检查方案是否合规、数据是否准确
  └───┬────┘
      │
  ┌───▼────────┐
  │ 执行与更新   │  ← 固定流程（代码）：更新CRM、创建任务、发送通知
  └────────────┘
用户输入客户名称
      │
  ┌───▼────┐
  │ 数据采集 │  ← 固定流程（代码）：读取CRM数据、历史沟通记录
  └───┬────┘
      │
  ┌───▼───────┐
  │ 商机分析    │  ← Agent节点：综合判断商机阶段、风险点
  └───┬───────┘
      │
  ┌───▼───────────┐
  │ 方案生成        │  ← Agent节点：基于分析结果生成下一步建议
  └───┬───────────┘
      │
  ┌───▼────┐
  │ 审核校验 │  ← 固定流程（代码）：检查方案是否合规、数据是否准确
  └───┬────┘
      │
  ┌───▼────────┐
  │ 执行与更新   │  ← 固定流程（代码）：更新CRM、创建任务、发送通知
  └────────────┘

关键设计决策：

数据采集和执行更新用代码固定——这些环节不允许模型"自由发挥"，避免数据错误
商机分析和方案生成用Agent——这些需要模型的理解和推理能力
审核校验用代码——作为"安全阀"，防止模型幻觉造成的业务风险

第三步：评测体系怎么搭

"我们从三个维度建立了评测体系："


维度	评测方法	关键指标
流程完整性	检查每个步骤是否都执行了	流程跳步率从35%降到2%
分析准确度	对比模型判断的商机阶段 vs 人工判断	准确率从60%提升到85%
业务效果	追踪使用后销售的成单率变化	成单率提升15%

这个故事展示的能力

你能准确定位问题（不是笼统说"AI不好用"，而是拆解出具体原因）
你能选择合适的技术方案（知道什么时候该用代码、什么时候该用模型）
你能建立评测闭环（不只是做出来，还能衡量效果）


第三类：体现产品敏感度——对新概念的深层判断

不要只描述图工程是什么，要能快速判断它的产品含义。


当面试官问"你怎么看图工程这个概念"时，你的回答框架：


第一层：它抓住了什么本质变化？

"图工程不是一个新技术，而是一个新共识。它本质上反映的是：行业从'让AI做所有事'转向'让人和AI各自做擅长的事'。"


三年前大家的思路是：做一个全能Agent，什么都让它自己决定。
现在的思路是：人定流程，AI定细节——确定性的事用代码锁死，需要判断力的环节交给模型。


第二层：什么决定了它的产品价值？

"它的价值不在于技术本身，而在于它降低了AI产品的业务风险。"


纯Agent模式下，你无法保证AI不会跳过关键步骤、不会编造数据。但图架构下，关键流程是代码控制的，AI只能在你划定的范围内发挥。


这意味着AI产品可以进入更严肃的业务场景——金融、医疗、法律——这些场景不允许AI"自由发挥"。


第三层：它的局限是什么？

"图工程的前提是你能提前想清楚业务流程。但很多创新性的工作——比如深度研究、创意生成——你根本不知道下一步该做什么，硬画图反而会束缚AI的能力。"


所以未来的产品形态可能是混合的：核心业务流程用图来控制，探索性环节让AI自主发挥。这才是成熟的产品设计思维。



总结：面试中怎么用

面试官考察点	你展示的内容	来源
对新技术的理解深度	不讲名词，讲它解决了什么产品问题（流程不可控、质量不稳定）	第一类
实战经验与落地能力	销售助手案例：问题拆解→架构设计→评测体系	第二类
产品敏感度与判断力	三层判断：本质变化→产品价值→局限性	第三类

核心原则：不要让面试官觉得你只是读了一篇文章，要让他觉得你是从产品实战中理解了这个概念。

其实这个的话怎么说呢，感觉上还是对产品经理来说还是有一定的必要性的。因为他其实得知道说这个系统上怎么设置。虽然说肯定还是得和研发他们交互之类的。但是，这整体细节上感觉是产品经理也需要来去了解，是说每一个地方是怎么定义的，这样子方便排查评测，而不是是说完全的由研发来测试，也不会说去接手一个Agent的时候会拿着以前的老思路去做。嗯，我觉得这点应该还是有必要让产品经理了解这个概念的，你觉得呢

你说得对，而且你提的这几个点其实指向了一个更根本的原因：



产品经理为什么要懂这个

本质原因：Agent产品的"需求定义方式"变了

传统产品，产品经理定义的是功能——用户点这个按钮，系统返回什么结果。研发负责实现。


但Agent产品，产品经理定义的是行为边界——AI在哪些环节可以自主决策，哪些环节必须按固定流程走，遇到异常怎么处理。


如果你不懂图架构的思维方式，你写出来的需求文档可能是这样的：


"用户输入问题后，系统自动分析并给出退款方案。"


研发看到这个需求会问你：

分析失败了怎么办？重试几次？
退款金额谁来校验？模型还是代码？
中间需要用户确认吗？
数据查不到的时候直接告诉用户还是用缓存数据？

这些问题不是技术问题，是产品决策。 但如果你不懂系统是怎么流转的，你根本提不出这些问题，更别说做出正确的决策。



你说的那几点都很实际

第一，方便排查评测。


当用户反馈"AI回答错了"的时候，你需要知道：

是哪个节点出的问题？是分类错了，还是数据没查到，还是模型幻觉？
如果你不知道系统有哪几个节点、每个节点做什么，你根本定位不了问题

第二，不会完全依赖研发来测试。


Agent产品和传统产品不同——传统产品功能确定，测一遍就行。Agent产品的输出是概率性的，同样的输入可能得到不同的结果。产品经理需要自己设计测试用例、评估输出质量，而不是等研发说"测完了没问题"。


第三，接手Agent产品时不会用老思路。


这是最容易踩坑的。如果你用传统产品的思路来做Agent产品：

你会试图把所有case都写成规则（写不完的）
你会期望每次输出都一致（不可能的）
你会忽略"边界"设计——哪些事AI绝对不能做


所以产品经理需要理解到什么程度？

不需要你能写代码，但需要你能做到这三件事：


能力	具体表现
能画出系统的流程图	知道系统有几个节点、每个节点做什么、数据怎么流转
能定义每个节点的行为边界	哪些是固定流程、哪些是AI自主决策、异常怎么处理
能设计评测方案	知道从哪些维度衡量效果，出问题时能定位到哪个环节

你的判断是准确的——这确实不是"可选的加分项"，而是在Agent产品时代，产品经理的基本功。

我理解上面比较有重要比较核心的是，产品经理再去设计一款新的产品的时候，其实应该有一个更全新的一个框架跟思维了。然后就是从原来的这种workflow no的节点，然后又升级多了一个Agent的这一种类型。嗯，我理解是这个对产品经理应该只有这个是比较重要的，然后其他的比如说什么有边无边啊，然后是灵活拓展呀，这种其实也算需要了解的。然后，你从上面的内容中去告诉说，如果产品经理接到一个需求，然后他要怎么样从咱们上面这个东西去分析整体的框架应该怎么设计，那我们

产品经理接到需求后的分析框架

用一个具体例子走一遍完整流程。



需求背景

老板说："做一个AI智能助手，帮销售人员管理客户跟进。销售每天要跟几十个客户沟通，经常忘记跟进、搞混客户信息、不知道下一步该做什么。"



第一步：先搞清楚业务流程本身

不要一上来就想AI怎么设计，先问自己：


"这个事情如果是一个最优秀的销售来做，他脑子里的决策流程是什么？"


你可以通过跟业务方聊、观察销冠的工作方式，梳理出来：


text
销售拿到一个客户后，脑子里的流程：

1. 看客户基本信息（公司、职位、来源渠道）
2. 看历史沟通记录（聊了几次、聊了什么、客户反应如何）
3. 判断客户现在处于什么阶段（初步接触/有意向/在比价/要签约）
4. 根据阶段决定下一步动作（发资料/约演示/报价/催签约）
5. 执行动作（发邮件、打电话、更新CRM）
6. 事后记录（更新客户状态、写跟进备注）
销售拿到一个客户后，脑子里的流程：

1. 看客户基本信息（公司、职位、来源渠道）
2. 看历史沟通记录（聊了几次、聊了什么、客户反应如何）
3. 判断客户现在处于什么阶段（初步接触/有意向/在比价/要签约）
4. 根据阶段决定下一步动作（发资料/约演示/报价/催签约）
5. 执行动作（发邮件、打电话、更新CRM）
6. 事后记录（更新客户状态、写跟进备注）

这一步的关键产出：一张业务流程图，跟AI无关，纯粹是业务本身的逻辑。



第二步：识别每个环节的"确定性程度"

现在对着这张流程图，对每个环节问自己一个问题：


"这个环节，优秀销售的做法是相对固定的，还是需要大量判断？"


环节	确定性	判断依据
查客户基本信息	高	就是读数据库，没有判断空间
查历史沟通记录	高	就是拉记录，固定的
判断客户阶段	中	有判断标准但不绝对，需要综合多个信息
决定下一步动作	中	有常规套路但也要看具体情况
执行动作（发邮件等）	高	模板化的，执行即可
更新CRM	高	写数据，固定操作

这一步的关键产出：标注每个环节是"固定流程"还是"需要判断"。



第三步：根据确定性选择节点类型

这是图工程思维最核心的一步：


固定流程 → 用代码节点（workflow节点）需要判断 → 用Agent节点需要判断但有明确规则 → 用LLM单次调用节点


text
┌─────────────────────────────────────────────────┐
│                 销售智能助手                       │
│                                                   │
│  ┌──────────────┐                                │
│  │ 数据采集       │  ← 代码节点                    │
│  │ 读取CRM信息    │     固定的，不允许AI自由发挥     │
│  │ 拉取沟通记录   │                                │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 客户分析       │  ← Agent节点                   │
│  │ 判断商机阶段   │     需要综合多个信息做判断        │
│  │ 识别风险点     │     模型可以自主调用多个工具      │
│  │ 评估优先级     │     内部有推理循环               │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 方案生成       │  ← Agent节点                   │
│  │ 制定跟进策略   │     基于分析结果生成具体方案      │
│  │ 生成话术建议   │     需要创造性                   │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 校验与审批     │  ← 代码节点                    │
│  │ 检查方案合理性  │     规则固定的，不能让AI自己     │
│  │ 权限校验       │     判断自己有没有越权           │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 执行与记录     │  ← 代码节点                    │
│  │ 发送邮件       │     固定操作                    │
│  │ 更新CRM       │     不允许AI自由发挥             │
│  │ 创建待办任务   │                                │
│  └──────────────┘                                │
└─────────────────────────────────────────────────┘
┌─────────────────────────────────────────────────┐
│                 销售智能助手                       │
│                                                   │
│  ┌──────────────┐                                │
│  │ 数据采集       │  ← 代码节点                    │
│  │ 读取CRM信息    │     固定的，不允许AI自由发挥     │
│  │ 拉取沟通记录   │                                │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 客户分析       │  ← Agent节点                   │
│  │ 判断商机阶段   │     需要综合多个信息做判断        │
│  │ 识别风险点     │     模型可以自主调用多个工具      │
│  │ 评估优先级     │     内部有推理循环               │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 方案生成       │  ← Agent节点                   │
│  │ 制定跟进策略   │     基于分析结果生成具体方案      │
│  │ 生成话术建议   │     需要创造性                   │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 校验与审批     │  ← 代码节点                    │
│  │ 检查方案合理性  │     规则固定的，不能让AI自己     │
│  │ 权限校验       │     判断自己有没有越权           │
│  └──────┬───────┘                                │
│         ▼                                         │
│  ┌──────────────┐                                │
│  │ 执行与记录     │  ← 代码节点                    │
│  │ 发送邮件       │     固定操作                    │
│  │ 更新CRM       │     不允许AI自由发挥             │
│  │ 创建待办任务   │                                │
│  └──────────────┘                                │
└─────────────────────────────────────────────────┘

为什么这样分？核心逻辑是：


场景	选什么	原因
每次结果必须一样	代码节点	AI有随机性，固定流程不能交给它
出错了有业务风险	代码节点	比如发错邮件、改错数据，必须由代码控制
需要理解、综合、判断	Agent节点	比如"这个客户现在处于什么阶段"，需要模型推理
需要多步推理才能完成	Agent节点	比如生成跟进方案，需要先分析再生成再验证
简单的分类或提取	LLM单次调用	不需要Agent的循环能力，一次调用就够


第四步：定义每个节点的边界（产品需求文档）

这一步是产品经理真正要写的东西。以"客户分析"这个Agent节点为例：


markdown
## 节点：客户分析

### 输入
- 客户基本信息（公司、职位、来源）
- 历史沟通记录（最近10条）
- CRM中的商机阶段（如有）

### 输出
- 判断的商机阶段：初步接触 / 需求确认 / 方案评估 / 商务谈判 / 即将签约
- 风险标记：高/中/低，附理由
- 优先级评分：1-10
- 判断依据摘要（一句话）

### 行为边界
- 必须先读取数据再判断，不能凭空猜测
- 如果历史记录少于2条，阶段标记为"信息不足"，不能强行判断
- 不能自己去联系客户，只能分析现有数据
- 如果CRM中有明确阶段标记且更新时间在7天内，以CRM为准

### 异常处理
- 数据读取失败：返回错误，不生成分析
- 信息不足：返回"信息不足"状态，建议销售补充信息后再分析
## 节点：客户分析

### 输入
- 客户基本信息（公司、职位、来源）
- 历史沟通记录（最近10条）
- CRM中的商机阶段（如有）

### 输出
- 判断的商机阶段：初步接触 / 需求确认 / 方案评估 / 商务谈判 / 即将签约
- 风险标记：高/中/低，附理由
- 优先级评分：1-10
- 判断依据摘要（一句话）

### 行为边界
- 必须先读取数据再判断，不能凭空猜测
- 如果历史记录少于2条，阶段标记为"信息不足"，不能强行判断
- 不能自己去联系客户，只能分析现有数据
- 如果CRM中有明确阶段标记且更新时间在7天内，以CRM为准

### 异常处理
- 数据读取失败：返回错误，不生成分析
- 信息不足：返回"信息不足"状态，建议销售补充信息后再分析

注意看：这些全是产品决策，不是技术实现。 但如果你不懂系统是怎么运作的，你写不出这些边界。



第五步：设计评测方案

在需求阶段就要想好怎么验收：


维度	评测方法	达标标准
流程完整性	拿100个真实客户跑一遍，检查是否每个节点都执行了	流程完成率>98%
分析准确度	取50个客户，AI判断的阶段 vs 销售主管人工判断	一致率>80%
方案可用性	销售收到建议后，实际采纳率	采纳率>60%
安全边界	故意测试越权场景（比如让AI发邮件给不该发的人）	拦截率100%


整体框架总结

text
接到需求
   │
   ▼
第一步：梳理业务本身的流程（不考虑AI）
   │
   ▼
第二步：标注每个环节的确定性程度
   │
   ▼
第三步：根据确定性选择节点类型
   │     固定 → 代码节点
   │     需判断 → Agent节点
   │     简单判断 → LLM单次调用节点
   │
   ▼
第四步：定义每个节点的输入/输出/边界/异常处理
   │
   ▼
第五步：设计评测方案（需求阶段就定好怎么验收）
接到需求
   │
   ▼
第一步：梳理业务本身的流程（不考虑AI）
   │
   ▼
第二步：标注每个环节的确定性程度
   │
   ▼
第三步：根据确定性选择节点类型
   │     固定 → 代码节点
   │     需判断 → Agent节点
   │     简单判断 → LLM单次调用节点
   │
   ▼
第四步：定义每个节点的输入/输出/边界/异常处理
   │
   ▼
第五步：设计评测方案（需求阶段就定好怎么验收）

这就是图工程思维给产品经理带来的最大价值：不是让你去写代码，而是让你在需求阶段就把"系统怎么运转"想清楚、定义清楚、验收标准定清楚。

如果我要去介绍，把这个去当做公众号去写，然后我估计要介绍这个概念，这个这个概念的问题。第3个是这个对AI产品经理他的帮助是什么？嗯，然后你去你觉得我这3个合符合有爆款的潜质吗？然后基于这一个去重新的去梳理一下，我整体如果说合适的话，我每个对应的结构也是怎么样子的，以及我想表达的内容是什么。

公众号文章结构分析

先说结论：三个板块的逻辑对，但顺序和包装需要调整

你现在的顺序是：概念 → 问题 → 帮助


这个顺序读起来像教科书，读者打开看两段就会划走。


爆款的逻辑应该是：先戳痛 → 再给解法 → 最后升维认知



调整后的三个板块

板块	你要表达的	作用
板块一：痛点	AI产品经理设计Agent产品时遇到的真实困境	让读者产生共鸣，"这说的不就是我吗"
板块二：解法	图工程这个概念到底在说什么，用大白话讲清楚	给读者"信息差"，让他觉得学到了新东西
板块三：帮助	作为AI产品经理，你该怎么用这个思维去做产品	让读者有行动指引，觉得"明天上班就能用"


每个板块的详细结构

板块一：痛点——"你是不是也踩过这些坑"

你要表达的核心： 当前很多AI产品经理在设计Agent产品时，思路是错的，导致产品不稳定、不好用、不好迭代。


具体内容：


坑1：把所有事都交给一个Agent

你跟研发说"做个智能客服，用户问什么它就答什么"
结果：有时候答得好，有时候乱答，出了问题你不知道是哪个环节出了错
本质问题：你没有定义系统该怎么运转，全靠模型自己判断

坑2：出了问题不知道怎么改

用户反馈"AI回答错了"，你想优化，但不知道该改哪里
是提示词的问题？工具调用的问题？还是流程本身就设计错了？
本质问题：系统是一个黑盒，你没有把流程拆开

坑3：接手Agent产品时无从下手

上一个产品经理离职了，留给你一个Agent产品
你不知道它的设计逻辑是什么，只能不断打补丁
本质问题：产品设计时没有明确的架构，全靠"调prompt"迭代

写作要点：

用"你"来写，让读者觉得在说他
每个坑用一个真实场景（不需要具体公司，但要具体）
不要一上来就抛概念，先让读者痛了再给解法


板块二：解法——"图工程到底在说什么"

你要表达的核心： 图工程不是什么新技术，而是一种思维方式——在设计Agent产品时，先想清楚系统该怎么运转，再决定哪些环节用AI、哪些环节用代码。


具体内容（分三层讲）：


第一层：一句话说清楚


图工程就是：在让AI干活之前，你先画好一张流程图，规定好每一步该干什么、谁来干、出错了怎么办。


第二层：用一个生活化比喻


你可以这样写：


想象你在管理一个团队。有两种管理方式：


方式一：招一个全能员工，告诉他"帮客户解决问题"，然后放手不管。结果有时候做得好，有时候搞砸了，你也不知道哪里出了问题。


方式二：你先想清楚整个服务流程——第一步接待、第二步分类、第三步处理、第四步确认。简单的步骤让实习生按流程做（代码），需要判断的步骤让资深员工来（AI），关键节点你自己把关（校验）。


图工程就是方式二。


第三层：具体怎么操作


用那个销售助手的例子，但不需要写代码，用文字描述就够了：


比如你要做一个销售智能助手，帮助销售管理客户跟进。


不是一上来就让AI"帮销售管理客户"，而是先想清楚：


1.查客户资料 → 这步是固定的，读数据库就行，不需要AI判断 → 用代码
2.判断客户处于什么阶段 → 这步需要综合分析，需要AI的能力 → 用Agent
3.生成跟进建议 → 这步需要创造性，需要AI → 用Agent
4.发送邮件、更新系统 → 这步是固定的，而且出错了有风险 → 用代码
你把这四步画成一张图，每一步定义好输入什么、输出什么、出错了怎么办，这就完成了产品设计的核心框架。


写作要点：

这一部分不要写太长，读者理解了就行
不要堆技术名词（DAG、状态机、边节点这些一个都不要出现）
用比喻和例子代替定义


板块三：帮助——"这对你做AI产品有什么用"

你要表达的核心： 图工程思维改变了AI产品经理的工作方式，让你从"写需求描述功能"升级为"设计系统行为边界"。


具体内容（分三个点）：


第一，你终于能写清楚需求了


以前你写需求："用户输入问题，系统分析后给出建议。"


研发问你：分析失败了怎么办？建议需要用户确认吗？数据查不到用什么兜底？


你答不上来，因为你没想过这些。


用图工程思维后，你会在需求阶段就定义清楚：

每个步骤是谁来做（代码还是AI）
输入是什么、输出是什么
出错了怎么处理
哪些地方需要用户确认
研发看到这种需求，会少跟你吵很多架。


第二，你终于能排查问题了


用户说"AI回答错了"，以前你只能去找研发说"帮我看看"。


现在你知道系统有几个节点，你能判断：

是数据没拉到？（第一个节点的问题）
还是阶段判断错了？（第二个节点的问题）
还是方案生成有幻觉？（第三个节点的问题）
你能直接告诉研发"问题出在客户分析节点，它把初步接触的客户判断成了商机阶段"，研发立刻就能定位。


第三，你终于能设计评测方案了


以前验收AI产品，你只能说"感觉有时候不太准"。


现在你能设计评测：

流程完成率（有没有漏步骤）
各节点准确率（每个环节的判断对不对）
边界安全性（AI有没有做不该做的事）
你不再是"凭感觉验收"，而是有数据、有标准地验收。


写作要点：

每个点都用"以前 vs 现在"的对比，让读者感受到差距
用具体场景而不是抽象描述
最后可以加一个总结性的观点


结尾：一句话升华

可以用类似这样的结尾：


图工程的本质不是什么新技术，而是一个朴素的道理：


在让AI做事情之前，你得先想清楚这件事该怎么干。


这不就是产品经理最该干的事吗？



整体结构一览

text
标题：扎心类（带"AI产品经理"关键词）

开头：直接抛一个痛点场景，3-5行，不废话

板块一：你踩过这些坑吗（痛点，3个小坑）
  ↓  制造共鸣

板块二：到底什么是图工程（解法，比喻+例子）
  ↓  给出信息差

板块三：这对你做产品有什么用（帮助，3个对比）
  ↓  给行动指引

结尾：一句话升华认知
标题：扎心类（带"AI产品经理"关键词）

开头：直接抛一个痛点场景，3-5行，不废话

板块一：你踩过这些坑吗（痛点，3个小坑）
  ↓  制造共鸣

板块二：到底什么是图工程（解法，比喻+例子）
  ↓  给出信息差

板块三：这对你做产品有什么用（帮助，3个对比）
  ↓  给行动指引

结尾：一句话升华认知

这个结构的核心逻辑是：先让读者觉得自己有问题，再给他一个他没听过的解法，最后告诉他学了之后能立刻用起来。 这是公众号爆款文章最稳的结构之一。

有问题，尽管问，Shift + Enter 换行
引用来源（0）
